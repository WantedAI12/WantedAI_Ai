# V80 전체 AI 기능 통합 배포

2026-09-15 15:23:07 KST, 기존 `perfumery-ai-core` 앱에 전체 기능을 Modal v18로 배포했다. 이후 저장 후보 원문 보존 문제를 수정하여 **15:44:30 KST, Modal v19**로 재배포했다. 기존 V70 이후 누적 소스 변경과 V80 참조 지원 범위를 같은 패키지에 포함했다.

## 주소와 실행 구성

- API: https://junseong2im--perfumery-ai-core-web.modal.run
- 기존 백엔드 키·인증 방식 유지. 새 Proxy Token은 생성하지 않았다.
- 웹 함수: CPU 1, 메모리 1GB, 최소 0 / 최대 1 컨테이너, 요청 제한 시간 300초.
- 대화 보조: 기존 Qwen3-0.6B-Q8_0 CPU 워커, 메모리 2GB, 필요 시 실행.
- 조향식 자체는 외부 LLM API 호출로 생성하지 않는다.
- 배포 진입점: `deploy/modal_release_v80.py`.
- 롤백 기준은 이전 Modal v17 / V69이며 해당 배포 파일은 보존했다.

## 포함 기능

1. V76 공유 신경망과 자기회귀 배합 수정, 전체 원료 후보 검색, 가격·사용량 제약.
2. 향수·바디로션·바디워시의 구분된 예측 경로, 시간별 향·농도·방출 및 물성 계산.
3. 제조 절차 계획, 유화 예측, 실제 자료 기반 수성 제형 결과 예측, 분자 쌍 향 주석.
4. 자연어 해석, 보완 질문·답변, 후보 생성·비교·수정·재평가, 저장된 후보·버전 정보 처리.
5. 감사 로그·보고서 JSON, 생성 진행 상황·감사·보고서 SSE.
6. IFRA·EU REACH·K-REACH·FDA 공개 자료 조회 및 등록 증거 확인 경로.
7. 캐시·중복 조회 감소, 한정된 동시 실행, 기존 인증, 대화용 소형 언어모델.

“전체 기능 포함”은 필요한 증거가 없는 요청을 승인하거나 모든 향 요청이 95점을 통과한다는 뜻은 아니다. 프로젝트 저장과 실제 운영 승인은 팀 백엔드 및 운영자의 담당 경계를 유지한다.

## V80 자료 지원 범위

- 참조 프로필 630개, 생성 지원 표현 657개.
- 과거 감사 대상 134개 중 132개에 참조 경로를 연결했다. 일부는 제한된 예시 또는 모델 추정이며, 순무의 일부 관능 항목은 미해석 상태를 보존한다.
- `industrial`은 특정 정량 향 목표가 없어 생성 목록에서 제외했다.
- `odorless`는 양의 향이 아니라 별도 무취 조건으로 분류한다.
- 그 외 일반 문맥·수식어는 단독 생성 목록에서 제외한다. 원본 어휘 3,809개는 삭제하지 않는다.
- 목록 API는 기본적으로 지원 대상만 표시한다. 감사·점검 목적의 `include_unavailable=true`로 비활성 기록을 조회할 수 있다.
- 일부만 해석된 요청의 후보는 `closest_candidate`로 전달하고, 부분 점수와 전체 점수를 분리한다. 미확인 제외 조건은 무시하지 않는다.

탄 양초·분필·일부 체취·파인오일 표현은 기존 Atlas 관측 자료를 다시 연결했고, 부티릭·염소 계열은 기존 학습 주석을 연결했다. 소시지는 [공개 냄새활성 연구](https://pmc.ncbi.nlm.nih.gov/articles/PMC11582465/), 순무는 [공개 관능 연구](https://pmc.ncbi.nlm.nih.gov/articles/PMC7700530/)를 사용한다. 원 논문의 숫자와 우리 146축으로 투영한 모델 참조를 구분한다. 논문 자료를 향수의 실제 후각 정확도나 제조 레시피라고 표시하지 않는다.

## 동일성 및 검증 기록

- 신경망: V76, `158b9f82ff9fb784eeec114ef1f7bbfc4d613badeb98f46e0f60435d5af38d2f`.
- wheel: `309354bd083013366f244838dc705d4c70e5503985ae481deeef59902490f1b6`.
- 비공개 배포 묶음: `480e68a906232c545d62c159a8cbf3cbfde94e7cc4f7b4120d8729853b29849d`.
- 원료 행 29,259개 및 기존 원료 값·활성 정책 보존. 자료 연결과 원료 추가 투입 허가는 다른 사실이다.
- 관련 회귀 179개와 V80 배포 구조 테스트 2개 통과.
- 해시 원문 보존 수정 후 관련 80개 테스트 통과. 배포 v19에서도 정상 비교·수정 200, 변조 데이터 422를 확인했다. 앞선 테스트와 중복되므로 단순 합산하지 않는다.
- 설치한 패키지 및 배포 이미지의 생성·로션·SSE·캐시·지원 목록 검사를 통과했다.
- 배포된 이미지 검사는 기존 Modal 관리자 인증과 컨테이너 내부 ASGI 요청으로 수행했다. 공개 URL의 무인증 요청은 401로 차단됐다. 백엔드 인증키를 이용한 외부 HTTPS 호출 검증과는 구분한다.

상세 기록:

- `.benchmarks/v80_supported_release/deployed-image-verification-01.json`
- `.benchmarks/v80_supported_release/deployed-image-verification-02.json`
- `.benchmarks/v80_supported_release/deployed-language-01.json`
- `.benchmarks/v80_supported_release/installed-api-02/report.json`
- `.benchmarks/v80_supported_release/regression-02.xml`
- `.benchmarks/v80_supported_release/full-api-01/`
- `.benchmarks/v80_supported_release/full-api-replay-01/report.json`

전수 검사와 수정 후 재검사를 합쳐 공개 API 45개 연산의 유효 응답 또는 의도된 증거 차단 경로를 확인했다. 초기 검사 중 발견한 해시 오류는 v19에서 고쳤다. 조사 도중 로컬 소스 변경을 감지해 중단된 나머지 3건은 고정된 wheel로 재검사하여 통과했다. 초기 실패 기록은 삭제하지 않았다.

원본 수집 자료와 학습 데이터 전체를 공개 배포하지 않았다. 인증된 연구 서비스에 필요한 파일만 배포했으며, 외부 자료의 상업적 이용·재배포 권리를 새로 취득했다고 주장하지 않는다.

## 저장 후보 해시 수정

중첩 JSON의 문자열 공백이 입력 검증 중 제거되는 것이 원인이었다. 기존 콘텐츠와 해시는 정상이었지만 검증기가 내용을 바꾼 뒤 다시 검사했다. 체크섬 대상 JSON을 변환 없이 검증하고 복사해 보존하도록 수정했다. 식별자의 공백 처리만 별도로 유지했다. JSON 형식·유한한 숫자·크기·중첩 깊이·해시·후보 조성·승인 상태 검사는 유지한다. 검사 문턱을 없애거나 변조된 기록을 재해시하여 허용하지 않는다.
