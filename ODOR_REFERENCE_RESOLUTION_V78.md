# V78 향 참조 연결과 배포 보류

2026-09-15. 미확보 항목 전체를 조사했지만 모든 정량 참조를 확보한 것은 아니다. 사용자가 **남은 향의 정량 참조를 모두 확보한 뒤 배포**하도록 지정했으므로 Modal은 변경하지 않는다.

## 현재 결과

- V77의 전체 3,809개 개념과 미연결 3,595개 항목을 보존하여 전수 분류했다.
- 기존 관측 자료를 이용한 명명 분자 참조 6개, 명시적으로 표시한 모델 추정 참조 263개, 분류 조합 17개, 동의어 연결 30개를 추가 연결했다. 총 316개 항목이다.
- 정량 참조 프로필은 203개에서 489개가 됐다. 여러 별칭이 같은 참조를 가리키므로 프로필 수와 개념 수는 다르다.
- 남은 항목은 실제 향 표기 128개, 일반 문맥 어휘 3,092개, 특성/수식어 55개, 세부 종류 지정이 필요한 분류명 4개다.
- 이 분류가 없는 자료를 채워 넣었다는 뜻은 아니다. ‘ethyl’, ‘methyl’, ‘무취’ 등의 표현을 새 냄새 강도 벡터로 만들지 않는다.

[128개 실제 목록과 확인할 부분](REMAINING_ODOR_REFERENCES_128.md)

## 자료 확보와 연결 방법

기존 Atlas/Dravnieks 관찰 프로필은 보존했다. 명명 화학물질의 실제 관찰 프로필은 그 물질 자체의 참조로 연결하고, 일반 향 계열의 참조 평균과 구분한다.

출처에 해당 향으로 표시된 서로 다른 분자가 충분한 경우에는 기존 V76 분자 모델로 전체 146축 프로필을 추정하여 고정 참조를 구성했다. 이는 **모델 추정 참조**이며 실측 강도나 독립적인 인간 후각 검증이 아니다. 대상 레시피나 현재 후보 원료 집합으로 정답을 맞춰 만들지 않는다. 기존 관측 참조 벡터와 출처 메타데이터 변경은 검증에서 거부한다.

추가로 [Pyrfume 공개 자료](https://github.com/pyrfume/pyrfume-data)의 Flavornet, Good Scents, Arctander, Leffingwell, Sigma, IFRA, AromaDB를 고정 commit `8054ea98ed675005ec10e67359902f500e4911b0`에서 확보하고 원본별 SHA256을 보존했다. 전체 표를 대조했고, 향 묘사의 유무를 강도 실측값으로 바꾸지 않았다. Sigma의 식별자 형식 차이도 처리했다. 분자 구조가 없거나 혼합물인 행은 단일 분자의 자료로 강제 연결하지 않았다.

Arctander 자료의 비상업적 이용 조건, Flavornet의 원 출처 표기, Leffingwell 이용 조건 파일의 미확인 상태 등을 보존한다. 수집했다는 이유로 상업적 재배포 권리가 확보됐다고 주장하지 않는다. 원본 데이터와 체크포인트는 이 공개 저장소에 추가하지 않는다.

추가 대조에서 정량 경로를 더 연결한 항목은 아세트알데하이드·피라진·스타이렌의 명명 분자 모델 참조와 마호가니·니트로머스크·알리아세우스의 출처 조건부 모델 참조다. 벤조인 수지와 동명 화학물질의 혼동, ‘무취’를 양의 향 벡터로 정규화하는 오류는 차단했다.

## API와 계산

- 기존 V76 신경망 가중치는 유지한다. V78은 새로운 신경망 학습 완료 선언이 아니다.
- 향수·로션은 공통 세부 향 목표를 사용하고 제품별 물리 계산은 구분한다.
- `reference_evidence`에 관측/모델 추정 여부와 출처 식별자 수를 반환한다. 모델과 같은 예측기를 사용해 구성한 참조에는 독립 검증이 아니라 자기 일관성 계산이라는 표시가 있다.
- 새 참조가 있는 세부 향은 기존 19축 연결이 없던 경우에도 해당 참조의 고정된 초기 탐색용 투영을 사용할 수 있다. 최종 판정은 전체 프로필로 한다.
- 아직 자료가 없는 향은 원인을 반환하며 자동 통과하지 않는다. 기존 주소·키·배포는 변경하지 않았다.

## 검증 및 재현 파일

- 최종 자료: `.benchmarks/v78_source_resolution/resolve-07/`
- 추가 공개 자료: `.benchmarks/v78_source_resolution/additional-sources-02/`
- 7개 자료 전수 연결 조사: `.benchmarks/v78_source_resolution/external-evidence-02/`
- 설치 패키지 API 검사: `.benchmarks/v78_source_resolution/api-01/`
- 전체 로컬 회귀 검사: `.benchmarks/v78_source_resolution/regression-full-01.xml`
- wheel/source 결합: `.benchmarks/v78_source_resolution/package-01/preparation.json`
- 로컬 검증용 프로필: `perfumery.v78-integrated-01.local.json`

기존 400개 개발 요청의 목표 해석 검사는 레시피 95점 통과율과 별개다. 데이터 파일 안의 `all_3595_measured_profiles_completed`는 계속 false다.

## 배포 조건

처음 미확보로 분류한 134개 항목을 고정된 요구 목록으로 저장한다. 현재 6개를 더 연결했으므로 128개가 남았다. 항목 이름을 삭제하거나 다른 분류로 옮겨서 조건을 통과시킬 수 없다.

`deploy/runtime_release_v78.py`의 `assert_deployment_ready`는 필요한 모든 참조가 존재하기 전에는 `DEPLOYMENT_HELD`로 실패한다. 로컬 묶음 조립과 API 검사는 가능하지만 배포 승인은 아니다. CI는 코드와 차단 기능을 검증하며 실제 자료 준비 완료를 대신하지 않는다.

운영은 V69를 유지한다. PR 작성 또는 dev push는 Modal 배포를 자동으로 실행하지 않는다.
