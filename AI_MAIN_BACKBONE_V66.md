# V66 메인 분자 향 모델: 직접 구현한 학습 수식과 CPU 추론

2026-09-12. 로컬 연구 실행용 개선판이며 소스와 재현 절차를 `dev` 변경에 포함합니다. 운영 배포는 하지 않았습니다. 외부 조향 프로젝트나 외부 학습 체크포인트를 붙이지 않고, 기존 분자 특징과 공개 관측값을 사용해 우리 학습기와 실행 코드를 변경했습니다. 로컬 학습 체크포인트와 실행 설정은 Git 업로드 대상이 아닙니다.

## 1. 이번에 실제 바뀐 부분

- 146개 출력을 독립적으로 규제하던 방식에서, 훈련 자료의 출력 공분산을 사용하는 다중 출력 커널 회귀로 변경했습니다.
- 절편을 목표 평균으로 고정하던 근사 대신, 절편을 포함한 정상방정식을 정확하게 풉니다.
- 향 기술이 없는 분자에서 자기 유사도가 낮아져 규제가 달라지던 커널을 자기 유사도로 정규화합니다. 누락된 향 기술 자체를 새 관측값으로 채우지는 않습니다.
- 역행렬이나 거대한 Kronecker 행렬 없이 두 고유기저에서 풉니다. 양의 준정부호, 수치 잔차, 절편 정류 조건을 검사합니다.
- 실행 시 두 측정 출력이 공유하는 입력 커널은 한 번만 계산합니다. 읽기 전용 계수와 256행 배치로 반복 검증·메모리 낭비를 줄였습니다. 256은 처리 묶음 크기이며 원료 수 제한이 아닙니다.
- 원래 stock-mixture 학습에 사용한 V54 부모와, 완제품 향 예측에 사용할 V66 백본의 역할을 명시적으로 분리했습니다. 기존 시료 모델에 새 분자 출력을 몰래 대입하지 않습니다.

## 2. 수학적 정의

분자 입력은 기존 계약을 보존합니다. 구조 fingerprint 1,024개, 물성 특징 16개, 출처 있는 native 향 프로필과 누락 표시, 출처 있는 향 기술, high/low 범주를 사용합니다. 물성 표준화는 매 학습 fold 안에서만 계산합니다.

기존 구조·물성·향 기술·범주 조건 커널을 `k`라 하면:

\[
\widetilde k(x,z)=\frac{k(x,z)}{\sqrt{k(x,x)k(z,z)}}.
\]

자기 유사도가 0인 특징의 교차 커널은 0으로 유지합니다. 정규화가 없는 향 기술이나 냄새 강도 측정을 만들어 내지는 않습니다. 구조 특징과 물성의 기여 계수도 검증 집합 결과로 사후 조작하지 않습니다.

표본 수 `n`, 출력 수 `d=146`, 훈련 표적 `Y`, 계수 `C`, 절편 `b`에 대해:

\[
\min_{C,b}\;\|Y-KC-\mathbf1b^T\|_F^2
 +\alpha\,\operatorname{tr}(C^T K C B^{-1}).
\]

출력 관계 행렬은 학습 자료 안에서만 추정합니다.

\[
B=(1-\rho)I+\rho\frac{d\,\operatorname{Cov}_{train}(Y)}
 {\operatorname{tr}(\operatorname{Cov}_{train}(Y))},\qquad 0\le\rho<1.
\]

상수 출력이면 `B=I`를 사용합니다. `rho<1`의 identity 수축으로 표본 수가 출력 수보다 작아도 행렬을 가역으로 유지합니다. 출력의 통계적 상관을 화학 결합이나 후각 수용체 상호작용이라고 해석하지 않습니다.

`H=I−11ᵀ/n`, `Kc=HKH`, `Yc=HY`를 두면:

\[
K_c C+\alpha C B^{-1}=Y_c,\quad \mathbf1^TC=0,
\qquad b^T=\overline Y-\overline K C.
\]

`Kc=U diag(lambda) Uᵀ`, `B=V diag(theta) Vᵀ`의 두 고유기저에서:

\[
C=U\left[\frac{U^T Y_c V}{\lambda_i+\alpha/\theta_j}\right]V^T.
\]

대괄호의 나눗셈은 원소별입니다. 실제 구현은 이 공식을 사용합니다. 독립 테스트에서 작은 확장 선형계와 Kronecker 직접 해법에 대조했습니다. 음의 상수 모드가 중심화로 숨는 반례도 거부합니다.

입력 커널이 유효하다는 전제 아래 학습의 주 계산량은 `O(n³+d³+n²d+nd²)`입니다. 추론에는 학습 시 고유분해가 필요하지 않습니다. 최종 두 출력은 모두 `alpha=0.1`, 향 기술 가중치 `0.5`, 제곱근 표적 변환, `rho=0.9`로 선택되었습니다. 역변환 전에 음수 잠재값을 0으로 제한합니다.

최종 정상방정식 상대 잔차는 applicability `3.18e-15`, use `3.22e-15`입니다. 계산이 수치적으로 정확하다는 증거이며, 그 자체로 향 예측이 정확하다는 증거는 아닙니다.

## 3. 화학·물리·환경 조건과의 연결

분자 향 모델과 제형 방출 모델은 물리적으로 다른 문제입니다. V66은 분자 구조와 기존 물성에서 향 기술 프로필을 추정합니다. V65의 제형 방출 계산은 증발·재유입·피부 흡수·분해·환기·세척 및 시간 적분을 담당합니다. 실제 제품의 온도·습도·상분율·전달 계수가 주어지는 계약은 그대로 유지합니다.

Atlas의 high/low는 범주형 관측 조건입니다. 이를 임의의 ppm, 증기압, 온도 보정값으로 바꾸어 학습하지 않았습니다. 새로운 활동도 계수, 유화 분배계수, 냄새 역치를 측정한 것처럼 만들어 넣지도 않았습니다.

실제 고정 물리 가중치와 카탈로그를 사용한 향수·로션·바디워시 3개 통합 경로에서 V66 SHA가 반환됩니다. 동일하게 주어진 합성 공정 계수에서는 기존 물리 질량·공기 농도 결과가 정확히 유지됩니다. 최대 질량수지 오차는 `2.61e-18 mg/cm²` 미만입니다. 이 확인은 수치 통합 시험이며 실제 제품의 방출 측정은 아닙니다.

## 4. 재학습과 비교 결과

자료는 기존 source 160개 조건 행입니다. 분자 구조가 있는 144개 행, 서로 다른 명시 분자 138개를 예측에 사용합니다. 구조 미해결 16개 행은 전체 분모에서 삭제하지 않습니다. 두 측정 head의 146개 출력을 모두 유지합니다.

분자 분리와 Murcko 골격 분리 각각 외부 5-fold, 내부 그룹 3-fold를 사용했습니다. 비고리 분자는 빈 골격 하나의 그룹으로 묶었습니다. 내부에서 108개 조합을 선택하고, 외부 fold 결과로 해당 fold의 파라미터를 조정하지 않습니다. V54 비교 모델도 같은 분할에서 다시 적합했습니다. 레시피 400개 결과는 학습에 사용하지 않았습니다.

| 분리 / 출력 | V54 평균 절대오차 | V66 평균 절대오차 | V54 코사인 거리 | V66 코사인 거리 |
|---|---:|---:|---:|---:|
| 분자 / applicability | 1.98119 | 1.93831 | 0.189899 | 0.187988 |
| 분자 / use | 2.77257 | 2.69513 | 0.167784 | 0.166462 |
| 골격 / applicability | 2.27671 | 2.25505 | 0.229213 | 0.223792 |
| 골격 / use | 3.24176 | 3.20971 | 0.211254 | 0.208039 |

오차와 거리는 작을수록 좋습니다. 네 경우 모두 점추정이 개선되고 예측 가능한 행 수는 감소하지 않았습니다. **그러나 모든 코사인 개선 신뢰구간과 골격 분리 MAE 개선 신뢰구간은 0을 포함합니다.** 분자 분리 MAE 감소는 약 2.16%, 2.79%이며 이것이 사용자가 실제 느끼는 향의 정확도 퍼센트는 아닙니다. 기존에 여러 번 사용한 source의 개발 평가이며 새로운 블라인드 평가라고 부르지 않습니다.

## 5. 실제 CPU 추론과 버전 연결

같은 체크포인트의 이식형 기준 구현과 최적화 실행을 1,152행에서 비교했습니다. 순서를 교대해 각 8회 측정한 커널 추론 중앙값입니다. 분자 특징 추출·전체 조향 탐색·HTTP 시간은 포함하지 않습니다.

| 체크포인트 | 기준 구현 | 최적화 구현 | 최대 절대 차이 |
|---|---:|---:|---:|
| V54 | 56.68ms | 21.89ms | 4.27e-14 미만 |
| V66 | 53.58ms | 21.05ms | 4.27e-14 미만 |

V66 핵심 추론은 이 측정에서 약 60.7% 단축됐습니다. 원료를 줄이거나 출력 축을 생략하지 않았습니다.

로컬 선택:

- `atlas`: stock-mixture 학습 당시 V54. 기존 물리 모델의 학습 부모 해시도 유지합니다.
- `odor_backbone`: 새 V66. 로션 조향 탐색과 세 제품의 통합 향 프로필에 사용합니다.
- 새 백본은 명시된 해시, 직접 부모, 같은 source·출력 축·특징 계약 및 개발 비퇴행 판정을 통과해야 합니다. 다른 모델로 조용히 대체하지 않습니다.
- 관측 기준 프로필은 같은 원자료에서 재생성해 V66에 연결했습니다. 모든 목표 프로필·배경·개념 메타데이터·source·source 행은 기존 파일과 정확히 같습니다. 목표 향이나 95점 문턱을 바꾸지 않았습니다.
- 기존 향수 `/v1/formulas`의 V45/19축 주 평가와 stock-assay 회귀는 이번에 재학습한 대상이 아닙니다. V66이 이 경로까지 전부 대체했다고 표현하지 않습니다.

## 6. 조향 요청과 회귀 검증 결과

실제 로컬 카탈로그·V66 가중치로 이전과 같은 요청 8건을 실행했습니다. 이전 V65 기록과 비교한 개발 진단이며, 새 블라인드 평가나 전체 400건 평가가 아닙니다.

| 요청 | V65 점수 | V66 점수 | 95점 통과 |
|---|---:|---:|---|
| fresh scent | 95.0000 | 95.2000 | 통과 유지 |
| clean scent | 88.8863 | 89.1100 | 미달 |
| green scent | 93.4095 | 93.9880 | 미달 |
| woody scent | 95.0000 | 95.2000 | 통과 유지 |
| musky scent | 92.0212 | 92.0753 | 미달 |
| clean, powdery scent | 93.2870 | 93.3647 | 미달 |
| clean, leathery scent | 92.1835 | 92.2659 | 미달 |
| clean, earthy scent | 93.4329 | 93.6723 | 미달 |

통과 건수는 **2/8로 동일**합니다. 오류 0건, 탐색이 완료됐다고 보장하지 않는 미달 6건을 그대로 기록했습니다. 이번 수정으로 통과율이 올랐다고 표현하지 않습니다.

실제 로컬 ASGI 호출은 향수 `/v1/formulas`와 로션 `/v1/applications/body-lotion/design` 모두 HTTP 200, 동일 요청 재호출의 JSON 일치와 캐시 적중을 확인했습니다. 향수 예제는 87.4298점으로 `no_safe_match`, woody 로션 예제는 95.2점으로 통과했습니다. HTTP 200은 요청 처리가 정상이라는 뜻이지 향 목표 달성을 뜻하지 않습니다. 외부 언어 모델 호출은 끈 상태였습니다.

선택한 수치·통합·기존 경로 회귀 테스트 **149개 통과**. 새 변경 파일의 정적 오류 검사와 `git diff --check` 통과. 전체 저장소 테스트 또는 원격 CI를 실행했다고 주장하지 않습니다.

카탈로그 29,259행과 모든 원료 필드·위험·상한은 그대로이고, 실제 로컬 프로필에 새 백본을 연결했습니다. 패키지는 `dist/main-backbone-v66/candidate-01/wheel/perfumery_ai_core-1.4.0-py3-none-any.whl`로 빌드했습니다. 이 wheel만으로 외부 재배포 불가 데이터·로컬 체크포인트가 자동 포함되지는 않습니다.

## 7. 파일과 재현

- 학습 수식: `fragrance_ai/research/structured_kernel.py`
- 분자 모델: `fragrance_ai/research/atlas_profiles.py`
- CPU 실행: `fragrance_ai/research/atlas_inference.py`
- 모델 선택: `fragrance_ai/recommender/local_runtime.py`
- 학습: `scripts/train_main_backbone_v66.py`
- 독립 수치·계약 시험: `tests/test_main_backbone_v66.py`
- 실제 모델 연결 시험: `scripts/verify_main_backbone_v66.py`
- 최종 학습 자료: `.benchmarks/main_backbone_v66/train-02/{protocol,model,report,predictions}.json`
- 연결·속도 시험: `.benchmarks/main_backbone_v66/runtime-01/report.json`
- 조향 8건: `.benchmarks/main_backbone_v66/request-01/{manifest,summary}.json`, `results.jsonl`
- 실제 API: `.benchmarks/main_backbone_v66/api-01/api-report.json`
- 선택 회귀: `.benchmarks/main_backbone_v66/integration-02.xml`, `legacy-01.xml`

모델 SHA256: `1b5af57f7902b4efeee2ab991156623adb8f966c9340d7f456ef3c3d790e1cc9`

관련 이론: 다중 출력 커널의 수학적 배경은 [Kernels for Vector-Valued Functions: a Review](https://arxiv.org/abs/1106.6251)를 참고할 수 있습니다. 이 저장소의 학습기·정규화·수치 검증·실행 구현은 직접 작성한 코드이며, 해당 문헌의 외부 구현을 가져온 것이 아닙니다.

이번 결과는 완전성이나 학위 수준을 선언하는 대신, 수식의 해, 보존 조건, 학습 분리, 실제 오차와 실행 경로를 확인한 개선입니다. 새 수식을 추가했다는 이유로 90%·95% 실제 후각 유사도를 달성했다고 표현하지 않습니다.
