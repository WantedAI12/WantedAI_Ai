# V73 자기회귀 신경망과 배합 피드백 개선

2026-09-14. 기존 모델을 보존한 로컬 후보다. 운영 Modal 배포와 기존 API 주소·인증은 변경하지 않았다.

## 구현한 두 가지

첫째, 신경망을 확장하고 실제로 재학습했다. 기존 단일 조향 모델의 분자 표현을 받아, 이전 단계에서 자신이 출력한 배합과 남은 향 프로필 오차를 다음 단계에 다시 입력한다. 48차원 GRU 상태를 유지하며 8단계 배합 수정량을 계산한다. 고정된 문장이나 조향식을 반환하는 기능이 아니다.

둘째, 실제 배합 계산에도 결과를 되먹이는 반복 개선을 연결했다. 가장 최근에 검증된 배합과 오차 이력을 저장하고, 신경망 제안·이력 기반 수정·부분 원료 교체를 시도한다. 제안은 기존 가격·농도·원료별 상한·필수 원료·시간별 향·기타 통과 조건으로 다시 검사한다. 신경망 출력 자체를 승인이나 품질 점수로 쓰지 않는다.

요청을 처리할 때 신경망 가중치를 다시 학습하지는 않는다. 오프라인으로 학습한 체크포인트를 사용하며, 그 요청에서 나온 예측을 새 관능 정답으로 데이터베이스에 기록하지 않는다.

## 신경망과 실행 구조

- 기존 표현층과 7개 출력층: 1,868,933개 파라미터를 그대로 보존했다.
- 새 자기회귀층: 35,266개 파라미터를 학습했다.
- 합계: **1,904,199개 파라미터**, 하나의 가중치 파일.
- 분자 표현 256차원을 64차원으로 전달하고, 배합·이전 수정량·프로필 오차·방출 조건 등 16개 수치 입력과 함께 GRU에 넣는다.
- 다음 배합은 음수가 없고 총량이 100%가 되도록 제안한다. 이후 제품별 원래 제약 조건에 맞게 투영하고 재평가한다.
- 전체 후보 원료를 신경망에서 평가한 후 그 반복 단계의 배합 후보를 구성한다. 별도의 영구 원료 목록 제한을 추가하지 않았다.
- 저함량 원료에 신경망 출력이 집중되어도 기존 배합의 원료를 투영 단계에 남겨, 나머지 질량을 채울 수 있게 했다. 작은 변경 제안과 큰 변경 제안을 구분해 검사한다.
- 추가 피드백은 최대 3라운드, 단계 사이에서 확인하는 8초 작업 예산을 사용한다. 실행 중인 수치 계산을 강제 중단하는 한도나 전체 API 응답시간 보증은 아니다.
- 신경망 추론은 NumPy CPU 연산이다. 학습은 로컬 RTX 4060을 사용했다.

이는 언어 모델을 새로 학습한 것이 아니라, 배합을 순서대로 수정하는 학습형 최적화기다. 자연어 해석과 기존 조향 기능은 유지한다.

## 실제 학습

기존 준비 데이터의 분자 행 26,699개를 모두 사용해 제품별 배합 복원 문제를 만들었다. 각 행이 향수·로션 문제의 기준 원료로 한 번씩 들어간다. 기존 분자/구조 그룹 분할을 유지하며 서로 다른 분할의 원료를 같은 문제에 섞지 않았다.

| 구분 | 배합 복원 문제 수 |
| --- | ---: |
| 훈련 | 45,860 |
| 검증·체크포인트 선택 | 3,900 |
| 별도 평가 | 3,638 |

40 epoch를 실제 실행했다. 각 epoch에서 모든 훈련 문제를 사용하고, 검증 결과로 체크포인트를 선택한 다음 별도 평가를 실행했다. 이전 배합의 정답을 매 단계 강제로 입력하는 teacher forcing은 사용하지 않았다.

훈련에 쓰인 방출·잔류 조건과 목표 배합은 통제된 모의 조건이다. 원료 프로필은 기존 출처에 연결된 모델 자료를 사용하지만, 새 향수나 로션을 제조해 얻은 관능 정답은 아니다. 기존 다른 출력층은 이번에 다시 학습하지 않고, 값이 바뀌지 않도록 동결했다.

## 배합 복원 평가 결과

같은 문제에서 같은 8단계 계산을 하는 고정 스텝 방식과 비교했다. 비교 방식의 학습률은 0.5, 이전 수정량 계수는 0.2다. 오차는 전체 향 프로필의 총변동 거리(TV)를 시간·조건별로 계산한 최댓값이며, 아래는 그 평균이다.

| 모의 조건 | 고정 스텝 오차 | 학습한 자기회귀 오차 | 상대 감소 |
| --- | ---: | ---: | ---: |
| 향수 조건 | 0.049467 | 0.017924 | 약 63.8% |
| 로션 조건 | 0.046982 | 0.015725 | 약 66.5% |

CPU 이식 검사에서 PyTorch와 NumPy 배합 출력의 최대 차이는 `7.4878335e-7`이었다. 기존 모델의 모든 원래 파라미터 배열은 정확히 동일하다.

**이 표는 서비스의 자연어 400건 평가나 실제 사람의 향 유사도 결과가 아니다.** 기존 전체 조향 탐색기 V72보다 서비스 통과율이 이만큼 올랐다는 뜻으로도 사용하지 않는다. 기존 모델 비교 실험은 별도로 계속 보존한다.

## 실제 API 연결

향수 경로는 기존 `/v1/formulas`, 로션 경로는 기존 `/v1/applications/body-lotion/design`을 사용한다. 요청 필드를 바꿀 필요는 없다.

향수의 피드백 이력은 `full_profile_assessment.search.full_pool_search.dose_refinement.autoregressive_refinement`에 있다. 로션은 `material_column_search.autoregressive_refinement` 및 `neural_autoregressive`에 기록한다. 학습한 체크포인트 해시, 실제 신경망 호출 여부, 반복 횟수, 검증된 점수 이력과 작업 종료 사유를 구분한다.

초기 연결 검사에서 시간별 목표가 이미 배열인데 사전형 입력 함수에 전달하던 오류를 발견했다. 해당 경로를 수정하고 실제 시간별 목표를 사용하는 회귀 테스트를 추가했다. `api-02`는 실패 기록이며 성공 근거로 사용하지 않는다. `api-03`은 요청은 정상 응답했지만 로션 사례가 앞 단계에서 목표를 달성해 신경망 분기 확인이 끝나지 않은 기록이다. 최종 패키지 연결 검사는 `api-04` 결과를 사용한다.

최종 영향 범위 회귀 테스트는 **175개 통과, 실패·오류·skip 0건**이다. `api-04`의 실제 요청 6건은 모두 HTTP 200이며, 향수와 로션에서 학습한 자기회귀층 호출을 확인했다. 이 프로세스에서는 PyTorch를 import하지 않았다.

| 제품 | 요청 | 최종 계산 점수 | 95점 달성 | 자기회귀 신경망 호출 |
| --- | --- | ---: | --- | --- |
| 향수 | floral scent | 95.0308 | 예 | 앞 단계에서 달성하여 생략 |
| 향수 | clean fresh citrus woody | 79.0622 | 아니오 | 예 |
| 향수 | citrus scent | 71.2207 | 아니오 | 예 |
| 로션 | green scent | 93.8763 | 아니오 | 예 |
| 로션 | fresh scent | 95.0001 | 예 | 앞 단계에서 달성하여 생략 |
| 로션 | citrus scent | 88.4545 | 아니오 | 예 |

따라서 정상 응답 6/6을 품질 통과 6/6으로 표시하지 않는다. 이 기능 검사에서 품질 통과는 2/6이며, 대표 성능 표본이나 V73 전체 400건 결과도 아니다. 신경망 제안을 호출하고 제약에 맞게 처리하는 연결은 확인했지만, 이 결과만으로 기존 서비스보다 전반적으로 우수해졌다고 선언하지 않는다.

## 재현 파일

- 학습 실행·분할·코드 해시: `.benchmarks/v73_autoregressive/train-01/protocol.json`
- 학습 과정: `.benchmarks/v73_autoregressive/train-01/learning_curve.json`
- 학습 완료·별도 평가: `.benchmarks/v73_autoregressive/train-01/completion.json`
- 체크포인트: `.benchmarks/v73_autoregressive/train-01/model.json`
- 가중치: `.benchmarks/v73_autoregressive/train-01/weights.npz`
- 모델 SHA256: `bc45f11dd2e1ad5b80ecfe7126c8983d6c4e7e70b4ea53d93e86c0163f0a9dfb`
- 가중치 SHA256: `5923fa48634b733149de41a6ffad556d668e3783aba25b0c3ccc52af0a3a6cfa`
- 원본 목표 벡터를 유지한 새 모델 연결: `.benchmarks/v73_autoregressive/reference-01/binding.json`
- 패키지·카탈로그·모델 연결: `.benchmarks/v73_autoregressive/candidate-04/preparation.json`
- 최종 wheel SHA256: `95fc3b7449017d309a6ac9e70c42bdd66755bc57f247ea64b13382cdea71de62`
- 명시적 로컬 프로필: `perfumery.v73-candidate-04.local.json`
- 로컬 API 팩토리: `deploy/autoregressive_runtime_v73.py`
- 실제 패키지 API 검사: `.benchmarks/v73_autoregressive/api-04`
- 회귀 테스트: `.benchmarks/v73_autoregressive/final-regression-02.xml`

기본 모델 자동 승격, Modal 재배포, GitHub push는 하지 않았다. 서비스 400/400 달성 여부는 해당 전체 실행이 완료된 근거 없이 선언하지 않는다.
