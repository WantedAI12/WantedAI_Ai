# V68 분자 향 백본과 제품 방출 적분

2026-09-12. 로컬 구현·학습·통합용 변경입니다. GitHub push, PR 변경, Modal 배포는 수행하지 않았습니다. 외부 조향 코드나 새 외부 모델 가중치를 붙이지 않고 기존 분자 입력과 관측 자료에 맞춰 구현했습니다.

## 실제 변경 범위

- 146개 관측 출력을 유지하면서 구조·물성·향 기술·조건의 조합을 훈련 자료에서 학습합니다.
- 물성 단위를 표준화하고 중복되는 물성의 상관을 줄입니다. 향 기술의 결측을 서로 같은 향으로 계산하지 않습니다.
- 학습 분자의 물성 범위를 벗어나는 입력과 주석 유무를 반환합니다. 이것은 신뢰 확률이나 후각 정확도가 아닙니다.
- 서로 다른 출력 head도 공통 분자 특징 계산을 한 번만 수행합니다. CPU 추론과 기존 JSON 입출력을 유지합니다.
- 향수·로션·바디워시의 조건부 통합 예측에 질량과 공기 노출의 동시 적분을 연결합니다. 짧은 초기 방출 피크가 그래프의 표시점 사이에서 사라지지 않습니다.
- 로션 자동 조향과 세 제품 통합 향 프로필의 백본을 V68로 선택할 수 있습니다. stock-mixture 학습 부모와 기존 향수 생성의 19축 엄격 판정은 바꾸지 않습니다.

## 1. 분자 표현과 양의 준정부호 커널

구조 입력은 키랄성을 포함하는 1,024비트 Morgan fingerprint입니다. 기존 16개 분자 물성, 출처 있는 19축 native 프로필, 향 기술과 관측 여부, high/low 범주를 유지합니다. high/low를 ppm이나 방출 속도로 바꾸지 않습니다.

훈련 fold의 물성 평균과 표준편차를 `mu`, `s`라 하고, 표준화 행렬을 `Z`라 합니다.

\[
R=(1-\gamma)Z^TZ/n+\gamma I,\qquad
z(x)=((x-\mu)/s)R^{-1/2},\quad \gamma=0.1.
\]

분산이 없는 열의 스케일은 1로 둡니다. 이 열의 미관측 범위에 물리적 단위 불변성이나 일반화가 입증됐다는 뜻은 아닙니다. `gamma`는 양의 정부호를 유지하는 통계적 규제이지 실측 물성이나 열역학 계수가 아닙니다. 조회 입력을 사용해 평균·표준편차·상관을 다시 적합하지 않습니다.

기본 커널은 구조 Tanimoto, 세 길이 척도의 물성 RBF, 관측 native 프로필의 Hellinger 내적, 관측 향 기술 Tanimoto입니다.

\[
k_{phys,\eta}(x,z)=\exp[-\eta\|z(x)-z(z)\|^2/16],
\qquad \eta\in\{0.25,1,4\}.
\]

향 주석이 없으면 해당 커널의 특징 벡터는 0으로 두되, 목표 관측값을 0으로 만들지는 않습니다. 따라서 주석이 없는 두 원료가 주석이 없다는 이유만으로 native 유사도 1을 얻지 않습니다. 알려진 비음수 프로필은 합으로 정규화하고 제곱근 특징의 내적을 사용합니다.

여섯 커널 각각에 공통 조건과 같은 ordinal 조건끼리만 연결되는 성분을 만들어 12개 기본 커널을 구성합니다. 범주 일치 커널과의 곱, 비음수 합, 양의 자기 유사도 정규화는 PSD 성질을 보존합니다. 거리 계산은 두 큰 내적의 차 대신 직접 제곱거리 계산을 사용합니다.

## 2. 학습 목적함수와 해 검증

각 훈련 커널을 중심화하고 Frobenius norm으로 정규화한 행렬을 `Kbar_j`, 중심화된 전체 출력의 Gram 행렬을 정규화한 것을 `Tbar`라 합니다.

\[
\min_{w\ge0,\;\mathbf1^Tw=1}
\frac12\left\|\sum_jw_j\overline K_j-\overline T\right\|_F^2
+\frac\lambda2\|w-p\|_2^2.
\]

양의 `lambda`로 엄격 볼록인 문제입니다. 직접 구현한 simplex 투영과 경사법으로 풀고, 투영 KKT 잔차 `2e-10` 미만일 때만 적합을 허용합니다. 그 뒤 명시적인 1% 입력공간 prior 수축을 적용해 주석이 없는 분자에서도 물성 기반 커널 정의가 남도록 합니다. KKT 잔차와 목적함수는 이 수축 **전** 해에 대한 값이며, 수축 후 가중치가 같은 QP의 최적해라고 표시하지 않습니다. 출력 점수·관측값·목표 향을 사후 수정하는 연산이 아닙니다.

학습 커널의 최종 가중 합은 각 입력의 자기 유사도로 다시 정규화합니다. 출력 단계는 V66의 절편을 포함한 다중 출력 RKHS 회귀를 사용합니다.

\[
K_cC+\alpha CB^{-1}=Y_c,\qquad
\mathbf1^TC=0,\qquad b=\bar Y-\bar KC.
\]

거대한 Kronecker 행렬이나 역행렬 없이 두 고유기저에서 계산합니다. 양의 준정부호와 정상방정식 잔차를 확인하고, JSON 직렬화·복원 및 CPU 배치 구현을 독립 해법과 대조합니다.

커널 결합의 이론적 배경은 [Cortes, Mohri, Rostamizadeh의 centered alignment 연구](https://www.jmlr.org/papers/v13/cortes12a.html)입니다. 여기서는 우리 입력 블록과 다중 출력에 맞춘 규제 Gram 근사 문제와 수축을 구현했으며, 논문의 모든 알고리즘이나 일반화 정리를 그대로 재현했다고 주장하지 않습니다.

## 3. 화학·물리·환경 방출 모델

영역별 양을 막의 잔존량 `F`, 공기 중 양 `A`, 피부 이동·환기·분해·세척 누적량으로 구분합니다. 모든 속도는 `min^-1`, 초기 면적당 양은 `mg/cm2`입니다.

\[
D(t)=1-w+w e^{-kt},\quad
e(t)=e_0/D(t),\quad u(t)=u_0/D(t),\quad
h(t)=h_0[1-\ell/D(t)].
\]

`w`는 증발하는 용량 비중, `ell`은 비반응성 용량 비중이며 합이 1을 넘을 수 없습니다. 물성·분배·방출 계수는 실제 제형과 환경에 해당하는 입력이어야 합니다.

\[
\dot F=-(e+u+h)F+rA,\qquad
\dot A=eF-(r+v)A.
\]

흡수·환기·분해 누적량의 도함수는 각각 `uF`, `vA`, `hF`입니다. 추가 상태 `J_F`, `J_A`는 `J_F'=F`, `J_A'=A`를 따릅니다. 세척은 막의 잔존량 일부를 세척 누적량으로 보내는 비음수 질량보존 점프이며, 과거 공기 노출 적분을 삭제하지 않습니다.

일정한 계수에서는 두 저장소의 스펙트럼 해와 occupation moment를 해석적으로 계산합니다. 용량 변화가 있으면 구간 평균 생성자의 지수와 두 반구간 합성을 비교해 적응적으로 세분합니다. 질량과 시간 적분을 같은 양의 전이 연산자로 전파하며, 표시점은 받아들여진 시간 격자를 바꾸지 않습니다. 세분화 오차는 **국소 수치 추정값**이지 실제 제품 오차의 엄밀한 상한이 아닙니다. 예산을 소진하면 근거 없이 거친 결과를 통과시키지 않고 오류를 반환합니다.

이 finished-product 경로에는 노출 moment가 없는 기존 신경망 전이 보정을 적용하지 않습니다. V60 체크포인트와 기존 로션 adapter는 별도 경로에 보존되며 JSON에서 실제 적용 여부를 분리합니다.

온도·습도는 계수의 제형·환경 맥락에 묶입니다. 측정되지 않은 온도 의존성·습도 의존성·활동도 계수를 이 모델이 새로 학습한 것은 아닙니다. octanol logP를 특정 로션의 유·수상 분배계수나 Henry 상수로 대체하지 않습니다. Henry 상수는 정의·단위·용액 조건에 따라 구별해야 하며 무한 희석 평형의 관계입니다. [IUPAC 정의](https://goldbook.iupac.org/terms/view/09020)

## 4. 실제 재학습 결과

기존 자료 160개 조건 행 중 구조가 확인되는 144개 행을 예측했습니다. 구조 미확인 16개는 전체 분모에서 지우지 않았습니다. 두 head의 146개 관측 출력을 모두 유지하고, V66과 동일한 외부 분자/골격 5-fold 및 내부 그룹 3-fold를 사용했습니다. 비고리 분자는 하나의 빈 골격 그룹에 남습니다. 내부에서 60개 설정을 선택하며, 외부 결과나 레시피 결과로 설정을 선택하지 않습니다. 반복 사용해 온 기존 자료의 개발 평가이며 새 블라인드 검증은 아닙니다.

| 분리 / 출력 | V66 MAE | V68 MAE | MAE 감소 | V66 코사인 거리 | V68 코사인 거리 |
|---|---:|---:|---:|---:|---:|
| 분자 / applicability | 1.93831 | 1.92831 | 0.52% | 0.187988 | 0.185061 |
| 분자 / use | 2.69513 | 2.67420 | 0.78% | 0.166462 | 0.164423 |
| 골격 / applicability | 2.25505 | 2.08589 | 7.50% | 0.223792 | 0.216970 |
| 골격 / use | 3.20971 | 2.93230 | 8.64% | 0.208039 | 0.198658 |

골격 분리 MAE 감소의 군집 bootstrap 구간은 두 head 모두 0보다 큽니다. 분자 분리의 개선 구간과 일부 코사인 개선 구간은 0을 포함합니다. 모든 향·제품에서 우월함이 통계적으로 입증된 것은 아닙니다.

## 5. 실제 연결·수치 시험

- 분자 학습·기존 모델·제형·로션 탐색·API 계약 관련 테스트 227개 통과.
- 독립 SLSQP 최적해, 작은 Kronecker 선형계, 행 순서 불변성, 결측 마스크, JSON 복원, 배치 경계와 불변 가중치 검사.
- 독립 7상태 행렬 지수, 비선형 건조·분해 ODE, 세척 점프, 표시점 불변성 및 계산 예산 오류 검사.
- 초기 방출과 환기 속도가 각각 `10000/min`인 수치 예: 0분·480분 그래프만 적분하면 0이지만 새 연산자는 초기 단위 질량당 정확한 `0.0001 mass*min` 공기 노출을 보존.
- 실제 V68 체크포인트를 로션과 세 제품 조건부 예측에 연결. 수치 예제의 최대 질량수지 오차 `2.61e-18 mg/cm2` 미만. 예제의 공정 계수는 합성이며 실측 제품 계수가 아닙니다.
- 원료 29,259행, 활성 원료 3,830개와 모든 원료 필드가 그대로임을 digest로 확인. source/wheel 연결만 재생성했습니다. 기준 향의 관측값도 정확히 유지했습니다.
- 실제 로컬 API 두 요청과 캐시 확인: 향수 예제 `87.4298`, 기준 미달 상태 유지; woody 로션 `95.0`, 통과 유지(이전 `95.2`). 두 요청의 통과 수는 늘지 않았습니다.
- 1,152행/146출력×2 CPU 추론과 범위 진단의 중앙값: V66 `22.47ms`, V68 `40.30ms`. 새 표현과 진단으로 핵심 추론 비용은 증가했습니다. 외부 LLM 호출은 껐으며 토큰/API 호출을 추가하지 않았습니다.

전체 400개 레시피 재평가, 새로운 실제 후각 정확도 90%/95%, 전세계 원료에 대한 정확도, 운영 배포 완료를 이 결과로 주장하지 않습니다.

## 6. 연결 파일과 재현

- 수학·분자 표현: `fragrance_ai/research/scientific_kernel.py`
- 학습·직렬화: `fragrance_ai/research/atlas_profiles.py`
- CPU 공통 계산: `fragrance_ai/research/atlas_inference.py`
- 보존·노출 적분: `fragrance_ai/recommender/exposure_transport.py`
- 제품 연결: `fragrance_ai/recommender/unified_product.py`
- 학습: `scripts/train_main_backbone_v68.py`
- 실제 체크포인트/API 확인: `scripts/verify_scientific_backbone_v68.py`
- 학습 산출물: `.benchmarks/scientific_backbone_v68/train-02/{protocol,model,report,predictions}.json`
- 실제 모델/API 확인: `.benchmarks/scientific_backbone_v68/runtime-03/`
- 회귀: `.benchmarks/scientific_backbone_v68/regression-01.xml`
- 최종 기본 설정 연결 확인: `.benchmarks/scientific_backbone_v68/activation.json`

최종 `perfumery.local.json`은 V68 모델과 `dist/scientific-backbone-v68/final-01/catalog/catalog_manifest.json`을 선택합니다. 기본 설정의 실제 API capabilities HTTP 200과 V68 SHA를 확인했습니다. 학습 report의 `runtime_promoted: false`는 학습 종료 시점의 기록이며, 이후 로컬 연결은 별도 activation 기록에 보존합니다. 운영 배포는 하지 않았습니다.

모델 SHA256: `622b3b8c1ac21ba5e43d7dfff1bf85adea11b891ee99404e9fb5fb032eb48218`.

새 모델·카탈로그·개인 실행 설정은 소스 코드와 별개인 로컬 산출물입니다. 배포·재배포 승인을 의미하지 않습니다. 기존 `/v1` 요청은 유지되며 통합 제품 응답에 `molecular_model_applicability`, `integrated_exposure`, 원료별 `cumulative_air_exposure_mg_min_m3`가 추가됩니다.
