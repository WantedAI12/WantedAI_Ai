global:
  scrape_interval: 5s

scrape_configs:
  - job_name: perfumery-api
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: auditor-token
    http_headers:
      X-Tenant-ID:
        values: [demo]
    static_configs:
      - targets: [api:8000]
