# V64 로컬 탐색 복구 개선

2026-09-12. 로컬 코드, 패키지, 카탈로그 연결을 개선했고 소스와 재현 절차를 `dev` 변경에 포함한다. 기존 V63 운영 배포와 구분하며 운영 서버는 변경하지 않았다. 이번 작업은 학습 가중치 재학습이 아니라, 학습 모델을 사용하는 배합 탐색의 수학적 오류와 실행 경로를 수정한 것이다.

## 수정한 내용

### 1. 최종 평가와 로션 탐색 조건의 불일치

기존 `lotion_reference_search.solve`은 향 프로필의 겹침과 제외 조건으로 배합을 찾았지만, 최종 평가에 포함된 코사인 조건은 탐색 성공 판정에 반영하지 않았다. 겹침 점수가 같은 후보 중 저렴하지만 향의 방향이 다른 후보를 계속 선택할 수 있었다.

146개 출력 축을 사용하는 회귀시험에서 이 문제를 재현했다. 140개 축에 균등하게 놓인 목표에 대해, 요청하지 않은 향 4%가 한 축에 몰린 후보는 89.6922점이고 여섯 축에 나뉜 후보는 96점이다. 두 후보의 겹침 점수는 모두 96점이다. 수정 전에는 첫 후보를 반환했지만, 수정 후에는 기존 95점 기준을 통과하는 배합을 찾는다. 이것은 합성 프로필을 이용한 소프트웨어 반례이며 실제 향 정확도 시험이 아니다.

실제 방출 계수로 가중한 혼합 벡터를 `p = A x`, 목표의 단위 벡터를 `u`, 통과 수준을 `q`라 하면 코사인 조건은 다음과 같다.

```
q ||p||₂ − uᵀp ≤ 0
```

이 볼록함수를 현재 실패 지점 `p₀`에서 선형화하면 다음 필요조건을 얻는다.

```
(q p₀ / ||p₀||₂ − u)ᵀ A x ≤ 0
```

이 조건을 추가한 뒤 다시 배합을 찾는다. 최종 평가는 원래 코드로 다시 수행한다. 반복 한도에 도달하면 탐색 미완료로 남기며, 해가 없다는 증명으로 바꾸지 않는다. 이 식은 현재 사용 중인 성분 가중 혼합 모델에서의 수학적 일관성을 개선하며, 새로운 혼합 관능 측정이나 마스킹 학습을 대신하지 않는다.

### 2. 일반적인 배경 향과 구별되지 않는 후보

목표와 비슷한 평균적 프로필이 높은 점수만으로 탐색을 끝내지 않도록, 최종 배경 구별 조건을 승인 수준의 탐색에도 연결했다. 통과한 후보가 배경과 구별되지 않는 고득점 후보로 교체되지 않게 했다.

기존 엄격한 구별 조건은 `cos(target,p) > cos(background,p) + 1e-8`이다. 반복 계산에는 `2e-8`의 수치 여유를 사용하고 최종 결과는 원래 `1e-8` 조건으로 다시 확인한다. 이 여유를 적용한 문제가 풀리지 않으면 원래 문제 전체가 불가능하다고 선언하지 않는다. 목표와 배경이 동일한 반례도 승인되지 않음을 시험했다.

승인 기준보다 낮은 단계에서는 최접근 후보 탐색에 집중한다. 승인될 수 없는 낮은 점수 후보에 승인 전용 조건을 강제하며 계산을 낭비하던 초기 구현은 보완했다.

### 3. 수천 개 원료 중 필요한 원료를 놓치는 문제

기존 제한 후보 탐색은 최대 여섯 번 확장하여 2,048개 부근에서 끝날 수 있었다. 3,830개 또는 12,000개 중 마지막 원료가 반드시 필요한 문제에서 전체 배합은 가능하지만 후보를 반환하지 못했다.

제한 후보가 불가능할 때 임시 위반 변수를 가진 별도 문제를 풀고, 그 위반을 줄일 수 있는 빠진 원료를 전체 후보에서 찾는 Phase I 계산을 추가했다. 쌍대값을 이용하므로 기존 향 검색 순위가 낮아도 필요한 원료가 들어온다. 같은 반례에서 256개 미만의 작업 후보로 가능 배합을 찾는다. 실제 후보 범위를 256개로 제한하는 것은 아니다.

원래 원료별 상한·하한은 유지한다. 임시 위반 변수는 최종 조향식에 들어가지 않는다. 불가능 문제와 기존 보조 변수가 있는 문제도 별도로 시험했다. 사용한 쌍대값의 부호와 상태 코드는 [SciPy 1.15.2 공식 문서](https://docs.scipy.org/doc/scipy-1.15.2/reference/optimize.linprog-highs.html)를 기준으로 확인했다.

### 4. 향 표현 배치와 혼합 시료 API

- 향 표현 모델은 배치마다 같은 가중치·분할·참조 파일을 다시 읽지 않고, 전체 작업 시작과 완료 시 검사한다. 검사 완료 전에는 새 예측을 캐시에 넣지 않는다.
- 단독 분자 예측도 완료 시 모델 변경을 다시 확인한다.
- 빈 원료 목록도 `(0, 출력 축 수)`의 일관된 배열 형태로 반환한다.
- 혼합 시료 API는 계산 전·계산 후·캐시 반환 시 모델과 카탈로그 변경을 검사한다. 잘못된 결과가 새 캐시에 들어가지 않도록 계산 완료 검사도 캐시 저장 전에 수행한다.
- 기존 주소·입력 형식·응답 본문 필드는 유지한다. 탐색 진단 항목만 추가한다.

## 비교와 재현

모델과 원료를 고정하고 탐색 코드만 비교했다. 기존 코드 대조군은 이전 로컬 wheel의 SHA256을 검증한 뒤 해당 탐색 모듈만 명시적으로 불러온다. 구버전 학습 모델로 몰래 되돌아가는 경로가 아니며 운영 코드에는 대조군 로더가 없다.

결과 위치:

- `.benchmarks/improvement_v64/lotion-before/`: 기존 자연어 요청 4건 기준선.
- `.benchmarks/improvement_v64/ablation-before/`: 배경 구별 관련 추가 요청 4건의 이전 탐색 대조군.
- `.benchmarks/improvement_v64/accepted/`: 최종 후보의 동일 8건 비교.
- `.benchmarks/improvement_v64/integration-02.xml`: 관련 API·모델·캐시·스트리밍 회귀시험.
- `.benchmarks/improvement_v64/runtime-numerics.xml`: 로컬 모델 선택·수치 계산 회귀시험.
- `.benchmarks/improvement_v64/solver-accepted.xml`: 최종 탐색 코드 회귀시험.
- `.benchmarks/improvement_v64/api-final/`: 실제 고정 모델 예측·로컬 API 응답.

### 실제 자연어 요청 비교

95점 기준을 고정한 진단용 8건이다. 이전 실패를 참고해 선정한 요청이므로 새 블라인드 평가 또는 전체 요청의 대표 통계로 해석하지 않는다.

| 요청 | 이전 점수 | 최종 점수 | 최종 95점 통과 | 이전 / 최종 시간(초) |
|---|---:|---:|:---:|---:|
| fresh scent | 95.0000 | 95.2000 | 예 | 14.810 / 13.795 |
| clean scent | 88.8863 | 88.8863 | 아니오 | 17.473 / 29.666 |
| green scent | 93.4095 | 93.4095 | 아니오 | 28.676 / 55.936 |
| woody scent | 95.0000 | 95.2000 | 예 | 15.205 / 9.875 |
| musky scent | 92.0212 | 92.0212 | 아니오 | 19.482 / 34.273 |
| clean, powdery scent | 93.2870 | 93.2870 | 아니오 | 23.809 / 27.865 |
| clean, leathery scent | 92.1835 | 92.1835 | 아니오 | 22.436 / 30.259 |
| clean, earthy scent | 93.4329 | 93.4329 | 아니오 | 16.991 / 16.434 |

**통과 수는 2/8로 동일하다.** 최종 점수 후퇴는 없고, fresh와 woody는 0.2점 개선되며 탐색 미완료 표시도 해소됐다. 새 탐색이 기존에 놓치던 가능 배합을 찾는 회귀시험은 통과하지만 이 8건에서 신규 통과는 없었다. clean/powdery 등 일부 요청은 현재 고정 원료 프로필의 계산 상한 자체가 95점 미만이다.

시간은 CPU 2개 작업을 병렬 실행한 로컬 단회 측정이다. 특히 기존 첫 4건 비교 중 일부는 별도 시험 작업과 겹쳤으므로 서버 지연 통계나 엄밀한 속도 배율로 해석하지 않는다. 여러 어려운 요청에서 계산 시간이 증가한 것은 그대로 기록한다. 이번 버전을 모든 요청에서 더 빠른 버전이라고 주장하지 않는다.

### 모델과 회귀시험

- 관련 API·향 표현·로션·캐시·모델 선택·수치 회귀시험을 최신 결과로 중복 제거하면 **212개 통과**, 실패·건너뜀 0개다. 새 오류를 재현한 회귀시험은 수정 전에 실제 실패하는 것을 확인했다.
- 새 배치 코드로 기존 고정 평가 분자 714개를 다시 예측한 결과, 저장된 V62 출력과 최대 절대 차이는 **0.0**이다. 새로운 정확도 성적이 아니라 예측 품질 보존 검사다.
- 실제 원료 3,830개 배치: 파일 전체 변경 검사는 **2회**, 반복 결과 동일. 해당 실행의 첫 배치 계산은 **3.425초**다.
- 새 가중치·새 원료 관능 기록은 추가하지 않았다. 원료 내용과 학습 체크포인트를 그대로 유지했다.
- 변경 파일 Ruff 검사, Python 구문 컴파일, `git diff --check`를 통과했다. 새 오류 회귀시험을 기존 CI 대상에도 추가했지만 원격 CI는 실행하지 않았다.

### 실제 로컬 API

현재 고정 카탈로그와 모델을 사용하는 ASGI 앱으로 확인했다. 외부 언어 모델 호출은 켜지 않았다.

| 경로와 요청 | HTTP | 결과 | 첫 요청 | 반복 |
|---|---:|---|---:|---|
| `/v1/formulas`, 피오니와 청사과 향 | 200 | 87.4298점, 미승인 후보 | 35.868초 | 캐시 적중, 응답 동일 |
| `/v1/applications/body-lotion/design`, woody scent | 200 | 95.2점, 연구용 프로필 기준 통과 | 11.205초 | 캐시 적중, 응답 동일 |

HTTP 200과 결과 반환은 요청 처리가 정상이라는 뜻이다. 향수 요청의 95점 통과를 뜻하지 않는다. 향수 기준선 48.636초는 프로파일러가 켜진 측정이므로 위 35.868초와 비교해 속도 개선율을 계산하지 않았다. 이 검사는 원격 Modal 배포 검사가 아니다.

실행 명령:

```text
python scripts/benchmark_search_recovery_v64.py --output <새 디렉터리> --workers 2 --case-id en-1 --case-id en-2 --case-id en-3 --case-id en-11 --case-id en-13 --case-id en-2-15 --case-id en-2-17 --case-id en-2-18
python scripts/verify_search_recovery_v64.py --output <새 디렉터리>
```

`perfumery.local.json`의 전체 고정 모델·카탈로그가 필요하다. 연구용 학습 자료는 wheel 하나에 전부 포함된다고 가정하지 않는다. CPU 비교에서는 `OMP_NUM_THREADS`, `OPENBLAS_NUM_THREADS`, `MKL_NUM_THREADS`를 각각 1로 설정했다.

### 채택하지 않은 변경

`candidate-01`은 미달 단계까지 배경 조건을 강제해 일부 요청 시간이 늘었다. `candidate-02`는 이전 최적 점수에서 탐색 구간을 시작하도록 바꿨지만, 제한된 계산 시간 아래에서 후보 발견 순서가 바뀌어 clean/powdery 요청이 93.2870→92.5139점으로 후퇴했다. 해당 구간 변경을 제거했다. 모든 중간 산출물은 덮어쓰지 않고 보존한다.

## 연결과 범위

최종 로컬 선택은 `dist/search-recovery-v64/candidate-03/`이다. 패키지를 새로 만들고, 실제 패키지 소스와 맞는 카탈로그 명세를 생성한 뒤 `perfumery.local.json`에 연결했다. 카탈로그 원료 29,259행, 기존 연구용 활성 3,830개와 원료의 모든 필드는 보존했다.

원료 내용 SHA256:

```
8c962601b6567d56bd1043733b2b2eaccf9deb5d35e4aba70101271fe65bf139
```

최종 카탈로그 명세 SHA256:

```
0c33ccb1204a38b3cbe17aa8e6c08522bc21df91e75bd6db23ed39a8fd99842d
```

최종 wheel SHA256:

```
31ec85895964aa9dba26eb8760eee5d4aee41398c3d08baba3b4d215f239ec4c
```

전체 400건 재평가 또는 새 인간 데이터 블라인드 학습·평가는 이번 작업에 포함하지 않았다. 기존 공개 데이터 평가 점수를 실제 완성품 향 유사도로 바꾸지 않는다. 일부 요청은 원료 프로필의 표현 범위 또는 데이터가 부족하여 여전히 미달이다.

저장소 전체 테스트가 통과했다고 주장하지 않는다. 최초의 기존 `test_full_pool_search` 실행에서는 예전 90점 기본값 기대와 현재 95점 기본값이 달라 실패한 항목이 확인됐다. 이번에 그 기대값이나 판정 기준을 바꾸어 테스트를 통과시키지 않았다. 관련 변경 범위의 실행 결과와 전체 저장소 상태는 구분한다.
