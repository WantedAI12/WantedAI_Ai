# V69 단일 조향·제조 신경망

## 구현 범위

향수와 바디로션의 메인 향 예측, 세부 향 표현, 방출 전이 근사, 제조 단계 선택, 배합 수정 방향을 하나의 학습 가중치 파일로 통합했다. 같은 신경망 안에 각 결과를 읽는 출력부가 있으며, 예전 세 모델을 실행한 뒤 결과만 합치는 방식이 아니다. 향수·로션은 같은 가중치를 사용하지만 제품 조건, 공정, 물리 계산과 단위를 구분한다.

일반 레시피 API에서 기존 V45 향수 검색 모델, V68 정량 향 모델, V61 세부 표현 모델, V60 전이 모델의 가중치를 읽지 않는다. 과거 stock-aliquot 실험 전용 기능은 별도 호환 경로로 보존한다. 그 요청이 들어올 때만 원래의 고정된 실험 모델을 읽으며, 향수·로션 제조용 모델로 취급하지 않는다. 선택형 자연어 입력 도우미와 결정론적 파서도 조향 신경망과는 역할이 다르다.

이 문서는 V69 로컬 학습·검증 결과와 GitHub 소스 인계 범위를 설명한다. 운영 Modal 배포는 변경하지 않았으며, 로컬 학습 결과와 운영 서버의 버전은 구별해야 한다.

## 신경망 구조

- 분자 입력: 구조 fingerprint, 물성, 출처가 있는 향 기술, 기존 원료 프로필, 관측 조건을 합친 1,599차원.
- 분자 인코더: 1,599 → 384 → 256.
- 원료 집합: 활성 질량으로 가중한 평균·분산·조건부 attention. 원료의 나열 순서에는 불변이며 동일 원료를 나눠 적어도 결과를 보존한다.
- 제품·공정 입력: 64차원 → 128차원. 누락된 값과 관측값을 구별한다.
- 제조 이력: 단계 embedding 48차원과 수치·관측 마스크 12차원을 96차원 순환 상태로 누적한다. 제조 순서는 결과에 영향을 준다.
- 공통 결합부: 992 → 384 → 256, 잔차 블록 2개.
- 출력: 세부 향 450개, 정량 향 146개씩 두 측정 항목(applicability/use), 전이 10개, 다음 행동 21개, 공정 점검 8개, 수정 방향 19개, 유화 입자 분포 101개.

향수·로션 전용의 별도 신경망이나 별도 체크포인트는 없다. NumPy CPU 추론과 PyTorch 학습 구현의 출력을 독립 비교한다. 추론 때 PyTorch·GPU·외부 모델 API는 필요하지 않다. 분자 특징 계산에는 RDKit을 사용한다.

## 데이터와 실제 학습

학습 스크립트는 준비한 각 과제의 훈련 행을 매 epoch 사용한다. 분자와 혼합물은 골격 그룹으로, 공정은 수치 조건 그룹으로, 유화 실험은 조성 그룹으로 나누고 정규화는 훈련 분할만 이용한다. 최적 epoch는 검증 분할에서 선택한다.

| 자료 | 역할 | 의미와 한계 |
|---|---|---|
| 분자 26,699개 | 기존 정량·세부 향 예측을 공동 증류 | 새 인간 관능 정답이 아니라 기존 예측기의 지식 이전 |
| 전이 계산 45,540건 | 비음수 전이 근사 학습 | 합성 물리 계산 자료 |
| 제조 단계 243,200행, 수치 조건 9,600개 | 향수·냉간 로션·가열 로션 절차 학습 | 출처 있는 절차에서 생성한 학습 사례. 실제 제조 243,200회가 아님 |
| 혼합물 8,000건 | 원료 집합 처리와 수정 방향 학습 | 가산 향 프로필·목표 차이 자료. 실제 혼합 관능·조향사 의사결정 자료가 아님 |
| 유화 실험 196건 | 정상상태 입자 크기 분포 학습 | 공개 실측 자료. 실리콘 오일 교반 실험이며 완제품 로션 검증이 아님 |

이전 모델로 생성한 정답을 반복 비교했으므로 분자 테스트는 **증류 재현성 평가**다. 새롭고 독립적인 인간 블라인드 검증이라고 부르면 안 된다. 개발 과정에서 비교한 분할 역시 최종적인 외부 검증 집합으로 재사용하면 안 된다.

## 조향·제조 과정 연결

향수 과정에는 의도 정리, 원료 검토, 계량, 기본 어코드, 향의 변조·연결·배경 역할, 희석, 비교, 수정, 배치 기록, 품질 검토와 충전 기록을 연결한다. 고정된 탑·미들·베이스 비율을 정답으로 학습시키지 않는다.

로션 과정에는 수상·유상 준비, 선택된 참조에 따른 냉간 또는 가열 유화, 냉각, 향료 투입, 배치 계량, 품질 검토와 충전 기록을 연결한다. 가열 범위나 방부제 pH 조건은 해당 출처의 원료·배합에만 적용한다. 온도·pH가 범위에서 얼마나 떨어져 있는지 수치 특징으로 제공하며, 목표 행동 라벨 자체를 입력에 넣지는 않는다.

학습된 다음 단계와 문서 기반의 필수 점검을 함께 반환한다. 두 결과가 충돌하면 `model_source_conflict`로 표시한다. 신경망 출력이나 `done` 단계는 제조 승인·안전성 승인으로 쓰지 않는다. 완료 이력에 실제로 수행하지 않은 작업을 자동 기록하지도 않는다.

## 유화 물리 학습

Manchester의 Rodgers 자료는 고정된 교반 용기·임펠러에서 24시간 후 정상상태의 입자 크기를 측정했다. 원본 CSV·메타데이터·체크섬을 보존하고, 동일 조성의 여러 교반 속도가 훈련과 평가에 섞이지 않게 나눈다.

교반 속도 N은 rpm을 초당 회전수로 바꾸고, 표면장력 σ는 mN/m를 N/m로 변환한다. 임펠러 지름 D에 대해 다음 무차원 수를 사용한다.

`Re = ρc N D² / μc`

`We = ρc N² D³ / σ`

`Oh = μc / √(ρc σ D) = √We / Re`

이와 점도비, 밀도비, 분산상 체적분율의 logit를 입력한다. Oh는 독립적인 새 변수라고 주장하지 않는다. 훈련 자료로 적합한 로그정규 기준 분포에 같은 공통 신경망의 잔차를 더하고 softmax로 부피분율 합계를 1로 보존한다. 비교에는 훈련 평균과 물리 특징을 사용한 기준식을 모두 남긴다.

입력 범위 안에 있다고 다른 유화제·장비·충전 조건까지 검증된 것은 아니다. 유화 입자 크기를 향료 방출계수로 임의 변환하지 않는다. 숙성 중 반응, 결정화, 상전이, 장기 점도·pH·미생물 안정성, 피부 적합성과 최종 향을 이 자료로 학습했다고 주장하지 않는다.

## 물리·탐색의 역할

학습된 전이 출력은 질량 보존·비음수·불가능한 전이의 영점 제약을 통과해야 한다. 완제품의 시간별 노출 적분은 기존 적응형 물질수지 계산을 유지한다. 신경망 확률이 물리 보존식을 대신하지 않는다.

배합 수정 방향은 어코드 후보의 탐색 순서를 정한다. 후보를 승인하는 기준은 기존 목표 향·제외 향·가격·원료별 제약이다. 통과 기준을 낮추거나 실패 점수를 성공으로 치환하지 않았다.

## API

기존 향수·로션 생성 경로와 JSON 형식은 유지한다. `/v1/ai/capabilities`에 `shared_formulation_model`을 추가하여 가중치 해시, 구조, 학습 출처와 평가 범위를 확인할 수 있다.

`POST /v1/formulation-workflows/plan`은 기존 요청에 `completed_step_ids`를 선택적으로 받는다. 반환되는 `learned_process`에는 모델의 다음 단계, 출처 기반의 다음 단계·점검, 전체 참조 단계 목록과 모델 해시가 들어간다.

`POST /v1/formulation-workflows/emulsion-prediction`은 명시적인 연구 참조 도메인과 교반 속도, 두 상의 점도·밀도, 표면장력, 분산상 비율을 받는다. 입자 크기별 부피분율과 평균 입경, 적용 범위를 반환한다. 자연어 로션 요청에서 알 수 없는 실측 물성을 지어내 이 경로에 넣지 않는다.

## 재현 파일

GitHub에는 구현·학습·검증 스크립트, 테스트와 이 설명서를 포함한다. 개인 경로가 담긴 실행 설정, 원본 자료, 재배포 검토가 끝나지 않은 학습 체크포인트·원료 카탈로그는 로컬에 보존하며 커밋하지 않는다. 따라서 저장소를 복제하거나 wheel만 설치하는 것으로 V69 학습 가중치가 자동 연결되지는 않는다. V69 실행에는 검증된 가중치·manifest·참조 데이터·카탈로그 및 해당 SHA-256을 지정한 로컬 설정이 필요하다. 누락된 V69 파일을 예전 모델로 몰래 대체하지 않는다.

로컬 기본 설정 `perfumery.local.json`은 검증한 `train-04` 체크포인트를 선택한다. 동일 선택은 `perfumery.v69.local.json`에, 원료 데이터를 보존한 이전 모델 실행 설정은 `perfumery.v68.rollback.json`에 남겨 두었다. 운영 서버 설정은 바꾸지 않았다.

- 데이터 준비: `scripts/prepare_formulation_v69.py`, `scripts/expand_formulation_process_v69.py`, `scripts/prepare_emulsion_v69.py`
- 공동 학습: `scripts/train_formulation_v69.py`
- NumPy 추론: `fragrance_ai/recommender/formulation_core.py`
- 실제 체크포인트·API 확인: `scripts/verify_formulation_v69.py`
- 학습 자료 및 평가 기록: `.benchmarks/formulation_core_v69/`
- 원료 정보 보존과 패키지 결합: `dist/shared-formulation-v69/`

## 최종 로컬 확인 결과

신경망 파라미터는 **1,868,933개**이며 압축 가중치는 **6.97 MB**다. 모델 manifest SHA-256은 `3186e467446abc71f249c052c8248c17c97d0e337976abf3666628af47dd997f`다.

| 확인 항목 | 결과 | 해석 |
|---|---|---|
| 제조 다음 단계 | 평가용 35,992행에서 99.33% | 문서에서 생성한 절차 사례의 재현성 |
| 기존 정량 향 모델 증류 | 평가 분자 1,819개, 고·저 관측 조건 중앙 cosine 0.99873 | 새 인간 후각 정확도가 아님 |
| 세부 향 증류 | 확률 MAE 0.00334 | 기존 예측기 대비 오차 |
| 합성 전이 | 평가용 6,636행 RMSE 0.000803 | 실제 방출 실험 대비 오차가 아님 |
| 실측 유화 입경 | 평가용 36건의 log-기하평균 입경 RMSE 0.20404 | 물리 특징 기준식 0.57395 대비 64.45% 감소 |
| 별도 유화 검증 분할 | 24건의 같은 오차 0.49689 | 기준식 0.38445보다 나쁨. 모든 분할에서 우월하다고 주장할 수 없음 |
| NumPy/PyTorch 출력 비교 | 최대 절대 차이 0.0000382 이하 | CPU 변환 일치도 |
| 관련 로컬 테스트 | 216개 통과, 4개 skip | skip은 통과로 집계하지 않음 |

데이터 그룹은 분자 26,699개, 제조 조건 9,600개, 유화 조성 89개로 분리했다. 유화 실험은 훈련 136건·검증 24건·평가 36건이며, 같은 조성의 다른 교반 속도를 분할 간에 나누지 않았다.

실제 기본 설정의 API에서 향수·로션 제조 안내와 유화 예측이 HTTP 200으로 응답했다. 향수·로션·바디워시의 세 제품 시간 예측도 같은 모델 해시를 사용했고, 합성 물리 입력의 질량수지 오차는 약 `2.61e-18 mg/cm²` 이하였다. 레시피 요청 중 과거 메인 모델 로더를 강제로 실패시키는 조건에서도 정상 응답하여 새 모델 연결을 확인했다.

고정 예제의 향수 조향 점수는 97.67, 로션은 95.20이었다. 초기 통합 후보의 로션 점수 84.16 회귀는 전체 146차원 프로필 비율을 보존하는 추가 학습과 float32 신경망 출력→float64 최적화 경계의 정밀도 수정으로 해결했다. 기준 점수·참조 관측값·원료 필드는 바꾸지 않았다. 이 예제 두 건을 전체 요청 통과율이나 실제 향 유사도로 확대 해석하지 않는다.

CPU 분자 128개 묶음 forward 중앙 시간은 약 9.35 ms이며 특징 계산 시간은 제외한 값이다. 예제 API의 첫 요청은 향수 약 6.23초, 로션 약 14.57초였다. 전체 서비스 평균 지연이나 운영 Modal 측정치는 아니다.

원료 29,259행과 활성 후보 3,830개를 보존했으며, 모든 원료 필드의 이전·이후 해시가 같다. 제조 전체의 실측 결과, 숙성·장기 안정성·최종 향까지 모두 학습한 모델은 아직 아니다. 이번 완료 범위는 **단일 공유 신경망의 공동 학습, 출처 기반 전 단계 연결, 실제 유화 보조 학습 및 로컬 API 연결**이다.

확인 기록은 `.benchmarks/formulation_core_v69/train-04/evaluation.json`, `verify-02/verification.json`, `verify-default-products-01/verification.json`에 있다.

## 출처

- [PerfumersWorld: 기능별 조향](https://www.perfumersworld.com/formulating-by-function.php)
- [Lotioncrafter: 냉간 유화 예시](https://lotioncrafter.com/blogs/skin-care/basic-lotion-with-simulgel-eg)
- [Lotioncrafter: 가열 로션 예시](https://lotioncrafter.com/blogs/skin-care/light-sprayable-lotion)
- [FDA: 화장품 제조·검사 기록 지침](https://www.fda.gov/cosmetics/cosmetics-guidance-documents/good-manufacturing-practice-gmp-guidelinesinspection-checklist-cosmetics)
- [Rodgers, 2025: Emulsification Drop Size Distribution Dataset, CC BY 4.0](https://doi.org/10.48420/30178552)
- [Deep Sets: 순서 불변 집합 표현의 이론적 참고](https://papers.nips.cc/paper/6931-deep-sets.pdf)

공개 코드나 외부 조향 서비스를 붙인 구현이 아니다. 출처 자료와 수학적 원리를 참고해 이 프로젝트의 입력·제약·추론에 맞춘 신경망을 직접 구현했다.
