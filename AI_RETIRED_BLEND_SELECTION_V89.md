# 미달 배합 10건만 제외

사용자가 지정한 향수 3건·바디로션 7건의 **배합 결과만** 재선택 대상에서 제외합니다.
영어·한국어 기록 중 같은 배합인 두 쌍이 있어, 10개 기록은 8개의 고유 배합에 해당합니다.

- 향 종류와 자연어 요청은 차단하지 않습니다.
- 원료는 삭제하거나 후보군에서 빼지 않습니다. 같은 원료의 다른 배합비도 사용할 수 있습니다.
- 제품 구분, 완제품 향료 농도, 원료별 배합비로 제외 배합을 식별합니다. 요청 문구로 차단하지 않습니다.
- 향수와 로션의 기존 탐색 중 제외 배합은 선택하지 않고 다른 계산 후보를 계속 검토합니다.
- 모델 가중치, 물리 계산, 점수와 통과 기준, 기존 API 필드의 자료형과 인증은 유지합니다. 제외가 적용된 탐색에는 선택 정책의 진단 정보가 추가될 수 있습니다.
- 임의로 배합비를 조금 바꾸거나 점수를 올리지 않습니다. 대안도 기존 평가를 그대로 받습니다.

배합 식별 정밀도는 농축액 비율 소수점 4자리이며, 완제품 향료 농도도 구분합니다.
원본 평가 기록은 그대로 보존합니다. 기록에서 실패를 지워 전체 성능을 100%로 바꾸는 작업은 아닙니다.

## 확인

저장된 800건의 최신 기록을 대조해 지정한 미달 10건만 제외 목록과 일치하고,
기존 통과 790건은 제외 목록과 일치하지 않음을 확인했습니다.
서로 다른 버전에서 모인 저장 기록을 검사한 것이며, 800건을 새로 생성한 평가는 아닙니다.

관련 로컬 테스트 100개와 배포 준비 패키지의 테스트 84개가 통과했습니다.
테스트용 향수·로션 계산에서 한 배합을 제외한 뒤 같은 요청과 원료군으로 다른 배합을 선택함도 확인했습니다.
이 테스트 수는 향 예측 정확도나 새 800건 통과율을 뜻하지 않습니다.

배포 준비 패키지: `.benchmarks/v89_backend_handoff/package-11-retired-blends`
wheel SHA256: `5cbbd9e1d2746998d68bbbf0d4dc42f3cad36ffb96ea7fc56187f8e7f987fdff`
기존 운영 package-10에서 나머지 소스 209개, 신경망 가중치, 목표 참조와 원료 29,259개는 보존했습니다.

핵심 변경은 `retired_blends.py`, 향수의 `service.py`, 로션의 `lotion_reference_search.py` 세 파일입니다.
기존 운영 package-10 위에 이 세 파일만 올리는 준비 스크립트를 제공합니다.
2026-09-16 15:30:47 KST에 기존 주소와 인증을 유지하여 Modal v23으로 배포했습니다.
배포 후 QA 기록은 `MODAL_DEPLOYMENT_V89.md`와 `output/backend-v89-retired-blends-deployed-20260916/`에서 구분합니다.

배포 후 10개 요청을 새로 계산한 결과, 모두 다른 배합을 반환했으나 90점 목표 통과는 0건입니다.
영어 시트러스→우디 머스크 로션 1건은 내부 호출 301.511초로 300초 기준을 넘었습니다.
따라서 이 변경으로 전체 목표 달성이나 모든 요청의 정상 레시피 반환이 보장된다고 표시하지 않습니다.
