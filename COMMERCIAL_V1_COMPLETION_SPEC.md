# Perfumery AI Core 상용화 후보 완료 계약

## 목적과 적용 범위

이 계약은 높은 계산 점수가 실측 인간 후각 정확도, 제품 안전 또는 제조 승인으로 오인되지 않도록 합니다. 모든 자동 gate를 통과한 빌드는 **상용화 후보 R&D 코어**일 뿐이며, 승인된 시장 처방이 아닙니다.

실제 인간 관능 결과가 없으면 `actual_olfactory_similarity_score`는 반드시 `null`입니다. `simulated_similarity_score`, `physsim_similarity_score`, 모델 신뢰구간과 90점 기준은 계산 프록시이며 실제 인간 후각 유사도 90%를 뜻하거나 증명하지 않습니다.

## 자동화된 코어 gate

다음 gate는 후보 생성과 재현 가능한 R&D 검증의 최소 조건입니다.

1. **자연어 목표**: 다국어 용어, 부정, 강도, 제외 조건, 원료 별칭과 시간 의도를 회귀 테스트합니다.
2. **처방 제약**: 농축액 합계, 활성물질/공급 상태 단위, 원료별 상한, 비용, 조달 조건, 희귀성, 제품 농도와 향 피라미드가 후보 생성 단계에서 검사됩니다.
3. **과학 프록시**: 농도, 희석, 헤드스페이스, 휘발성, 시간축, 공개 물성·역치와 혼합 상호작용을 후보 비교에 사용합니다. 누락 데이터는 적용 가능성·불확실성으로 표기하며 측정값으로 승격하지 않습니다.
4. **학습 모델**: 고정 split, 누출 감사, 강한 기준선, ablation, 다중 seed, 불확실성·OOD 조건을 아티팩트 manifest에 기록합니다. 로컬 SHA-256은 무결성 보조 수단이며, 상용 신뢰는 독립 서명·모델 레지스트리로 추가 검증해야 합니다.
5. **데이터 계보**: 각 출처의 URI, 해시, 레코드 수, 라이선스/사용 경계, 갱신일, formulation 영향 여부를 기록합니다. 라이선스나 재배포 권한이 미확정인 자산은 상용 배포에서 격리합니다.
6. **부분 안전 screen**: 금지·고위험·희귀 원료와 내장 IFRA 부분 규칙을 검사합니다. 내장 규칙은 전체 IFRA Standards나 시장별 법규의 대체물이 아닙니다.
7. **아티팩트 검증**: 테스트, 벤치마크, 데이터 감사, 모델/데이터 해시 감사, wheel 빌드와 isolated install을 clean 환경에서 실행합니다.

## 상용 제조·판매를 위한 별도 필수 gate

다음은 현재 코어의 로컬 메타데이터만으로 충족되었다고 판정할 수 없습니다. 인증된 증거 서비스와 인간·제조 검증으로 확인해야 합니다.

- 실제 공급 SKU/로트별 COA, SDS, IFRA 증명서, 정량 알레르겐, 재고·리드타임과 원본 문서 해시
- 목표 시장·제품 카테고리의 최신 전체 규제 검토와 버전 고정 룰팩
- 적격 블라인드 인간 관능 연구와 처방 단위 결과
- 실제 베이스·포장·공정에 대한 파일럿 배치, 안정성, 포장 적합성, 품질 시험
- 완성 제품 범위에 묶인 외부 규제 승인자의 전자서명, 인증서 유효성·권한·폐기 상태 검증
- 원료·로트·농도·베이스·포장·시장·룰팩·데이터·모델·코드가 포함된 canonical release specification

필수 항목 하나라도 없거나, 만료·변조·범위 불일치·권한 미확인이면 제조 및 판매 release는 차단되어야 합니다. 처방이나 release specification이 바뀌면 관련 증거와 승인은 재검토·재승인되어야 합니다.

## 보안·감사·운영 완료 조건

- OIDC/RBAC로 후보 생성, 증거 입력, 검토, 승인, 배포 권한을 분리합니다.
- 증거 원본과 모델 release envelope를 신뢰 루트로 검증하고, 내용 해시·서명자·유효기간·폐기 상태를 확인합니다.
- actor, 시각, 전후 상태, 대상 release specification, 근거 문서를 포함한 append-only 감사 로그를 유지합니다.
- request-scoped DB 연결, 동시성·락·재시도 정책, 작업 큐, timeout·자원 제한, 모니터링과 백업·복구를 검증합니다.
- 잠긴 의존성, 컨테이너/환경 digest, SBOM, 라이선스 정책, CI provenance attestation을 release gate에 포함합니다.

## 완료 판정

자동 gate를 통과하면 상태는 `commercial_candidate_automated_gates_passed`일 수 있습니다. 이는 계산 프록시와 아티팩트 계약이 정해진 조건을 통과했다는 뜻입니다.

`manufacturing_ready` 또는 시장 출시 승인은 위의 상용 증거·보안·운영 gate가 모두 검증된 경우에만 허용됩니다. 단순한 로컬 DB 행, 파일명, 문자열 해시 또는 미검증 보고서 참조만으로는 이 상태를 부여할 수 없습니다.
