# 배합 개선 정체 원인 진단 — 2026-09-14

## 결론

코드 오류와 설계 문제가 모두 있다. 신경망 크기나 학습 횟수 하나로 설명할 수 없다.
특히 현재 배합 표현의 도달 가능 범위, 수정 폭 선택, 마지막 채택 기준과 학습 목표의 불일치가 겹친다.

사용자의 원인 확인 요청 이후 제품 코드 수정은 중단하고 진단만 수행했다. 그 전에 시작한 V74 12-epoch 학습은 종료되어 체크포인트를 보존했다. 이 후보는 고정 최적화기 비교 조건을 통과하지 못해 활성화하지 않았다. 배포·기본 모델·인증 설정은 변경하지 않았다.

## 1. 배합 목표 자체가 현 표현 범위 밖에 있는 경우 — 설계 문제

가장 최근 완료된 V72 후보의 향수 400건·로션 400건 원본 응답을 다시 읽고 800개 파일의 해시를 확인했다. V74의 전체 400건 결과가 아니다.

| 제품 | 기존 95점 통과 | 확인한 병목 |
| --- | --- | --- |
| 향수 | 27/400 | 186/400건은 현재 원료 벡터·가격·배합 한도 아래의 검증된 상한부터 95점 미만 |
| 로션 | 248/400 | 152/400건이 `search_incomplete`로 표시됨 |

향수의 상한은 조향 원료 수와 추정 노트 구간을 완화한 문제의 상한이다. 일부 요청은 반복 횟수를 늘려도 현재 고정된 표현·제약 안에서 95점에 도달할 수 없다. 이는 실제 향의 불가능성 증명이 아니라 현재 소프트웨어 모델의 범위다.

로션의 `search_incomplete`는 불가능성 증명이 아니다. 이 152건에는 계산 점수가 없는 1건도 들어 있다. 향수와 로션의 실패를 같은 원인으로 묶으면 잘못된 개선 방향이 된다.

근거: `.benchmarks/v74_autoregressive/diagnose-01/service_audit.json`

## 2. 좋아질 수 있는 작은 수정을 버림 — 최적화 논리 문제

완료된 V74 체크포인트로 검증용 192개 배합 문제를 다시 계산했다. 학습에 사용하지 않은 검증 세트이며, 서비스 요청 400건이나 사람 평가가 아니다.

| 제품 | 수정 단계 | 거절된 단계 | 같은 방향에서 수정 폭만 줄이면 오차가 감소한 단계 |
| --- | --- | --- | --- |
| 향수 | 768 | 192 | 120 |
| 로션 | 768 | 189 | 92 |
| 합계 | 1,536 | 381 | 212 |

거절 381회 중 212회, 약 55.6%는 더 작은 이동에서 개선 가능했다. 진단에서는 1/2부터 1/512까지의 몇 가지 축소 폭만 확인했으며, 그 대안은 실제 결과에 채택하지 않았다.

현재 구현은 한 번 제안한 배합이 나빠지면 이전 배합을 그대로 유지한다. 같은 방향의 작은 단계를 다시 찾지 않는다. 질량 기준 이동 폭은 제한하지만 원료별 방출 민감도는 크게 달라, 질량상 작은 이동도 향 프로필에서는 과도한 이동일 수 있다.

위치: `fragrance_ai/recommender/constrained_autoregressive.py:224`, `fragrance_ai/research/constrained_autoregressive_network.py:196`

근거: `.benchmarks/v74_autoregressive/diagnose-final/report.json`

중간 체크포인트 진단에는 거절 833회 중 483회가 해당했다. 최종 체크포인트에서는 거절 횟수 자체가 줄었으나 같은 문제가 남았다. 두 수치는 서로 다른 학습 시점이다.

## 3. 최신 배합으로 수정한 뒤 과거 배합에 다시 섞음 — 재현된 코드 오류

신경망은 `current_lines()`의 최신 채택 배합을 받아 수정안을 만든다. 그러나 서비스는 부분 수정안을 만들 때 최신 배합 대신 고정된 `dose_baseline`을 기준으로 섞는다. 자기회귀 단계에서 계산한 주변 탐색과 실제 검사하는 주변 탐색이 달라진다.

같은 혼합 함수를 사용한 2원료 재현:

- 최초 배합 80:20, 현재 배합 20:80, 새 제안 15:85, 목표 19:81.
- 현재 배합 점수: 99.00.
- 최초 배합 기준 25/50/75/100% 혼합: 55.25 / 71.50 / 87.75 / 96.00. 전부 현재보다 나쁘다.
- 최신 배합 기준 25% 혼합: 99.75. 개선된다.

완성 제안 자체는 검사하지만, 그 제안의 올바른 중간 경로가 사라지는 오류다. 실제 향수 조성의 효과량 추정이 아니라 배합 좌표 계약을 검증하는 최소 수치 재현이다.

위치: `fragrance_ai/recommender/service.py:1427`
수정 방향: 기존 1회 탐색의 동작은 보존하고, 자기회귀 제안의 부분 혼합은 해당 제안을 만든 최신 배합을 기준으로 고정해서 평가한다.

## 4. 작은 양의 방출값이 정규화 과정에서 왜곡됨 — 재현된 계산 오류

분모를 무조건 `max(total, 1e-12)`로 바꾼 뒤 향 비율을 계산한다. 실제 총 방출이 양수이지만 이 값보다 작으면 결과가 합계 1인 프로필이 되지 않는다. V74는 이 V73 함수를 재사용한다.

실제 호출 형식처럼 방출 행 최댓값을 1로 맞춘 경우에도 재현된다:

- 원료별 프로필: `[1,0]`, `[0,1]`.
- 방출 계수: `[1e-18,1]`, 배합: `[1,0]`.
- 올바른 정규화 향: `[1,0]`.
- 현재 계산: `[0.000001,0]`.
- 목표가 `[1,0]`일 때 오차 0이어야 하지만 현재 최악 프로필 오차는 약 0.5.

이 사례는 약한 방출 또는 후기 시간대의 계산 정확성 문제를 증명한다. 전체 400건 실패의 주원인이라고 입증한 것은 아니다. 계산된 실제 원료 방출 은행에는 1e-18 수준의 계수가 존재한다.

위치: `fragrance_ai/recommender/autoregressive_neural.py:30`, `fragrance_ai/research/autoregressive_network.py`의 동일 정규화; V74 `features`에서도 재사용.
수정 방향: 양의 신호를 안정적으로 재조정한 뒤 정규화하고, 진짜 무신호는 별도로 처리한다. 학습·CPU 추론·목표 생성에 같은 수식이 필요하다.

## 5. 학습에서 좋아지는 대상과 서비스의 채택 기준이 다름 — 학습 설계 문제

- V73: 2~12개 후보의 합성 배합 문제, 두 제품 모두 146차원 원료 프로필, 임의의 양의 방출 시나리오로 학습했다. 실제 호출에서는 2,000개 이상 후보와 향수의 19차원 평가를 만난다.
- V74: 8~3,830개 후보, 원료 한도·가격·계산된 방출값까지 확장했지만, 목표는 여전히 무작위로 만든 실현 가능한 배합의 프로필이다. 실제 자연어 목표와 이전 탐색기가 정체된 배합 상태를 직접 학습한 것은 아니다.
- V74 학습은 시간·출력 전체의 최악 TV/cosine 오차를 줄인다. 향수 서비스는 명목 프로필과 시간 가중 평균 점수의 최솟값을 쓰고, 별도 강도·지속·기존 신경망 점수 보존도 확인한다. 로션은 회피 향과 배경 향 구분도 확인한다.
- 신경망의 학습 손실은 거절된 제안의 손실과 추가 페널티를 포함한다. 서비스가 실제 유지하는 배합의 최종 점수와 같은 값이 아니다.

V73 실제 API 보존 응답에서도 `citrus scent`의 신경망 수정안은 전체 프로필 71.220700→71.221371로 미세하게 좋아졌지만 기존 인지 점수 보존 조건에서 거절됐다. 이 조건을 삭제해서 해결할 문제가 아니라, 생성 단계부터 함께 만족하도록 목표와 제약을 맞출 문제다.

위치: `scripts/train_constrained_autoregressive_v74.py`, `fragrance_ai/research/constrained_autoregressive_network.py:200`, `fragrance_ai/recommender/profile_match.py`, `fragrance_ai/recommender/service.py`

## 6. 이번 학습이 바꾼 범위

기존 분자 인코더와 7개 출력 헤드의 1,868,933개 파라미터는 그대로다. V74에서 학습한 것은 배합 수정용 GRU의 36,467개 파라미터다. 따라서 원료 프로필 자체의 편향이나 고정된 표현 범위를 이 학습으로 모두 개선했다고 볼 수 없다.

추가로 향수 검증에서 정규화 없이 GRU에 들어가는 물리 기울기 크기의 중앙값은 약 141이었고, 업데이트 게이트의 약 31.3%가 0 또는 1 근처에서 포화됐다. 입력 크기 조정의 필요성을 보여주지만, 포화 비율만으로 성능 정체의 원인 비중을 단정하지 않는다.

## 완료된 V74 학습 결과

12 epoch, 학습 768개·검증 192개·최종 시험 384개. 검증으로 체크포인트를 선택했고 최종 시험은 선택에 사용하지 않았다. 원료는 공유하므로 분자 비중복 평가가 아니다.

아래 점수는 제약을 만족하는 합성 역배합 문제의 계산 점수다. 실제 API의 400개 자연어 요청 성능이 아니다.

| 방법 | 향수 평균 / 95점 통과 | 로션 평균 / 95점 통과 |
| --- | --- | --- |
| V73 제안 후 동일 제약 적용 | 49.28 / 19/192 | 81.41 / 54/192 |
| 고정 최적화기 | 68.56 / 20/192 | 87.28 / 39/192 |
| V74 학습된 수정기 | 66.17 / 22/192 | 87.63 / 50/192 |

V74는 V73보다 두 제품의 평균 오차는 줄였지만, 향수 평균에서는 고정 최적화기를 넘지 못했고 로션 95점 통과 수는 V73보다 낮다. 모든 지표의 상위 개선판이 아니다. 이 사실을 숨기지 않고 `accepted_for_local_inference=false`로 보존했다.

CPU와 학습 모델 출력의 최대 차이는 약 `1.13e-7`, 기존 본체 파라미터는 바이트 단위 동일, 시험 배합의 질량·원료 한도·가격 검사는 통과했다. 단순 CPU 변환 오류나 과거 체크포인트를 잘못 학습한 문제가 주된 설명은 아니다.

체크포인트: `.benchmarks/v74_autoregressive/train-01/model.json`
SHA256: `0ee5e2ad3ca7392f9f0f396d0a287d51a3b14bee1845c3ea44b0baa3dbeb9801`
결과: `.benchmarks/v74_autoregressive/train-01/completion.json`

## 수정 우선순위

1. 최신 배합 기준 부분 수정과 작은 단계 재시도 연결.
2. 약한 방출의 안정적 정규화와 민감도에 맞는 좌표·입력 크기 처리.
3. 제품별 실제 채택 점수와 추가 보존 조건을 학습 과정에 일치시킴.
4. 이미 탐색이 정체된 배합 상태와 다양한 자연어 목표로 학습 문제를 구성.
5. 향수의 상한 미달 186건은 고정 원료 프로필·목표 표현·실제 제약의 충돌을 따로 분해해 개선.

점수 문턱을 낮추거나 제약을 제거하는 작업과는 구분한다. 이번 진단은 원인과 재현 증거를 정리한 것이며, 위 수정이 완료됐다는 보고가 아니다.
