# V89 배포와 개선 결과

## 현재 배포: 응답시간 개선판 / Modal v24

2026-09-16 20:03:17 KST에 `.benchmarks/v89_lotion_latency/package-13`을 기존 URL로 배포했다.

- 물질 이동 추론에서 0 질량·빈 제조 이력 때문에 사용되지 않는 분자 계산 및 출력층 계산을 생략했다.
- 원료 자료·모델 가중치·평가식·목표 기준·지정 배합 제외 정책은 유지했다. 재학습이나 점수 보정 변경은 없다.
- v23 패키지에서 `formulation_core.py`만 변경했다. 나머지 211개 Python 모듈은 보존했다.
- wheel SHA256: `68ae8d18c5490af771f2fbfb55db0b57d0992e1f542e1b1dfd3bde6d5dfbb307`
- 묶음 SHA256: `0038af4a3ce7ff6f870ac73822ed8dfdd9bd469de6b44b6e12a922685c24d21d`
- CPU 1·RAM 1 GB·최대 웹 컨테이너 1개·웹 함수 제한 **300초** 유지. URL·Proxy 인증과 기존 키를 변경하지 않았다.
- 로컬 설치 패키지의 관련 회귀 테스트 133개 통과. 고정 1,049개 원료 배합의 최종 계산 단계는 7.66초에서 2.87초로 단축됐으며 해당 단계의 JSON은 완전히 같았다. 이는 전체 요청의 단축률이 아니다.
- Linux 이미지의 코드·자료·모델 해시 검사와 배포 후 기본 API 검사 14건이 기대 결과를 만족했다. 빈 근거 저장소의 의도된 422도 포함한다.
- 기존 301.511초 로션 요청을 배포 이미지에서 두 번 새로 계산해 **285.466초·284.237초, 모두 HTTP 200·결과 캐시 miss**를 확인했다. 이 내부 API 재측정에서는 300초 초과가 재현되지 않았다. 해당 요청은 여전히 모델 점수 85.5626의 미달 후보이며, 시간 개선을 품질 통과로 바꾸지 않았다.
- 시간초과 사례의 배포 후 반복 재측정 결과와 검증 범위는 [별도 보고서](output/AI_DEPLOYED_LATENCY_V89_20260916.md)에 기록한다. 공개 HTTPS의 실제 백엔드 토큰 검사는 여전히 별도다.

## v23 지정 배합 제외판 배포 기록

2026-09-16 15:30:47 KST에 package-11-retired-blends를 기존 URL로 배포했다.
사용자가 지정한 향수 3건·로션 7건의 배합 결과만 재선택 대상에서 제외한다.
향 종류나 원료를 제거하지 않으며, 같은 요청의 다른 배합은 기존 탐색과 평가를 그대로 거친다.

- 운영 v22를 기준으로 조향 코드 3개만 변경했고 나머지 소스 209개, 가중치와 참조 자료는 보존했다.
- wheel SHA256: `5cbbd9e1d2746998d68bbbf0d4dc42f3cad36ffb96ea7fc56187f8e7f987fdff`
- 묶음 SHA256: `b4da2cf3f12302bb5bb8cd70051a1eec914de07302544365fe302a02030dfc2e`
- 기존 URL, Proxy 인증, CPU·메모리·동시 실행·시간 제한은 유지했다.
- 설치 이미지의 패키지·모델·자료 해시와 목표 기본값 검사가 통과했다. 배포 후 AI QA도 완료했다.
- 로컬 100개·설치 패키지 84개 테스트 통과. 저장된 통과 790건과 제외 목록의 충돌은 0건이다.
- 이 변경은 배합 제외 정책이며 점수·통과율을 올리는 재학습이나 새 800건 성능 평가가 아니다.

### v23 배포 후 QA 결과

- 기본 검사 14건과 추가 API 호출 77건의 원문을 저장했다. 이 중 1건은 같은 이미지에 만든 격리된 빈 근거 저장소 검사다.
- confidence 숫자|null, 로션 기본 풀, 조향 SSE, 감사·버전 이벤트와 보고서 SSE, 진단 evaluate/reassess, 저장 후보 비교·수정, 보완 질문, 분당 30회 이후 429 반환을 확인했다.
- 지정 배합 10건은 새 요청 모두에서 재출력되지 않았다. 10/10건이 다른 배합을 반환했으나 **90점 목표 통과는 0/10**이었다. `recipe`는 비어 있고 별도 후보 필드에 배합이 있다.
- 모델 점수 범위는 80.2491~89.9658점이다. 배합을 제외한 것으로 품질 문제가 해결됐다고 보지 않는다.
- `body_lotion/extra-6`는 내부 ASGI 호출에 301.511초가 걸려 300초 웹 함수 기준을 넘었다. 추가 QA 그룹은 12/13 통과이며 전체 QA 판정은 실패로 보존했다.
- 이 지연 사례에는 1,049개 원료 항목이 있었고, 1,015개가 각각 0.0001% 미만이었다(이 항목들의 합계 0.002236%). 비용 증가의 단서이며 전후 동일 환경 대조로 원인을 확정한 것은 아니다.
- 동일 설치 패키지 회귀는 212개 중 211개 통과했다. 실패 1개는 과거 V32 봉인 배포 파일을 요구하는 시험이며 직전 v22 패키지에서도 동일하게 실패했다. 현재 배포 로션 생성은 별도 실제 이미지 검사에서 성공했다.
- 측정 시간은 이미지 내부 호출 시간이다. 실제 백엔드 Proxy Token을 쓰는 공개 HTTPS 종단간 검사는 기존 지시대로 백엔드팀 담당이며, 키 발급·교체·권한 변경은 하지 않았다.
- 기본 원문: `output/backend-v89-retired-blends-deployed-20260916/verification.json`.
- 추가 QA·10개 요청 원문: `output/ai-qa-modal-v23-20260916/qa.json`.
- 보고서: `output/AI_QA_MODAL_V23_20260916.md`.

## v22 연동 수정판 배포 기록

2026-09-16 14:10:02 KST에 package-10을 기존 URL로 배포했다. 모델은 v21 배포 패키지를 유지하고 백엔드 연동 파일 7개만 변경했다. 조향 모듈 130개의 동일성을 확인했으며 package-04~08 모델 실험은 포함하지 않았다.

- confidence는 API 경계와 OpenAPI에서 number|null을 강제한다. 설명 문자열은 confidence_kind다.
- 로션 registry_pool 생략 기본값을 core에서 conditional_research로 변경했다. 명시적 core 선택과 기존 위험도·가격·안전 조건은 유지한다.
- 공개·운영자 근거가 모두 없으면 change-impact는 계속 HTTP 422이며 detail.status=abstained, detail.code=EVIDENCE_SNAPSHOTS_MISSING이다.
- wheel SHA256: `73da4e6ec9467ee0732592940ec4669f978338414cce469dcb04b6792df74f16`
- 묶음 SHA256: `461441cba29e2fb8a7fd7997d75718b8042c0309f3642b8cd0959884aa577260`
- 테스트 114개 통과. 실제 배포 이미지에서 confidence, 생략·명시 풀의 동일 배합, 빈 근거 격리 조건의 422 응답을 확인했다.
- 현재 원문: `output/backend-v89-wirefix-deployed-20260916/`.
- 실제 백엔드 토큰을 쓰는 공개 HTTPS 검사는 사용자 지시에 따라 백엔드 팀 담당이다.

## v21 배포 및 모델 개선 기록

2026-09-16 13:10:34 KST, 기존 `perfumery-ai-core` 앱에 **V89 / Modal v21**을 배포했다. 주소와 기존 인증 방식은 유지했다. 중간 배포 v20 기록도 보존한다.

- URL: https://junseong2im--perfumery-ai-core-web.modal.run
- 배포 진입점: `deploy/modal_release_v89.py`
- 설치 패키지: `.benchmarks/v89_backend_handoff/package-03`
- wheel SHA256: `7328241f25dec72eee5310ad1137bc985c0c03498c65646585659658d6cb407f`
- 묶음 SHA256: `9ef568c3fa0b59176b212306fa30dd1e3a77f1cae544f7ce9c99ef0127c5ff44`
- 공유 신경망 체크포인트: V76, `158b9f82ff9fb784eeec114ef1f7bbfc4d613badeb98f46e0f60435d5af38d2f`
- CPU 1, RAM 1 GB, 컨테이너 최대 1개, 웹 함수 제한 300초. GPU 사용 없음.

## 반영한 변경

1. `confidence`는 숫자 또는 null로 반환한다. `heuristic_only` 같은 설명은 `confidence_kind`로 분리한다. 실제 배포 이미지 응답에서도 확인했다.
2. 생성·평가·로션 설계 관련 10개 요청 경로의 기본 목표를 90으로 연결했다. 명시적으로 입력한 95 등의 목표와 실제 계산 점수는 그대로 유지한다.
3. `change-impact`의 HTTP 200 응답을 OpenAPI의 `ChangeImpactResponse`에 명시했다. `status`, `comparison_kind`, `diagnostic_only`, `manufacturing_approval`을 일관되게 제공한다. 기존 before/after/changes와 근거 게이트는 보존한다.
4. `GET /v2/evidence/versions`로 실제 등록된 이전·현재 버전을 조회한다. 임의의 이전 버전을 입력해 발생하던 연결 혼동을 줄인다.
5. `GET /v1/applications/unified/coefficients/contract`에서 바디워시 계수의 단위, 범위, 출처 구분, 단계·원료별 필수 입력을 안내한다. 기본값이나 실측 자료를 만들어 넣지는 않는다.
6. 3,809개 원본 개념·4,606개 별칭에 장면 60개·표현 194개, 스타일 24종·표현 96개를 연결했다. 새 사전과 해석 API는 `/v1/ai/odor-language`, `/v1/ai/odor-language/interpret`다. 장면 해석은 프로젝트가 정의한 조합이며 새 실측 후각 축이 아니다.
7. 클린 참조와 배경의 구분에 소스 기반 로그 비율 판정을 적용했다. 다른 향은 기존 코사인 구분 기준을 유지한다. 저장된 로션 400건의 판정 재검사에서 기존 통과의 탈락은 없었고, 실제 재생성에서도 클린 한·영 2건이 통과했다.
8. 향수 배합 수정의 최적화 목적을 최종 평가의 명목·시간 가중 집계와 맞췄다. 점수 상수 가산이나 고정 90점 치환은 사용하지 않는다.
9. 한 요청 내 하위 탐색이 남은 시간 예산을 공유한다. 코어 예산 270초와 이후 준비·최종 검사·직렬화의 여유를 구분했다. 예산 소진 후 새 최적화를 시작하지 않는다. 협력적 제한이므로 모든 요청의 응답 시간 상한을 증명한 것은 아니다.

## 배포 이미지 검사

`output/backend-v89-final-deployed-20260916/verification.json`에 원문과 해시를 보존했다.

- 로컬 관련 회귀 테스트 179개 통과.
- Linux 이미지 설치 시 패키지·모델·자료 해시 및 새 경로 검사 통과.
- 실제 배포 이미지에서 health, 근거 버전, 계수 계약, 향 사전·문장 해석, change-impact, 바디워시 context/predict, 로션 생성, 향수 생성, 조향 SSE 11건 HTTP 200 확인.
- 무인증 공개 URL 호출은 401.
- 실제 백엔드 Proxy Token 검사는 사용자 지시에 따라 백엔드 팀 담당으로 남겼다. 새 토큰 발급·교체·권한 변경은 하지 않았다.

실행 경로는 기존 Modal 관리자 SDK 인증 → 실제 배포 컨테이너 내부 ASGI다. 공개 HTTPS에 백엔드 토큰을 넣은 호출과 구분한다.

배포 이미지의 우디 예제:

| 제품 | 계산 점수 | 레시피 원료 수 | 계산 호출 시간 |
| --- | ---: | ---: | ---: |
| 향수 | 91.990706 | 12 | 13.93초 |
| 바디로션 | 90.131675 | 73 | 7.65초 |

단일 예제의 내부 호출 시간이며 전체 요청 평균, p95 또는 공개 네트워크 왕복 시간이 아니다. 바디워시 예측 검사는 명시적으로 합성 계수를 사용했다. 검증 완료 실측 계수표는 아직 제공되지 않는다.

## 하던 미달 요청 개선의 위치

기존 미달 12건을 새로 계산해 **2건 추가 통과, 10건 미달**을 확인했다. 이미 통과한 전체 요청을 다시 생성한 800건 전수 평가가 아니다. V89 package-02 평가와 package-03의 장시간 3건 재평가를 합친 기록이며 패키지별 해시를 구분했다.

- 클린 로션 한·영 2건: 새 배합 각각 30개 원료, 계산 점수 90.0001, 목표 및 구분 판정 통과.
- 아쿠아틱 향수·로션과 시간 변화형 일부 요청: 아직 90점 미달.
- 점수가 전부 오른 것은 아니다. 향수의 시트러스→플로럴→우디 요청은 이전 88.095851에서 시간 예산 보완판의 84.930059로 낮아졌다. 같은 요청의 시간은 약 477초에서 295초로 줄었다.
- 따라서 전면 품질 향상 또는 400/400 완료라고 주장하지 않는다.

감사 기록: `.benchmarks/v89_backend_handoff/failure-audit-final-03.json`.

## 별도 실험 후보 — 배포하지 않음

V89 package-04는 오차가 큰 요청에 앞단 탐색 시간을 더 주는 후보다. 위 향수 요청에서 88.041839까지 회복했지만 327.16초가 걸려 기존 웹 함수 제한 300초를 초과했다. 이 후보는 채택하지 않았다. package-05~08의 배합 재사용 실험도 현재 운영에 포함하지 않았다. v23은 package-03의 모델과 package-10의 연동 수정을 유지하고 지정 배합 제외만 추가한 package-11-retired-blends였다. **현재 v24는 이 v23 위에 물질 이동 계산 최적화만 적용한 package-13**이다. 소스 작업 공간의 실험 변경과 배포 패키지를 혼동하지 않는다.

후보의 가중치를 재학습한 것은 아니며, 원료·참조 데이터도 이번 작업에서 교체하지 않았다. 실험 원문은 `.benchmarks/v89_backend_handoff/adaptive-perfume-04`에 보존했다.
