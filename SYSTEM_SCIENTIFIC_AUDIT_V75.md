# V75 전체 시스템 진단

확인일: 2026-09-15. 대상은 로컬 V75이며, Modal 배포본을 검사한 보고서가 아니다.

## 결론

개선이 더딘 원인은 단순히 신경망이 작거나 반복 횟수가 적기 때문이라고 보기 어렵다. **데이터에 들어 있는 정보, 학습하는 정답, 탐색 중 계산, 최종 판정이 서로 다른 문제를 다룬다.** 그 상태에서 탐색기와 일부 학습 지표를 개선해 왔다.

V75를 기본 로컬 모델로 연결할 때 확인한 195개 회귀 테스트와 6개 API 사례는 전체 품질 향상의 증명이 아니었다. 실제로 당시 향수 한 사례는 0.24165점 하락했다. 이전 완료 기록도 전체 400개 재검사를 완료하지 않았음을 명시한다. 이 보고서는 그 검증 공백을 분리하고, 수정 대상을 전체 파이프라인으로 다시 정의한다.

현재 상태는 **전체 진단 범위를 확장한 상태이지, 아래 문제를 전부 수정한 상태가 아니다.** 이 작업에서는 제품 코드·가중치·승인 상태·배포 설정을 변경하지 않았다.

## 이번에 검사한 범위

| 범위 | 실제 수행 | 결과가 의미하는 것 |
|---|---|---|
| 활성 원료 전체 | 3,830개 물성·식별자·희석 정보 대조 | 연결된 정량 정보의 범위 |
| 모든 고정 요청 | 400개 자연어 해석·향 축·낙관적 도달 범위 계산 | 요청 표현과 원료 표현의 구조적 문제 |
| 향수 계산 경로 | 전체 3,830개 원료를 각각 기준 원료와 2성분 배합, 동일 64회 표본으로 대조 | 제안 계산과 최종 계산의 수치 일치 여부 |
| 로션 이동 계산 | 256개 속도 조건을 독립 행렬지수 계산과 비교 | 비음수성·질량 보존·시간 구간 합성의 정확성 |
| 과학·모델 회귀 검사 | 27개 테스트 파일, 429개 테스트 | 429개 통과, 실패·오류·생략 0 |
| 로컬 API 기능 표면 | 고정 V75 앱의 capabilities 및 OpenAPI 확인 | 43개 경로 등록, 기능별 지원 상태 확인 |
| 전체 생성 성능 | 향수 400개 + 로션 400개, 고정 wheel·모델·요청·95점 기준으로 새 실행 | 실행 중. 800개 완료 전 최종 통과율로 사용하지 않음 |

400개는 현재 프로젝트의 고정 한국어·영어 개발 요청이다. 모든 가능한 자연어, 모든 제형, 모든 API 조합을 뜻하지 않는다. 2성분 수치 대조 역시 생성 레시피의 성공률이 아니다.

## 1. 원료 등록 수와 정량 예측 정보가 크게 다르다

활성 원료 3,830개를 같은 런타임 조회 방식으로 확인했다.

| 값 | 연결 원료 수 | 활성 원료 대비 |
|---|---:|---:|
| 분자 물성 레코드 | 3,826 | 99.90% |
| 증기압 | 9 | 0.23% |
| 끓는점 | 17 | 0.44% |
| 증기압 또는 끓는점 | 17 | 0.44% |
| 냄새 역치 | 80 | 2.09% |
| 휘발성 값과 냄새 역치를 모두 보유 | 5 | 0.13% |

3,796개 레코드는 구조 계산 출처로 시작한다. 분자량·극성 같은 계산값이 있다는 뜻이지, 그 원료의 증기압·냄새 역치·제형별 방출이 측정됐다는 뜻은 아니다. 일부 구조 계산 레코드에는 별도의 물리값이 결합되어 있으므로 해당 행의 모든 필드를 계산값이라고 단정하지 않는다.

현재 대부분의 향수 원료는 노트별 증기압·역치 추정값과 넓은 불확실성 분포를 사용한다. 원료 후보 수만 늘려서는 이 병목이 해소되지 않는다.

### 이미 로컬에 있지만 런타임에 연결되지 않은 자료

기존 headspace 자료 DB를 읽기 전용으로 검사했다. 전체 InChIKey가 정확히 같은 활성 원료에 대해 증기압 296개, 끓는점 630개, Henry 상수 92개가 대응한다. 이름이나 구조 골격만으로 합친 결과가 아니다.

이 숫자는 **추가 연결 후보**다. 단위·온도·출처·중복 관측의 일치 여부를 검토하고 실제 조회 경로에 연결하기 전에는 예측 커버리지에 포함하면 안 된다. OPERA 자료의 실험값 후보와, 검증 게이트를 통과하지 못한 별도 증기압 예측 모델도 구분해야 한다. 실패한 예측 모델은 활성화하지 않았다.

## 2. 향 표현의 공통 연결이 비어 있다

최종 향수 목표는 19개 축이고, 보조 신경망은 146개 원천 표현을 투영해 활용한다. 현재 투영에는 fresh, aquatic, white_floral, amber의 출력 연결이 없다.

전체 400개 요청에서 확인한 결과:

- 144개: 이 네 축 중 하나 이상 포함.
- 132개: 지원 축과 미지원 축이 함께 포함.
- 12개: 보조 신경망이 지원하는 요청 축이 없음.

이는 144개 요청 전체가 반드시 실패한다는 뜻은 아니다. 다른 계산 경로가 계속 작동한다. 그러나 해당 요청에서는 보조 신경망과 최종 목표가 같은 표현 공간에서 판단하지 않는다.

다음 항목도 구분해야 한다. 원천 자료의 high/low는 숫자로 검증된 희석 농도가 아니며, applicability/use는 절대 향 강도 측정값이 아니다. 이런 값을 단순히 물리 농도나 수용체 활성으로 해석할 수 없다.

## 3. 학습 데이터가 최종 문제의 정답은 아니다

현재 기본 모델 학습 자료를 만드는 코드에는 다음 세 가지가 명확히 구분되어 있다.

1. 분자 표현: 기존 동결 예측기의 출력을 정답으로 사용한 증류.
2. 혼합물 표현: 성분별 프로필을 질량비로 평균한 합성 정답.
3. 제조 절차: 향수·냉제 로션·가열 로션의 세 절차 템플릿에서 만든 다음 행동 및 조건 충돌 정답.

자료 행 수가 늘어도 독립적인 제조 과정, 측정된 혼합물 상호작용, 새로운 원료 실험이 같은 비율로 늘어나는 것은 아니다. 세 절차를 따르는 능력과 모든 조향사·모든 로션 제조 조건의 결과를 예측하는 능력은 구분해야 한다.

V75의 자기회귀 학습은 전체 원료 후보군을 포함하며, 이전 배합과 상태를 다시 입력받는 구조도 실제로 학습했다. 하지만 학습·검사에서 재료 은행을 공유한다. 분리된 레시피 난수는 미지 분자·미지 제형 검증과 같지 않다.

## 4. 학습하는 계산과 최종 계산의 차이

자기회귀 학습의 제안 모델은 대략 다음 형태다.

`프로필 = Σ(질량비 × 시간별 응답 계수 × 원료 프로필) / Σ(질량비 × 시간별 응답 계수)`

향수 최종 판정은 여기에 직접 대응하지 않는다. 액상 몰분율에서 기상 농도와 OAV를 계산한 뒤, Hill 포화 반응·성분 간 억제·시간별 증발·확률 표본 평균을 적용한다. 따라서 선형 제안 모델의 손실이 줄어도 최종 배합 점수가 반드시 좋아지지는 않는다.

비선형 후처리가 그 간격을 줄이고 있지만, 학습 자체가 최종 연산 전체의 정답을 배운 것은 아니다. 또한 학습 모델과 고정 최적화기를 비교할 때 검토하는 이동 폭 수가 다르므로 계산량을 맞춘 비교가 추가로 필요하다.

## 5. 실제 수치 버그와 정상 동작을 분리했다

### 정상 동작으로 확인된 부분

전체 원료를 사용한 3,830개 2성분 진단에서 제안 계산과 최종 향수 계산의 최대 프로필 차이는 `3.23648e-7`이었다. 100점 척도로 환산하면 약 0.000032점이다. 이 시험으로 전체 혼합 공간을 보증할 수는 없지만, 거대한 통과율 격차를 이 수치 차이 하나로 설명할 근거도 없다.

로션의 256개 조건에서는 독립 행렬지수 해와의 최대 차이가 `2.31e-14`, 질량 보존 오차가 `2.66e-15`였고 음수 질량은 없었다. 로션 물질 이동 코드를 전부 틀렸다고 판단할 근거는 없다. 다만 방정식의 수치 정확성과 입력 계수의 실측 정확성은 별개다.

### 재현한 경계 조건 문제

- **0 투입량:** 작은 양수로 바꾸는 처리 때문에 존재하지 않는 성분에 작은 향 신호가 생긴다. 64개 0투입 성분을 추가한 진단에서 최대 프로필 차이는 약 0.000121이었다. 현재의 일반적인 양수 배합이 모두 이 영향을 받는다는 뜻은 아니다.
- **행 분할:** DoseModel에 같은 원료 ID를 두 행으로 나누면 포화·억제 계산이 달라졌다. 진단 차이는 약 0.115였다. 최종 공통 표본 경로는 중복 ID를 거부하며, 현재 활성 원료에는 같은 구조·같은 원액 농도의 중복 그룹이 없다. 따라서 현행 400개 실패의 원인으로 단정하지 않는다.
- **희석 원료의 운반 용매:** 향료 몰수에는 원액 농도를 적용하지만 분모에서 원료 안의 운반 용매 몰수를 빠뜨린다. 완제품 농도 100% 진단에서 순물질과 1% DPG 희석액의 신호가 동일했다. 현재 활성 은행에는 희석 원료가 없으므로 적용 범위와 실제 영향을 구분해야 한다.
- **작은 값 처리 위치:** 제안 계산은 시간별 감쇠 뒤 하한을 적용하고 최종 계산은 감쇠 전 하한을 적용한다. 고휘발성 조건에서 불일치가 재현됐으나 이번 실제 원료 대조에서는 차이가 작았다.

이 네 항목은 정합성 수정 대상이다. 이들을 고친 것만으로 전체 성능 문제가 해결됐다고 발표해서는 안 된다.

## 6. 물리·후각 모델의 가정

현재 포화 지수 0.55, 노트별 증기압·역치, 분자량·극성 기반 수용체 접근 계수, 향 프로필 유사도 기반 성분 억제는 모델 가정이다. 성분별 수용체 결합 상수·효능을 실제로 식별한 모델이 아니다.

경쟁 결합으로 혼합물 반응을 설명하는 연구에서도 성분별 수용체 반응에 기반한 매개변수를 사용한다. 현재 코드의 향 라벨 유사도를 그 측정값과 동일하게 볼 수 없다. [경쟁 결합 연구](https://pmc.ncbi.nlm.nih.gov/articles/PMC6511041/), [후각 혼합물 길항 작용 연구](https://pmc.ncbi.nlm.nih.gov/articles/PMC5915184/)

Monte Carlo 반복 수를 늘리면 지정한 가정에 대한 적분 오차는 줄어들 수 있다. 그러나 입력 가정 자체의 오류를 교정하지 않는다. 현재 코드도 구간의 의미를 `prior_propagation_not_calibrated_prediction_error`로 구분한다.

## 7. 목표 정의와 탐색 비용도 공통 병목이다

실제 응답에서 단순한 “citrus scent”는 시트러스 1, 나머지 18개 축 0으로 해석된다. 사용자가 다른 향을 전부 배제한다고 말한 경우와, 주요 인상만 표현한 경우가 같은 닫힌 목표 벡터로 취급될 수 있다. 반면 로션은 원천 자료에서 만든 조건부 기준 프로필을 사용한다. 제품별 95점이 같은 대상을 비교하지 않는다.

활성 원료 3,830개 전체를 대상으로 비용·한도·시간 변화를 무시한 매우 낙관적인 기하학적 상한에서는 400개 중 2개만 95점 미만이었다. 이는 나머지 398개가 달성 가능하다는 증명이 아니다. 요청별 허용 원료, 가격, 배합 한도, 비선형 시간 변화까지 반영한 상한과 실제 최적화 결과를 함께 보아야 한다. 과거 버전의 도달 불가 개수를 현 버전의 숫자로 재사용하지 않는다.

원료 전체와 요청별 사용 가능 원료도 다르다. 이번 시트러스 요청에서는 활성 3,830개 중 구하기 어려움 조건으로 1,696개가 제외되어 2,134개가 탐색에 들어갔다. 후보 일부를 코드가 임의로 고정한 것인지, 요청 조건 때문에 제외된 것인지 나누어 보고해야 한다.

추가 탐색은 시간 예산에 따라 종료된다. 컴퓨터 부하에 따라 시도 횟수가 달라질 수 있으므로, 모델 비교에서는 동일 계산량 검사가 필요하다. 이번 6개 작업자 전체 실행은 로컬 부하 조건의 진단이며 이전 실행과의 점수 차이를 전부 가중치 개선 효과로 해석하지 않는다.

## 8. API 등록과 기능 완성은 다르다

고정 V75 앱의 `/v1/ai/capabilities`는 HTTP 200이며 공통 체크포인트가 V75임을 확인했다. 43개 OpenAPI 경로가 등록되어 있고, 생성·보완 질문·후보 비교·감사 SSE·보고서 경로도 존재한다.

그러나 다음 기능은 현재 지원 상태가 아니다.

- 개인 피부 적합성 자동 판단.
- 범용 pH 적합성 판단. 입력된 반응 계수로 가수분해를 계산하는 기능과 다르다.
- 자연어로 지정한 향 전환 속도의 직접 제어.
- 바디워시 전체 베이스 제형 자동 설계. 향료 생성과 제공된 세정·잔류 계수 기반 예측은 존재한다.
- 임의 로션 매트릭스에 대한 검증된 범용 방출 모델.

이를 하나의 “전체 기능 완료”로 묶어서는 안 된다. 각 API의 입력 조건과 실제 계산 범위를 유지해야 한다. 이번 검사에서 외부 승인 상태나 규제 게이트를 바꾸지 않았다.

## 전체 수정의 단위와 완료 조건

향 이름별 패치나 특정 테스트만을 위한 조정을 반복하지 않고 다음 연결 단위로 수정해야 한다. 아래는 이번 진단에서 도출한 수정 명세이며 아직 구현 완료로 표시하지 않는다.

| 연결 단위 | 전체 적용 범위 | 완료 판단 |
|---|---|---|
| 물성·신원·단위 | 전체 활성 원료와 모든 물리 계산 소비자 | 출처·단위·온도·농도·누락이 같은 의미로 전달되고 전체 커버리지 표가 재생성됨 |
| 향 표현·목표 | 자연어, 원료, 신경망, 향수·로션 평가 | 19/146/450 축의 대응과 미지원 처리가 일관되며 미지 표현을 거짓 양성으로 채우지 않음 |
| 공통 순방향 계산 | 학습 정답, 제안 계산, 재평가, 최종 JSON | 동일 배합·농도·환경에서 같은 정의의 계산을 사용하며 독립 수치 검사를 통과함 |
| 학습 | 분자·혼합·공정·자기회귀 전체 데이터 | 정답 출처를 분리하고 제안 손실과 최종 성능의 개선이 함께 확인됨 |
| 탐색 | 모든 요청별 허용 원료 | 제약 보존, 계산량 정합, 미달 원인 분류, 불필요한 중복 평가 해소 |
| 제품 API | 향수·로션·바디워시와 관련 흐름 | 지원 여부와 필수 계수가 일치하며 미지원 기능을 완료로 보고하지 않음 |
| 성능 판정 | 고정 향수 400 + 로션 400 | 전 사례·실패·점수 없음·악화 사례를 포함한 결과와 비교 원본 보존 |

## 재현 자료

- [전체 진단 원본 JSON](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/science-04/report.json)
- [원료별 물성 연결](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/science-04/materials.json)
- [원료별 계산 경로 대조](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/science-04/cross-engine-materials.json)
- [400개 요청별 표현·상한](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/science-04/request-geometry.json)
- [독립 로션 계산 대조](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/science-04/transport-oracle.json)
- [429개 회귀 결과](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/scientific-regression.xml)
- [800개 실행의 현재 진행 상태](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/full400-before-01/summary.json)
- [800개 고정 실행 조건](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/.benchmarks/v75_system_audit/full400-before-01/protocol.json)
- [반복 가능한 전체 진단 코드](/C:/Users/user/Desktop/Newera/WantedAI_Ai-dev-clean/scripts/audit_scientific_system_v75.py)

모델 SHA256: `4c0357b7a6acb121402b97ed1a8acf975c9457dc82b01ca8bf6ea03c32918745`

wheel SHA256: `50143b1eb9270e581a801ce0ef3835356d8458a0e9095bc038de7bacf09b5d89`

전체 생성 실행은 고정 wheel을 별도 디렉터리에 풀어 실행하므로 이후 작업 파일 수정과 분리된다. 원본 응답은 압축 JSON과 해시로 보존된다. 이 보고서의 정적 진단 수치는 고정되어 있으며, 생성 시험의 최신 진행 상황은 위 summary.json에서 확인한다.
