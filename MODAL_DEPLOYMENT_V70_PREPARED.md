# V70 배포 준비 기록

2026-09-14. **로컬 준비·검증만 완료했으며 실제 Modal 배포는 하지 않았습니다.** 현재 운영은 V69/v17입니다.

## 보존한 연결과 자원

- 앱 이름 `perfumery-ai-core`와 함수 이름 `web`을 유지합니다.
- 기존 Proxy 인증을 유지합니다. `requires_proxy_auth=True`이며 새 키를 만들지 않았습니다.
- CPU 1개, 메모리 1,024 MiB, 컨테이너 최소 0·최대 1, 유휴 종료 300초, 요청 제한 300초를 유지하도록 작성했습니다.
- 기존 별도 CPU 언어 워커를 그대로 연결합니다. GPU나 유료 외부 LLM을 추가하지 않습니다.

## 준비한 파일

- 배포 진입점: `deploy/modal_release_v70.py`
- 비공개 묶음 조립·검증: `deploy/runtime_release_v70.py`
- 실제 로컬 묶음: `tmp/modal-runtime-v70/release-01`
- 파일 374개, 256,708,691 bytes. 원료·학습된 추론 가중치·필요한 부모 모델·공개 규제 원문과 관측 이력을 포함합니다.
- 원본 학습 코퍼스·Windows 언어 실행 파일·사용하지 않는 모델은 넣지 않습니다. 공개 자료의 재배포 허가를 새로 얻었다는 의미가 아닙니다.

| 고정 대상 | SHA256 |
| --- | --- |
| 비공개 묶음 | `f94ea18d1035fb8112f1cb21265448311eec909466aa7d4d94994325792cfb23` |
| wheel | `22767b37f437e92306423782e63cee88834b8ac91b130f504a36cb38941375bd` |
| 공개 규제·공급 자료 | `495b8d1276a5bd9b77f36bf6ad3ab4f575712dd6bcccbad563bb3aa6b82a534d` |
| V69 공유 체크포인트 | `3186e467446abc71f249c052c8248c17c97d0e337976abf3666628af47dd997f` |

## 확인한 것

원문 한 개가 빠지거나 변경되는 경우, 해시가 없는 경우, 예상하지 않은 파일이 들어온 경우, 공개 자료 경로로 실행 파일을 넣는 경우를 거부합니다. 프로필의 모든 간접 참조까지 다시 검사합니다.

새로운 경로로 옮긴 묶음과 설치된 wheel을 사용해 원료 3,830개·네 규제 체계와 API 시작을 확인했습니다. 저장소 밖으로 옮기면 깨지던 원료 DB 경로는 프로필에 묶인 실제 파일을 사용하도록 고쳤습니다. 시작 전후 묶음 파일이 변하지 않았는지도 확인했습니다.

검증 기록은 `.benchmarks/v70_repair/bundle-installed-check.json`, 설치된 패키지 검사는 `.benchmarks/v70_repair/installed-final-09/report.json`입니다. 실제 운영 URL에 새 버전이 적용됐다는 기록이 아닙니다.

## 실제 배포 시 구분할 것

실제 배포에는 사용자 승인이 필요합니다. 승인 후 이 진입점으로 배포하면, Linux 이미지 안에서도 묶음과 설치된 패키지의 사전검사를 먼저 수행하도록 구성했습니다. **그 Linux 이미지 빌드와 운영 부하 검사는 아직 실행하지 않았습니다.** 로컬 시작 성공을 운영 성능이나 메모리 보증으로 바꾸어 표시하지 않습니다.

새 소스나 자료를 바꾸면 wheel·카탈로그·프로필·비공개 묶음을 함께 다시 고정해야 합니다. 이전 V69 진입점과 묶음은 보존합니다. 원료·규제·공급 자료의 상세 범위와 백엔드 처리는 [V70 자료 연결 계약](AI_REGULATORY_DATA_V70.md)을 따릅니다.
