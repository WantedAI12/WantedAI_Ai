# V79: 자료 부족과 지원 불가를 구분한 수정

## 결론

이전의 “128개는 정량 자료가 부족해서 지원 불가”라는 설명은 부정확했다. 실제 자료 부족뿐 아니라 최소 표본 수 제한, 구조 모호성, 표현 연결 누락, 전체 요청 차단 로직이 섞여 있었다.

기존에 미연결로 분류했던 128개를 전부 다시 대조했다. 그중 **118개는 이미 확보한 자료를 바탕으로 제한적 참조 연결이 가능했다.** 새로 측정한 관능 데이터는 0개다. 출처의 냄새 주석을 기존 분자 모델에 연결한 추정과 기존 참조의 합성·별칭 연결을 실측 자료와 구분한다.

## 무엇을 잘못 판단했고 어떻게 바꿨나

| 이전 처리 | 수정한 처리 |
| --- | --- |
| 원료 예시가 3개 미만이면 모두 제외 | 1~2개라도 출처가 있는 예시는 사용하되, 제한된 예시이며 모집단 검증이 아님을 기록 |
| 한 이름에 구조가 여러 개면 제외 | 같은 연결 구조의 입체화학 대안을 보존하고 고정 해석 가정을 공개 |
| `green_fruity`를 하나의 미지 향으로 처리 | 공백·밑줄·하이픈을 구분하여 명시된 green + fruity 참조를 연결 |
| `raisins`와 `raisin`을 별도 미연결 대상으로 처리 | 확인된 단수·복수 표현을 같은 기존 참조에 연결 |
| 요청 중 일부 향이 미확인이면 전체 후보를 버림 | 해석 가능한 부분으로 후보를 만들고 미해결 요구를 별도로 보존 |
| 부분 요청에서 검색 점수도 없어 정교화가 정지 | 내부 최적화에는 부분 점수를 사용하되 전체 점수로 노출하거나 통과 처리하지 않음 |
| `skin scent` 같은 관용 표현의 다른 해석이 숨겨짐 | 관용 표현과 문자 그대로의 향 참조를 함께 제시하고 후보만 반환 |
| 무취를 다른 향처럼 양의 프로필로 요구 | 냄새 강도·베이스 조건으로 구분하며 무첨가와 실제 무취가 같다고 주장하지 않음 |
| 자료 미연결을 “안전 원료로 조향 불가”라고 안내 | 참조 연결 문제와 원료 안전성 문제를 구분하는 문구로 수정 |

규칙은 전체 참조 컴파일러와 향수·로션 후보 검색에 적용했다. 무작위 유사어 치환, 다른 향 계열로의 자동 대체, 사용자 제외 조건 해제는 하지 않는다.

## 기존 128개 중 다시 연결한 118개의 구성

- 출처가 있는 제한적 향 주석 예시를 모델 참조로 연결: 113개. 아이리스, 리치, 유향, 압생트, 김치 등이 포함된다.
- 이름에 연결된 구조 대안을 보존하여 연결: 3개. 리날룰, 카르본, 보르네올이다.
- 기존 건포도 참조의 복수 표현 연결: 1개.
- green + fruity 명시적 복합 표현 연결: 1개.

구조 대안의 평균은 실제 배합비나 이성질체 분포를 의미하지 않는다. 예시 1개의 불확실성을 오차 0이나 폭 0인 신뢰구간으로 표시하지 않는다. 제한적 자료의 모델 점수는 그 자료 범위의 일치도이지 모든 사용자가 느끼는 해당 향의 정확도가 아니다.

다른 명시적 복합 표현 연결도 함께 반영되어 전체 참조 프로필은 **489개에서 622개**로 늘었다. 이전 489개 프로필과 출처 정보는 그대로 보존했다. 실제 원료 행, 원료 안전·가격 조건, 신경망 가중치, 95점 승인 문턱도 변경하지 않았다.

## 아직 구분해 남겨둔 10개

출처의 정확한 원료 예시 연결을 찾지 못한 9개: burnt candle, butyric, chalky, chlorine, industrial, pine - in (pine oil), sausage, seminal/sperm-like, turnip.

나머지 1개는 odorless(무취)다. 이것은 냄새 주석 자료가 없는 것이 아니라 양의 향 프로필과 다른 종류의 요구다. 제품 베이스의 냄새와 실제 감지 조건 없이 “무취 완료”로 바꾸지 않았다.

이 명단은 “재현이 불가능한 향” 목록이 아니다. 현재 확보한 자료와 구현으로 남은 연결·해석 요구 목록이다. 원래 배포 보류 대상 134개 ID를 삭제하거나 분모를 바꾸지 않았다.

## API 응답

기존 주소와 인증 방식은 바꾸지 않는다. 아래는 로컬 수정 버전의 응답이다.

- 전체 참조를 구성할 수 있으면 기존 전체 점수와 판정 흐름을 사용한다.
- 일부 긍정적 요구가 미해결이면 `closest_candidate`에 후보를 반환한다. 향수의 `calculated_profile_similarity`, 로션의 `score`는 `null`이며 전체 기준 통과는 `false`다.
- 부분 점수는 향수 `full_profile_assessment.partial_profile_score`, 로션 `partial_profile_score`로 분리한다. `score_scope`는 `resolved_positive_intent_only`다.
- `unsupported` 또는 `unsupported_requirements`에는 미해결 내용을 보존한다. `reference_evidence`에는 제한적 지원, 모델 추정 여부, 구조 대안 등을 기록한다.
- 모르는 향의 제외 조건은 만족한 척하지 않는다. 모순된 요구나 모든 목표가 미확인인 경우에도 임의의 기본 향을 생성하지 않는다.
- 무취 해석은 `odor_absence_condition`으로 구분한다. 향료 무첨가만으로 제품이 실제 무취라고 판정하지 않는다.

## 검증과 실행 파일

- 관련 회귀 테스트: **162개 통과**, 실패·오류·건너뜀 0개.
- 전체 개념·어휘 3,809개 참조 컴파일 검사. 원래 보유한 개념은 삭제하지 않았다.
- 고정 요청 400개의 목표 구성 가능 여부: **400/400**. 조향식 95점 통과율이 아니다.
- 원래 보류 대상 134개의 실제 문장 입력 전수 검사: 123개 참조 구성, 1개 관용 표현의 복수 해석, 9개 참조 미연결, 1개 무취 조건으로 구분했다.
- 설치한 wheel의 실제 API **12건 통과**. 압생트·리날룰 후보 생성, 일부 미확인 요청의 향수·로션 후보 보존, 해석 모호성·무취 분리, 미확인 제외 조건 차단을 확인했다.

실행 프로필: `perfumery.v79-integrated-03.local.json`. 검증 후 기본 `perfumery.local.json`도 같은 설정으로 선택했다. 이전 V77 명시 프로필은 보존했다.

기록: `.benchmarks/v79_source_support/recovery-02/report.json`, `resolution-ledger.json`, `api-final/report.json`, `regression-final.xml`.

wheel SHA256: `9aa64f7f1479892bb9341481b8461aa9d966fecdf1d9fb212008030133c8462a`.

기존 V76 신경망 SHA256: `158b9f82ff9fb784eeec114ef1f7bbfc4d613badeb98f46e0f60435d5af38d2f`.

신경망을 다시 학습한 변경이 아니라 **자료 연결·표현 해석·후보 검색의 제외 판단을 수정한 변경**이다. 새 모델의 후각 정확도가 향상되었다고 입증한 결과는 아니다.

## 배포

Modal은 이 작업에서 변경하지 않았다. “모두 정량 참조를 확보한 다음 배포”하라는 사용자의 조건을 유지한다. 모델 추정치를 실측 자료로 바꿔 적거나 무취 항목을 명단에서 삭제하여 배포 조건을 통과시키지 않는다.
