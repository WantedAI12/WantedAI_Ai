# V63 응답속도·토큰 사용 최적화

2026-09-10 로컬 구현·검증 기록이다. 이후 같은 최적화를 V63로 Modal에 반영했다. 아래 로컬 수치와 원격 수치는 구분하며, 현재 배포 정보는 [MODAL_DEPLOYMENT.md](MODAL_DEPLOYMENT.md)에서 확인한다.

## 실제 측정

같은 로컬 CPU 환경, 단일 BLAS 스레드, 실제 고정 모델·원료 카탈로그·API를 사용했다. LLM은 모의 응답이 아닌 고정된 Qwen3-0.6B-Q8_0 모델이다.

| 항목 | 변경 전 | 변경 후 |
| --- | ---: | ---: |
| 조향 첫 요청, 피오니와 청사과 향 | 72.97초 | 38.43초 |
| 위 요청의 실제 전체 원료 LP 실행 횟수 | 42회 | 14회 |
| 위 요청의 전체 원료 LP 누적 시간 | 48.32초 | 16.78초 |
| 동일 조향 요청 재호출 | 0.070초 | 0.073초 |

첫 조향 요청은 47.34% 단축, 약 1.90배 빨라졌다. 변경 전후 각각 한 번의 비프로파일링 측정이며, 모든 요청의 평균·p95 또는 Modal 성능을 입증한 결과는 아니다. 캐시 재호출 속도는 기존부터 빠르며 이번 개선의 주요 성과로 집계하지 않았다.

배합비, 원료 목록, 향 점수, 시간별 예측, 승인 상태를 포함한 응답 본문은 동일하다. 바뀐 필드는 패키지·카탈로그 배포 식별 해시 4개뿐이다. 해당 예제의 점수는 기존과 같은 87.42977282790069이며 95점 기준 미달 상태도 그대로 유지된다.

## 토큰 및 비용

- 기본 조향 생성은 외부 LLM 호출이 없고, 선택형 언어 모델도 호출하지 않는다. 따라서 이 경로의 LLM 토큰 사용량은 0이다. 비용 절감 대상은 CPU 계산 시간이다.
- 보완질문은 정확히 동일한 메시지와 동일한 모델 식별값일 때, 유효한 구조화 해석을 120초 동안 재사용한다. 최대 64개·1 MiB로 제한한다.
- 실제 질문 `머스크 없이 장미 향 바디로션으로 만들어줘`의 첫 호출은 5.699초, 재호출은 0.140초였다. 두 응답 본문이 같고 LLM 호출은 1회에서 0회가 됐다. 재호출은 입력 재처리와 출력 생성을 하지 않는다.
- 새 질문·만료된 결과는 기존 프롬프트와 최대 출력 160토큰을 유지한다. 외부 유료 LLM API 비용은 없지만 자체 모델의 CPU 비용은 발생한다.
- 프롬프트 축약안 두 개는 기존에 맞던 해석을 틀려 채택하지 않았다. 20개 진단 요청에서 기존 16/20, 축약안 3/20 및 15/20이었다. 이 숫자는 언어 해석 진단이며 향 재현 정확도가 아니다.

기존 요청/응답 JSON 형식은 유지했다. 추가된 HTTP 헤더는 `X-Perfumery-Language-Cache`와 `X-Perfumery-LLM-Calls`이며 백엔드는 필요하면 로깅할 수 있다. V63 배포에도 기존 API 주소와 인증을 유지하므로 새 키나 연동 방식 변경은 필요하지 않다.

## 구현과 보존한 동작

- 전체 목적함수·제약 행렬·경계·해법·수치 허용오차를 해시한 뒤, 한 조향 요청 안에서 동일하고 성공한 LP 결과만 재사용한다. 8 MiB·64개 한도이며 요청 종료/예외 시 폐기한다.
- 시간초과·불가능 판정·예외는 캐시하지 않는다. 새로운 원료, 제한값, 목표, 행렬, 해법 또는 허용오차는 별도 문제로 계산한다. 반환 배열은 복사해 다음 계산에 의한 변조를 방지한다.
- 후보 하나의 공급 가용성 평가에서 전체 카탈로그를 반복 탐색하던 부분을 해당 배합 원료 조회로 바꿨다.
- 전체 원료 후보 범위, 12/24/사용자 상한 탐색, 200회 최종 시뮬레이션, 모델 가중치, 95점 기준, 안전·농도·가격 제한 및 최종 검증은 변경하지 않았다.
- 언어 모델 오류·유효하지 않은 JSON은 캐시하지 않으며 기존 결정론적 복구 경로를 유지한다. 캐시 hit에도 런타임 변경 감지를 수행한다.

## 검증 및 산출물

- 새 최적화·언어 회귀 테스트: 25개 통과.
- 관련 전체 원료 LP 회귀 테스트: 12개 통과. 변경과 무관한 파서 테스트 등 19개는 선택하지 않았다.
- API·런타임 정책·모델 선택·기존 언어 응답 검증: 49개 통과.
- 총 86개 관련 테스트 통과. 전체 저장소 테스트 또는 전체 400개 조향 평가를 재실행한 것은 아니다.
- 새 파일의 정적 검사 및 변경 파일 공백 검사 통과.
- 모든 29,259개 카탈로그 행과 원료별 데이터는 동일하다. 활성 상태도 3,830개로 변경하지 않았다. 모델 파일은 재학습·변경하지 않았다.

로컬 패키지: `dist/latency-v63/candidate-01/wheel/perfumery_ai_core-1.4.0-py3-none-any.whl`

패키지 SHA256: `136ab1c78238bc8a62a8ad11ba0b030dc411b3c098ac8ffa01a61696b9c8eefd`

카탈로그 manifest SHA256: `105046997799bb60d165ad4e24baa23e081e4b63b4ff2e625649de2e57e2d813`

로컬 프로필 SHA256: `aff49eb757f31e19844cea94be99a91577d8a60055fb4b4740c66c6ae71ef686`

원료 snapshot SHA256: `8c962601b6567d56bd1043733b2b2eaccf9deb5d35e4aba70101271fe65bf139`

원본 측정: `.benchmarks/latency_v63/before-wall`, `after-wall`, `verification-01`. 축약안 실패 결과는 `language-01`, `language-02`에 보존했다. 단일 스레드 지정 및 해법 교체 실험도 별도 보존했고, 의미 있는 속도 개선이 없어 적용하지 않았다.

재현 스크립트: `scripts/profile_runtime_latency_v63.py`, `scripts/verify_latency_v63.py`, `scripts/benchmark_language_tokens_v63.py`. 측정 경로는 매번 새 디렉터리를 지정해야 한다. 로컬 실행은 새로운 catalog binding을 선택한 `perfumery.local.json`을 사용한다. 현재 배포 진입점은 `deploy/modal_release_v63.py`이며 기존 V62 배포 스크립트는 과거 릴리스용으로 보존했다. `benchmarks/latency_v63_summary.json`의 배포 여부는 로컬 측정 당시 상태이며 현재 배포 상태가 아니다.
