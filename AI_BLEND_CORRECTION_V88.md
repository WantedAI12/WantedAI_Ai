# V88 배합 보정

로컬 구현입니다. Modal 배포나 GitHub push는 하지 않았습니다.

## 보정하는 대상

보정값을 원료의 실제 배합비 변화량으로 사용합니다. 향 프로필 오차를 줄이는 방향으로 원료별 질량 비율을 바꾸고, 합계 100%·원료별 상한·가격·제외 조건 안으로 다시 계산합니다. 표시 점수에 상수를 더하는 기능은 아닙니다.

향수는 현재 배합에서 농도·포화·원료 간 억제·증발을 계산해 원료별 보정 계수를 구합니다. 수정 후보는 같은 물리 모델로 다시 검사하며, 향 강도와 시간대별 보존 조건을 깨뜨리면 채택하지 않습니다. 로션은 유상·수상 방출을 반영한 행렬에서 전체 향 축과 배경 향 구별 조건을 함께 보정합니다.

반올림된 배합의 아주 작은 질량 오차는 제약 안에서 정리합니다. 향수 보정은 낮은 품질의 처음 배합이 아니라, 기존 V87 경로가 실제로 선택한 가장 좋은 미달 배합을 출발점으로 사용합니다.

## 기존 성공 결과 보존

기존 V86 계산과 V87 복구 계산을 먼저 수행합니다. 그 과정에서 사용할 수 있는 레시피가 나오면 바로 반환합니다. V88 배합 보정은 V87까지 실패한 요청에만 실행합니다. 새 보정 결과가 더 나쁘면 그 요청에서 이미 계산한 기존 후보를 유지합니다.

이전 성공 응답 787건의 전달 경로를 검사했으며, 해당 검사에서 응답 변경과 추가 보정 호출은 0건이었습니다. 저장된 응답 전달과 소스 경로를 검사한 것으로, 성공 사례의 새 시뮬레이션 검증이나 새 전체 통과율을 뜻하지 않습니다.

## 구현 파일

- `fragrance_ai/recommender/blend_correction.py`: 원료 비율 잔차 보정
- `fragrance_ai/recommender/physical_blend_correction.py`: 향수의 농도·방출 보정과 실제 물리 재계산
- `fragrance_ai/recommender/failure_recovery.py`: 이전 성공 결과를 보존하는 단계별 실행
- `fragrance_ai/recommender/lotion_reference_search.py`: 로션 생성 계산에 보정 연결
- `fragrance_ai/recommender/failure_inverse_v87.py`: 향수 생성 계산에 보정 연결

## 실제 미달 요청 검사 결과

이전 미달 13건 중 11건에서 반환된 후보의 원료 비율이 바뀌었습니다. 추가 통과는 1건이며, 12건은 여전히 미달입니다.

| 요청 | 이전 계산 점수 | 보정 후 계산 점수 | 결과 |
| --- | ---: | ---: | --- |
| 향수: 첫향 우디, 잔향 시트러스 — 영어 요청 | 88.4675 | 90.1736 | 추가 통과 |
| 로션: 첫향 시트러스, 중간향 플로럴, 잔향 우디 | 79.8064 | 85.0293 | 개선됐지만 미달 |
| 로션: 첫향 우디, 잔향 시트러스 — 영어 요청 | 89.9847 | 89.9699 | 소폭 하락, 미달 |

관측된 하락도 기록했습니다. 같은 요청 실행에서 새 보정이 기존 후보를 악화시키면 채택하지 않지만, 시간 제한을 둔 탐색의 이전 실행 수치를 모두 재현한다는 보장은 아닙니다.

로션 9건은 package-01에서, 향수 4건은 최종 package-03에서 검사했습니다. 이후 package-03의 로션 통합 경로에서도 위 로션 사례를 호출해 85.0293점과 HTTP 200을 다시 확인했습니다. 서로 다른 버전의 기록을 최신 단일 버전 전체 800건 재평가로 표시하지 않습니다.

최종 원문·해시·원료별 보정 전후 비율: `.benchmarks/v88_blend_correction/result-audit-02/report.json`. 긴 미달 복구 요청은 로컬에서 약 477초까지 걸렸습니다. 기존 백엔드·Modal 제한 내 처리까지 완료된 배포판은 아닙니다.

## 고정 검사 패키지

- 최종 향수 시작점·실행 순서 수정 패키지: `.benchmarks/v88_blend_correction/package-03-anchored`
- wheel SHA256: `76dfaed77a9189305fef8577e260f978a774eab2dd2a09e6680126043942800f`
- 같은 학습 체크포인트와 원료·참조 데이터를 유지했습니다. 이번 변경은 새 신경망 학습이 아니라 배합 계산의 보정 단계입니다.
- 최종 관련 단위·회귀 테스트: `.benchmarks/v88_blend_correction/final-unit-tests-01.xml` — 49개 통과

실제 요청별 원문과 버전별 결과는 `.benchmarks/v88_blend_correction`에 보존합니다. 후보 배합이 개선된 것과 모든 요청이 90점에 도달한 것은 별개이며, 전체 90점 달성을 완료했다고 표시하지 않습니다. 미달 경로에는 추가 계산이 있으므로 기존 서비스 시간 제한 내 응답이 보장된다는 뜻도 아닙니다.
