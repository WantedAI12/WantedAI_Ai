# V75 배합·분자 프로필 통합 개선

## 적용한 개선

- 최신 채택 배합으로 만든 수정안을 최초 배합에 다시 섞던 오류를 수정했다.
- 작은 양의 방출값을 안정적으로 정규화한다. 무신호와 작은 신호를 구분한다.
- 같은 수정 방향에서 이동 폭을 최대 1/512까지 줄여 검사한다. 학습 손실도 실제로 유지된 배합을 기준으로 계산한다.
- 향수는 명목 프로필과 시간 가중 점수의 최솟값, 로션은 전체 시나리오·출력 점수의 최솟값으로 수정한다. 명시적으로 피할 향도 포함한다.
- 신경망 물리 기울기 입력에 로그 변환을 적용했다.
- 신경망 제안을 비선형 농도·억제·시간 계산의 미분으로 추가 최적화한다. 강도·지속·기존 신경망 점수 보존 조건을 함께 계산하고, 최종 200회 물리 계산으로 다시 검사한다.
- 공유 모델의 146차원 관측·예측 원료 프로필과 독립적으로 고정된 참조 프로필을 향수 후보 생성에도 연결했다. 기존 19차원 요청 목표와 최종 점수는 바꾸지 않는다.
- 상한 미달 요청은 원료 표현과 가격·배합 한도의 영향을 별도로 진단한다. 진단용으로 완화한 조건이나 배합은 실제 생성에 사용하지 않는다.

추가 피드백은 V75에서 최대 6회, 단계 사이에 확인하는 16초 작업 예산을 사용한다. 개선이 없거나 목표에 도달하면 조기 종료한다. 이는 요청 전체의 강제 제한 시간이 아니다. V73의 기존 3회·8초 경로는 보존했다.

## 학습과 통합

향수와 로션은 하나의 1,905,400개 파라미터 체크포인트를 사용한다. CPU 추론에는 PyTorch나 외부 LLM API가 필요하지 않다.

배합 수정층 36,467개 파라미터를 8 epoch 학습했다. 학습 768개·검증 192개·최종 시험 384개이며 후보 수는 8·32·128·512·2,048·전체 원료 규모를 포함한다. 원료 한도·가격과 계산된 방출 계수를 사용한다. 초기 상태 절반은 이미 고정 최적화를 거쳤고, 목표 일부는 현재 원료로 실현 가능하다는 보장이 없는 독립적인 희소 의도다. 400개 서비스 벤치마크의 정답·통과 표시는 학습에 넣지 않았다.

본체 재학습에는 기존 분자·방출·제조 과정·혼합·유화 자료 전체를 유지했다. 매 epoch 학습 행 수는 각각 22,930·32,316·169,040·6,000·136개다. 분자는 고·저 농도 출력을 모두 학습한다.

첫 전체 파라미터 재학습 후보는 유화 오차가 증가해 채택하지 않았다. 이어 분자 인코더와 정량 프로필 출력 경로만 4 epoch 재학습했다. 원료·혼합 프로필을 개선하면서 성분 없는 공정·물리 입력 경로와 관련 파라미터를 정확히 보존했다. 미세 향 확률 MAE에는 작은 변동이 있으므로 모든 하위 지표가 좋아졌다고 주장하지 않는다.

각 학습 후 하나의 가중치 파일로 합치고 배합 수정 시험을 다시 수행했다. 모든 작업을 하나의 끝단 손실로 동시에 재학습했다고 표현하지 않는다.

## 원료·혼합 프로필 결과

기존 자료의 고정 분리 평가이며 새로운 제품 관능 실험이 아니다.

| 항목 | 기존 오차 | 개선 오차 | 상대 감소 |
| --- | --- | --- | --- |
| 분자 프로필 최악 TV 평균 — 시험 3,638개 프로필 | 0.034359 | 0.033042 | 약 3.8% |
| 혼합 프로필 최악 TV 평균 — 시험 1,000개 혼합 | 0.031553 | 0.030725 | 약 2.6% |

검증 분리에서도 두 프로필 오차가 감소했다. 원래 방출·제조 과정·유화 경로는 유지됐다.

근거: `.benchmarks/v75_autoregressive/core-comparison-02/report.json`

## 통합 후 배합 수정 결과

제약이 있는 계산 역배합 시험이다. 실제 자연어 API 400건의 통과율이 아니다. 원료 은행은 공유하므로 분자 비중복 시험도 아니다. 검증으로 epoch를 선택했으며 최종 시험은 선택에 사용하지 않았다.

| 제품 | 고정 최적화기 평균 오차 | 통합 V75 평균 오차 | 고정 95점 통과 | 통합 V75 95점 통과 |
| --- | --- | --- | --- | --- |
| 향수 | 0.368091 | 0.305611 | 14/192 | 26/192 |
| 로션 | 0.330939 | 0.317028 | 38/192 | 47/192 |

상대 평균 오차 감소는 향수 약 17.0%, 로션 약 4.2%다. 고정 비교 알고리즘과 단계 수는 같지만 V75는 작은 이동 폭을 추가 평가한다. 계산량이 동일한 속도 벤치마크는 아니다. V74와는 시험 목표·초기 상태가 달라 숫자를 직접 비교하면 안 된다.

근거: `.benchmarks/v75_autoregressive/assembled-01/completion.json`

## 실제 API 연결

신경망 호출 수, 실제 물리 검사 수, 최종 채택 수를 구분해 기록한다. 초기 연결에서 향수 제안이 실제로 채택되지 않는 문제를 확인한 뒤 비선형 물리 최적화를 보강했다. 보강 후 `floral scent`는 93.70→95.40점으로 개선됐고 신경망에서 시작한 수정이 실제 검사를 거쳐 채택됐다.

최종 패키지 API 6건은 모두 HTTP 200으로 완료됐다. 보존된 V73의 같은 요청과 비교하면 95점 통과는 2/6→3/6이고 기존 통과의 탈락은 없다. 다만 복합향 한 건의 점수는 약 0.24점 하락했다. 같은 시점·동일 계산량의 통제된 성능 벤치마크는 아니다.

| 제품·요청 | V73 보존 결과 | V75 최종 결과 |
| --- | --- | --- |
| 향수 — floral scent | 95.03 | 95.40 |
| 향수 — clean fresh citrus woody | 79.06 | 78.82 |
| 향수 — citrus scent | 71.22 | 85.61 |
| 로션 — green scent | 93.88 | 95.00 |
| 로션 — fresh scent | 95.00 | 95.00 |
| 로션 — citrus scent | 88.45 | 90.42 |

향수 세 요청에서는 신경망에서 시작한 배합 수정 21회가 실제 물리·품질 조건을 통과해 채택됐다. 로션 개선을 신경망 수정기의 직접 채택 효과로 표시하지 않는다. 관련 회귀 테스트 195개는 오류·실패·건너뜀 없이 통과했다.

최종 API 원본과 결과: `.benchmarks/v75_autoregressive/api-03/report.json`
패키지·설정 해시: `.benchmarks/v75_autoregressive/candidate-03/preparation.json`
회귀 테스트: `.benchmarks/v75_autoregressive/regression-release.xml`

이 연결 시험을 모든 자연어 요청의 성능으로 일반화하지 않는다. 이전 버전보다 모든 요청에서 항상 우수하다는 의미도 아니다.

## 체크포인트와 실행

- 모델: `.benchmarks/v75_autoregressive/assembled-01/model.json`
- 모델 SHA256: `4c0357b7a6acb121402b97ed1a8acf975c9457dc82b01ca8bf6ea03c32918745`
- 가중치 SHA256: `5550d4412e20723f8553550149ebdd6db9ba5717b8d7f71571911fc5148aea03`
- 로컬 후보 설정: `perfumery.v75-candidate-03.local.json`
- 로컬 앱 팩토리: `deploy.autoregressive_runtime_v75:create_local_app`

기본 로컬 `perfumery.local.json`도 검증한 V75 설정으로 연결했다. 기존 설정은 `.benchmarks/v75_autoregressive/activation-01/previous-perfumery.local.json`에 보존했다. 별도 환경변수 없이 선택한 모델·두 제품의 공유 체크포인트·현재 소스 해시 일치·추론 과정의 PyTorch 미사용을 확인했다. 운영 Modal은 변경하지 않았다.

최종 종합 결과: `.benchmarks/v75_autoregressive/completion.json`

기존 체크포인트·실패 후보·V74 진단·API 초기 결과는 보존했다. 소스와 개인 체크포인트·원료 데이터는 분리한다. 가중치 파일을 저장소에 공개하거나 Modal에 배포하지 않았다.

## 해석 범위

향수 186/400건의 기존 상한 미달을 모두 해소했다고 주장하지 않는다. 원료 측정치를 만들어 넣거나, 불리한 향 축을 숨기거나, 95점 문턱을 낮추지 않았다. 프로필 경로 확장과 재학습은 실제 개선이지만 기존 상한을 무효화하는 증거를 자동으로 만들지는 않는다.

물리 모델·출처 기반 프로필·계산 배합 시험과 실제 제품 향의 사용자 유사도는 다른 검증 범위다. 이번 작업에는 Modal 배포·GitHub push·인증 변경이 포함되지 않는다.
