# V77 전체 향 표현·목표 연결 확장

2026-09-15. 로컬 기본 설정 적용 완료. Modal 배포와 GitHub push는 하지 않았다.

## 적용 결과

| 구분 | 결과 |
| --- | --- |
| 기존 향 표현 사전 | 665개 개념을 모두 보존 |
| 확장 사전 | 향·특징·역사적 향 어휘 3,809개 개념 |
| 독립된 출처 기반 정량 참조 프로필 | 118 → 203개 |
| 단일 개념에서 참조 목표 구성이 가능한 항목 | 214개: 직접 연결 199개 + 명시적 특징 조합 15개 |
| 나머지 항목 | 3,595개. 어휘/자료는 등록했지만 해당 향의 정량 참조가 없음을 명시 |
| 기존 400개 개발 요청 | 새 해석기와 참조 목표 구성 400/400 성공 |
| 관련 회귀 테스트 | 251개 통과, 실패·오류·건너뜀 0개 |
| 설치 wheel API 검사 | 8건 모두 HTTP 200, 정상 경로와 근거 부족 차단을 각각 확인 |

3,809는 서로 다른 향을 모두 실측하거나 재현했다는 숫자가 아니다. 역사적 문헌의 냄새 관련 일반 단어도 포함한다. 400/400은 요청 해석·목표 연결 결과이며, 400개 레시피의 95점 통과율이 아니다. 전체 세계의 향을 빠짐없이 수집했다는 주장도 하지 않는다.

## 무엇이 달라졌나

시트러스만 별도로 보정하지 않는다. 모든 등록 개념에 같은 규칙을 적용한다.

1. 계열, 세부 향, 특징, 강도 지시, 제외 조건, 시간대 지시를 분리한다.
2. 자연어에 명시된 세부 향을 우선한다. 장미에 일반 플로럴 목표를 중복 가산하거나, 레몬을 시트러스와 별개의 필수 조건으로 이중 계산하지 않는다.
3. 출처에서 관찰된 배경 향을 보존한다. 언급하지 않았다는 이유로 다른 모든 향 축을 0으로 만들지 않는다.
4. 명시적 제외 조건은 별도로 적용한다. 요구한 향의 전체 계열을 동시에 금지하는 모순은 표시한다.
5. `90% lemon and 10% orange`는 상대적 향 의도 가중치로 처리한다. 완제품 농도나 원료 배합비로 바꾸지 않는다.
6. `fruity floral`처럼 단어 구성 자체가 명확한 표현은 각각의 참조 특징을 조합한다. `absinthe`처럼 특정 향 이름에 자료가 없으면 다른 계열로 몰래 대체하지 않는다.
7. 금지 조건만 있는 시간대는 기존 계약대로 전체의 남은 긍정 의도를 상속한다. 상속 여부를 결과에 표시한다.
8. 기존 한글·영문 키워드, 세부 향 투영 사전, 정량 모델의 모든 축을 함께 연결한다. ‘얼씨’·‘구르망’ 등의 기존 별칭을 미지의 단어로 오판하지 않는다.

## 향수와 로션

공통 해석기는 `fragrance_ai/recommender/odor_space.py`의 `compile_targets`이다. 학습/탐색에 사용할 참조 목표와 최종 평가 목표를 생성된 레시피와 무관하게 고정한다.

- 향수: 같은 146차원 참조 목표를 비선형 농도·헤드스페이스·포화·억제·증발 계산과 연결했다. 보조 가이드의 값·기울기와 최종 계산이 같은 목적함수를 사용한다. 수치 미분 검사도 통과했다.
- 로션: 같은 세부 향 해석기를 사용하지만, 기존 로션 전용 제형·유한 용량 방출·노출 적분 계산을 유지한다.
- 기존 19축 점수는 `legacy_19_axis_assessment`에 별도로 보존한다. 새 점수와 기존 점수는 직접 비교할 수 없다.
- 새 향수 경로에서 기존 19축 선호 점수는 후보 생성 참고값이다. 그 점수의 90점 사전 필터가 새 목적함수와 충돌하지 않도록 분리했다. 최종 요청 기준 95점과 원료·배합·안전·공급 조건은 낮추지 않았다.
- 기존 학습된 V76 자기회귀 신경망을 재사용하고, 같은 목적함수의 전체 후보 물리 기울기 탐색을 연결했다. 이번 작업에서 신경망 가중치를 새로 학습한 것은 아니다. 출처 기반 참조 프로필을 추가 계산하고 목표/추론 경로를 확장한 버전이다.

## 데이터와 출처

- 기존 `odor_expression_v61.json`의 모든 개념과 출처·귀속 정보를 보존했다.
- 기존 Dravnieks/Atlas 관찰 자료를 사용해 참조 프로필을 추가 계산했다. 최소 독립 식별자/유효 표본 조건을 통과한 경우만 연결했다. 기존 118개 참조 벡터는 변경하지 않았다.
- [Odeuropa 다국어 향 어휘](https://github.com/Odeuropa/multilingualTaxonomies)의 공개 영어·프랑스어·독일어·이탈리아어 자료 3,823행을 가져왔다. CC-BY-4.0, 고정 commit, 원본 파일, 파일별 SHA256, 귀속 정보를 보관했다. 이 문헌 어휘를 실측 냄새 강도로 간주하지 않는다.
- 일반 단어는 명시적으로 향을 이름 붙이는 문맥에서만 사용한다. 네 언어의 자료를 수집했다는 것과 네 언어의 모든 자연어 요청을 이해한다는 것은 다르다.
- 원료 카탈로그 29,259행의 원료 값은 바꾸지 않았다. 물질 스냅샷 SHA256도 이전과 같다. 새 향 이름을 추가했다고 원료 사용 권한이 자동으로 늘어나지 않는다.

최종 데이터: `.benchmarks/v77_odor_space/build-07/space.json`, `reference.json`, `sources/`.

## API 결과와 점수

기존 주소·인증 방식을 변경하지 않았다. `/v1/odor-expressions`, `/v1/odor-expressions/interpret`, `/v1/ai/capabilities`에 확장 사전과 참조 연결 상태가 드러난다. 향수와 로션 생성에는 같은 해석기가 연결된다.

최종 설치 wheel 검사에서 `citrus scent`는 다음과 같았다.

| 제품 | 계산 점수 | 판정 |
| --- | --- | --- |
| 향수 | 85.11795842877335 | 95점 미달 |
| 로션 | 82.29411607238173 | 95점 미달 |

두 값은 서로 다른 제품 물리 모델의 점수이며 인간 유사도 백분율이 아니다. HTTP 200은 요청 처리가 성공했다는 뜻이지 95점 승인이라는 뜻이 아니다. 알려지지 않은 향과 자료가 없는 향은 이유를 반환하고 목표 통과로 처리하지 않는 것을 확인했다.

검사 기록:

- `.benchmarks/v77_odor_space/audit-06/coverage.json`: 3,809개 개념 전수 검사.
- `.benchmarks/v77_odor_space/audit-06/fixed-request-targets.json`: 기존 400개 요청 전수 해석·목표 검사.
- `.benchmarks/v77_odor_space/regression-final.xml`: 251개 관련 테스트.
- `.benchmarks/v77_odor_space/api-04/report.json`: 최종 wheel API 검사.

## 로컬 실행 설정

기본 `perfumery.local.json`을 검증된 `perfumery.v77-integrated-04.local.json`과 같은 내용으로 연결했다. 이전 설정은 `perfumery.before-v77-20260915.local.json`에 보존했다. 기본 설정으로 앱을 새로 구성해 기능 조회 HTTP 200, 개념 수 3,809, 선택한 모델 해시를 확인했다. 기존 실행 중인 로컬 서버가 있다면 설정 변경을 읽도록 재시작해야 한다.

- wheel: `.benchmarks/v77_odor_space/package-04/wheel/perfumery_ai_core-1.4.0-py3-none-any.whl`
- wheel SHA256: `151a0d96d88fc415796170dbf1efb246d3cd8babea7954c1e3860fbca22bda91`
- 단일 V76 신경망 SHA256: `158b9f82ff9fb784eeec114ef1f7bbfc4d613badeb98f46e0f60435d5af38d2f`
- 확장 참조 SHA256: `a09c590eb9ecd4feff1c52e8d92e363642744f3fe46f5ac5c86a897a6a18ea69`
- 향 공간 SHA256: `e8ee94465c57903d5733003e17099eb09478080265091cbae637995191782a74`

이전 V76 원래 기준 800회 계산은 `.benchmarks/v76_system_repair/full400-after-01/`에서 별도로 유지한다. 새 해석 검사 결과를 그 배합 성능 시험의 결과에 섞지 않는다.
