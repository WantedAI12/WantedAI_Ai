# V87 배포 준비 상태

2026-09-16. 로컬 구현·패키지 검사 완료. Modal 업로드, GitHub push, PR 갱신은 아직 하지 않았습니다.

## 배포 설정

새 배포 진입점은 `deploy/modal_release_v87.py`입니다. 기존 `perfumery-ai-core` 앱 이름과 프록시 인증 방식은 유지합니다. 기존 V80 배포 파일과 배포된 이미지는 수정하지 않았습니다.

기본 목표는 90점입니다. `target_similarity`를 생략하면 90을 사용하며, 95를 명시하면 95를 그대로 사용합니다. 기본값은 일반 조향, SSE 조향, 보완 질문, v1·v2 평가, 재평가, 로션 설계·준비·최적화에 연결했습니다. 배포할 패키지의 OpenAPI 10개 경로와 실제 로컬 보완 질문 응답에서 확인했습니다.

응답의 `calculated_profile_similarity`와 `target_match_score`는 실제 계산값을 유지합니다. `confidence`는 숫자 또는 null이며, `heuristic_only` 같은 문자열은 `confidence_kind`에 분리합니다. 정확도나 신뢰도를 일괄 90으로 덮어쓰는 처리는 넣지 않았습니다.

## 통과한 결과 보존

기존 V86 계산을 먼저 수행합니다. 사용 가능한 레시피가 있으면 동일 결과를 즉시 반환하며 추가 개선 계산을 하지 않습니다. 미달 결과에만 V87의 원료 후보·배합·반올림 보완을 적용합니다. 한국어 향신료 표현 보완도 이 실패 복구 범위에만 적용됩니다.

저장된 성공 응답 786건의 전달 경로를 검사했고, 응답 변경과 추가 복구 호출은 모두 0건이었습니다. 이는 저장된 응답의 보존 검사이며, 성공 사례를 새 모델로 다시 시뮬레이션한 검사는 아닙니다.

## 미달 사례 재평가

남아 있던 향수 5건과 로션 9건만 다시 호출했습니다. 14건 모두 HTTP 200이며 응답 계약 오류는 없었습니다. 향수 1건이 추가 통과했고 13건은 미달입니다. 추가 통과한 요청은 '첫향은 우디, 잔향은 시트러스'이며 계산 점수는 90.4524입니다. 로션 clean 2건은 점수가 90점보다 높아도 배경 향과의 구별 조건을 충족하지 못해 통과로 바꾸지 않았습니다.

전체 800건을 새 버전으로 재평가한 결과나 400/400 달성을 의미하지 않습니다. 버전별 원문·해시는 `.benchmarks/v87_failed_recovery`에 보존했습니다.

미달 향수의 로컬 처리 시간은 약 196~332초였습니다. 실패 후 복구 계산이 추가되므로 기존 백엔드 120초 제한과 Modal 300초 제한 내 완료가 보장되지 않습니다. 제한을 임의로 늘리거나 원격 배포가 완료됐다고 처리하지 않았습니다.

## 고정된 배포 자료

- 패키지: `.benchmarks/v87_failed_recovery/package-05-target90`
- wheel SHA256: `254750f1e39c6b965c46b217ac171683d55dff8007b7e970a5b8b8e508c11f94`
- 실행 번들: `tmp/modal-runtime-v87/release-01`
- 번들 SHA256: `ef1396157a16d0034d32c202dc4f8007d693441947d9549403774672a2020d65`
- 배포 대상의 로컬 API 검사: `.benchmarks/v87_failed_recovery/release-target-check-01/report.json`

미달 14건의 계산에는 package-04-isolated를 사용했습니다. package-05는 같은 계산 코드에 API 기본 목표 연결을 추가한 패키지입니다. 두 패키지를 같은 시점의 원격 배포 검증으로 표기하지 않습니다.
