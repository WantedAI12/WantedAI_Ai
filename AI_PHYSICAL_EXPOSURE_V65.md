# V65: 제형 물질수지와 해석적 향 노출 적분

2026-09-12. 로컬 구현·검증 결과이며 소스와 재현 절차를 `dev` 변경에 포함한다. 운영 배포는 별개다.

## 무엇을 바꿨는가

외부 조향 프로젝트나 새로운 외부 학습 모델을 붙이지 않았다. 기존 원료·분자 향 표현·제형 계수에 맞춰 우리 방출 계산을 직접 개선했다. NumPy는 벡터 계산, SciPy는 기존 최적화와 독립 수치 검증에 사용한다.

기존에는 0·15·60·240·480분 같은 표시 시점의 공기 농도를 사다리꼴로 적분했다. 이 방법은 그 사이에 발생하고 사라지는 초기 방출을 거의 누락할 수 있다. V65는 방출 방정식의 구간별 해석해에서 노출 적분을 직접 계산하고 배합 최적화와 최종 레시피 평가에 같은 값을 사용한다.

목표 향 프로필, 기존 학습 가중치, 95점 기준, 원료의 안전·배합 상한·가격·가용성은 변경하지 않았다. 적분 방식이 바뀌었으므로 전후 점수 차이는 학습된 인간 후각 정확도 향상과 구별해야 한다.

## 1. 제형과 물질수지

원료별 상태는 제형 잔존량 F, 공기 중 질량 A, 피부 이동 누적량 S, 환기 배출량 X, 분해된 모물질 상당량 D이다. 단위는 모두 mg/cm²이다. 피부 이동은 일방향 손실 경로이며 인체 전체 흡수·독성 모델이 아니다.

수상 부피 V_a와 유상 부피 V_l의 단위는 cm³/cm²이다. 유·수상 농도 분배비 K_lw를 사용하면 유효 용량은 다음과 같다.

\[
B_i(t)=V_a(t)+K_{lw,i}V_l,\qquad
V_a(t)=V_{a,\mathrm{fixed}}+W_0[\rho+(1-\rho)e^{-k_wt}].
\]

가스 전달계수 k_g, 피부 투과계수 k_s는 cm/min, 헤드스페이스 높이 H는 cm, 환기율 v와 수상 가수분해율 k_h는 1/min이다. K_aw는 공기·수상 농도 분배비이다.

\[
e_i(t)=\frac{k_{g,i}K_{aw,i}}{B_i(t)},\quad
u_i(t)=\frac{k_{s,i}}{B_i(t)},\quad
q_i(t)=k_{h,i}\frac{V_a(t)}{B_i(t)},\quad
r_i=\frac{k_{g,i}}H.
\]

이 네 유효 전이율의 단위는 모두 1/min이다.

\[
\dot F_i=-(e_i+u_i+q_i)F_i+r_iA_i,
\qquad \dot A_i=e_iF_i-(r_i+v)A_i,
\]
\[
\dot S_i=u_iF_i,\qquad \dot X_i=vA_i,\qquad \dot D_i=q_iF_i.
\]

따라서 각 원료에 대해 F+A+S+X+D=M_initial이 유지된다. 이미 분해된 초기 비율은 D(0)에 들어간다. 열린 공간 근사는 r=0인 같은 방정식으로 계산한다. 열린 공간 가정의 유효성 검사도 유지한다.

분해 생성물의 냄새와 질량은 별도 화학종으로 예측하지 않는다. D는 모물질 상당량으로서의 보존 장부다. 유·수상 순간 평형, 희석 상태의 독립 원료 이동, 부피 가산성을 가정한다.

## 2. 양수성을 보존하는 해석적 전이와 적분

한 구간에서 계수를 동결하면 자유 상태 y=(F,A)의 생성 행렬은 다음과 같다.

\[
G=\begin{pmatrix}-a&r\\ e&-d\end{pmatrix},\quad
a=e+u+q,\quad d=r+v.
\]

비대각 원소가 음수가 아닌 행렬이므로 정확한 전이는 비음수 상태를 보존한다. 코드는 커다란 쌍곡함수를 계산한 뒤 작은 지수와 곱하지 않고, 감쇠율을 분리한다.

\[
g=\sqrt{(a-d)^2+4er},\quad
\lambda_f=(a+d+g)/2,\quad
\lambda_s=\frac{ev+(u+q)r+(u+q)v}{\lambda_f}.
\]

영 전이율은 연속적인 극한으로 처리한다. 느린 감쇠율을 두 큰 수의 차이로 구하지 않아 뻣뻣한 계수 조합에서 상쇄 오차를 줄인다.

\[
e^{G\Delta}=e^{-\lambda_f\Delta}I+
\frac{e^{-\lambda_s\Delta}-e^{-\lambda_f\Delta}}{g}(G+\lambda_f I).
\]

구간 적분은 같은 전이에서 얻는다.

\[
\int_0^\Delta e^{Gs}ds
=\phi(\lambda_f)I+
\frac{\phi(\lambda_s)-\phi(\lambda_f)}g(G+\lambda_f I),\quad
\phi(\lambda)=\frac{1-e^{-\lambda\Delta}}\lambda.
\]

`expm1`, 가까운 고유값의 급수 전개, 영 감쇠율의 극한을 사용한다. 이 적분에서 피부 이동·분해·환기를 각각 계산한다. 공기 적분 자체도 반환하므로 환기율로 나누거나 큰 누적 배출량 두 개를 빼서 복원하지 않는다.

물질수지나 양수성이 깨진 결과를 강제로 정규화해 통과시키지 않는다. 최종 원료별 보존 검사와 유한·비음수 상태/적분 검사를 실패하면 점수 계산 후보로 전달하지 않는다.

## 3. 표시 시간과 독립적인 계산

내부 격자는 t_j=jh이다. 표시점을 추가한다고 격자를 다시 나누지 않는다. 각 구간의 가운데 시각에서 G를 동결하며, 구간 안의 관측은 같은 G와 같은 구간 시작 상태에서 계산한다. 부분 관측이 본 진행 상태를 바꾸지 않는다.

노출 창 [a,b]가 격자 안에서 시작하면 먼저 그 시각의 상태를 같은 전이로 계산한 뒤 교집합 길이만 적분한다. 후기 창에서도 I(b)-I(a)를 사용하지 않는다. 작은 구간 기여량은 보상 합산으로 누적한다.

이 방법은 상수 계수에서는 해석해이며, 시간에 따라 물이 줄어드는 경우에는 **2차 정확도의 중점 계수 근사**이다. 시간 의존 행렬끼리는 일반적으로 교환하지 않으므로 전체 구간의 정확한 해라고 주장하지 않는다. 독립 ODE와 격자 세분화로 오차 감소를 확인한다.

표시점 불변성은 같은 배합의 전방 상태와 같은 창의 노출 적분에 대한 성질이다. 기존 `minimum_air_concentration_mg_m3` 제약은 여전히 0분을 제외한 요청 시각마다 검사한다. 표시 시각에 새 물리 제약을 추가하면 최적 배합까지 같다고 보장하지 않는다.

## 4. 향 노출과 역설계 연결

원료 i의 창별 평균 공기 농도는 다음과 같다.

\[
\overline C_i[a,b]=\frac{10^6}{H(b-a)}\int_a^b A_i(t)dt.
\]

공기 질량 적분의 mg·min/cm²를 mg·min/m³로 바꾸는 데 10^6/H를 사용한다. 평균 농도를 원료의 공기 냄새 역치로 나눈 값이 평균 OAV 프록시이다. 이는 측정된 주관적 강도가 아니다.

동일 베이스·희석 선형 가정에서는 원료별 초기량에 비례해 상태와 적분이 바뀐다. 따라서 한 번의 다원료 전방 계산으로 모든 원료의 선형 응답 열을 얻는다. 원료마다 API나 전방 시뮬레이션을 반복하지 않는다.

배합 w, 원료별 평균 OAV 응답 R, 고정된 분자 향 프로필 p에 대해 비교 프로필은 다음과 같다.

\[
P_{s,k,h}(w)=\frac{\sum_iw_iR_{s,k,i}p_{h,i}}{\sum_iw_iR_{s,k,i}}.
\]

s는 물성 시나리오, k는 요청된 노출 창, h는 기존 applicability/use 관능 기술 헤드이다. 두 헤드는 개인차를 모두 설명하는 확률분포가 아니다. 기존 전체 축 겹침·코사인·제외 향·배경 구별 조건, 가격·원료 상한·흡수 상한과 95점 판정은 그대로 유지한다. 최종 배합을 새로 시뮬레이션하여 탐색 중 예측 점수와 1e-6 이내에서 일치하는지도 확인한다.

캐시 키에는 제형 계수·원료·시간뿐 아니라 노출 창과 적분 버전을 포함한다. 이전 사다리꼴 `integration_weights`는 호환용 진단만 남기고 `integration_weights_used=false`로 표시한다.

## 5. 통합 제품 모델의 적용 범위

향수·로션·바디워시 공통 전이 모델은 계수×시간으로 된 무차원 입력 범위를 갖는다. V65는 큰 건조율이 허용된 입력인데 긴 계산 구간 때문에 이 범위를 넘는 경우, 체크포인트 범위를 넓히거나 계수를 잘라내지 않고 시간 구간을 세분한다.

용량이 단조 감소하므로 구간 끝의 용량으로 환산한 이동률은 구간 내부 전이율의 상한이다. 이 상한과 무차원 한도를 이용해 분할 수를 정한다. 출력점에 의존하지 않으며 세분화 후 실제 원료 수×계산 구간 수의 작업 한도도 검사한다. 여러 제조 단계가 있으면 같은 요청의 남은 작업량을 공유·차감한다. 이는 적용 범위 보호이지 학습 정확도의 증명은 아니다.

건조 후 비반응 용량 비율이 부동소수점 오차로 1을 넘는 문제는 `1-w`만 계산하는 대신 `lipid + (1-(w+lipid))`로 보존 성분을 명시해 해결했다. 물리 비율을 임의로 낮추지 않는다.

## 6. 환경 조건의 의미

환기, 헤드스페이스 높이, 수분 감소, 도포량, 분배·투과·가수분해 계수는 식에 직접 들어간다. 온도·상대습도는 계수가 해당 적용 환경과 일치하는지 확인하는 문맥이다. 실측 활성화 에너지나 수분활성 자료 없이 임의의 온도·습도 보정식을 새로 만들지 않았다. 자연어 로션 설계가 사용하는 공학 추정 계수와 실제 측정 계수의 구분도 유지한다.

## 7. 실행 결과와 재현

수치 검증 기록: `.benchmarks/physical_exposure_v65/numerical-accepted/numerical-report.json`.

빠른 원료와 느린 원료의 증발률을 1/min, 0.01/min으로 둔 고정 계수 반례에서, 빠른 원료의 전체 노출 기여는 기존 표시점 적분으로 0.01266%였으나 해석적 적분은 50.20869%이다. 독립 닫힌식과의 최대 상대 오차는 4.40e-15였다. 합성 계수를 사용한 수학적 반례이지 실제 향료 측정치가 아니다.

수분 감소·역전달·피부 이동·초기 분해·가수분해를 함께 둔 독립 ODE 비교에서는 다음을 확인했다.

| 내부 간격(분) | 공기 노출 적분 절대 오차(mg·min/m³) | 최종 상태 최대 오차(mg/cm²) |
|---|---:|---:|
| 0.5 | 0.13210267 | 1.5594e-8 |
| 0.25 | 0.03301101 | 3.8985e-9 |
| 0.125 | 0.00824600 | 9.7463e-10 |

관측된 수렴 차수는 2.00064, 2.00118이다. 이 조건에서 질량 보존 최대 오차는 3.13e-17 mg/cm² 미만이다. 이 수치는 수치 해법의 검증이며 계수의 현실 정확도를 보증하지 않는다.

재현 명령:

```powershell
$env:PERFUMERY_AI_LOCAL_PROFILE='disabled'
$env:OMP_NUM_THREADS='1'
$env:OPENBLAS_NUM_THREADS='1'
$env:MKL_NUM_THREADS='1'
& tmp/rd-api-v2-verify/Scripts/python.exe -m pytest tests/test_physical_exposure_v65.py tests/test_unified_product_v60.py -W error -q
& tmp/rd-api-v2-verify/Scripts/python.exe scripts/verify_physical_exposure_v65.py --output .benchmarks/physical_exposure_v65/new-numerical-run
```

전체 요청 95점 달성이나 사용자 후각 95%를 입증한 결과가 아니다. 고정 분자 표현 범위와 기준 향의 출처 불확실성은 별도 문제이며 기준을 쉬운 것으로 바꿔 숨기지 않았다.

## 8. 실제 체크포인트 연결 결과

V64 때 실행한 같은 자연어 진단 8건을 기존 체크포인트·원료·물성 시나리오·95점 기준으로 재실행했다. 다만 전후 적분 정의가 다르므로 새 블라인드 정확도 실험이나 동일 점수 함수의 학습 비교는 아니다.

| 요청 | V64 점수 | V65 점수 | V65 통과 |
|---|---:|---:|---|
| fresh | 95.2000 | 95.0000 | 예 |
| clean | 88.8863 | 88.8863 | 아니오 |
| green | 93.4095 | 93.4095 | 아니오 |
| woody | 95.2000 | 95.0000 | 예 |
| musky | 92.0212 | 92.0212 | 아니오 |
| clean, powdery | 93.2870 | 93.2870 | 아니오 |
| clean, leathery | 92.1835 | 92.1835 | 아니오 |
| clean, earthy | 93.4329 | 93.4329 | 아니오 |

통과는 **2/8건으로 동일**, 실행 오류 0건이다. 모든 결과에 새 적분 버전이 들어갔고 배합비도 변경됐다. 원료별 배합비 절대 변화의 합은 요청별 3.89~28.20 퍼센트포인트였다. 이는 향이 더 좋다는 측정이 아니라 새 물리 응답이 실제 역설계에 연결됐다는 확인이다. 유한 탐색 예산의 `search_incomplete`는 8건 모두 true였으며 최적 배합을 증명한 것은 아니다. 동일 95점 허용오차 판정은 유지했다.

원본 결과: `.benchmarks/physical_exposure_v65/request-01/results.jsonl`, 실행 요약과 소스·데이터·프로필 고정 여부는 같은 폴더의 `summary.json`, `manifest.json`. 2개 작업자로 전체 109.65초였으며 일반적인 응답속도 향상으로 주장하지 않는다.

로컬 실제 ASGI API도 확인했다.

- `/v1/formulas`: 한국어 피오니·청사과 요청 HTTP 200, 점수 87.4298, 상태 `no_safe_match`. 이 요청은 성공 처방이 아니라 미달 결과를 정상 반환한 것이다.
- `/v1/applications/body-lotion/design`: woody 요청 HTTP 200, 점수 95.0000, 상태 `research_profile_target_met`.
- 두 요청 모두 반복 응답이 동일하며 캐시 적중. 첫 응답은 각각 41.33초, 17.24초. 운영 Modal 지연 시간 측정은 아니다.
- 기존 714개 고정 분자 예측 결과와의 최대 차이 0.0. 새 학습이나 새 홀드아웃 정확도 측정이 아니다.
- 3,830개 활성 원료 배치 예측·재사용 확인. 원료 필드와 기존 모델 해시는 변경되지 않았다.

기록: `.benchmarks/physical_exposure_v65/api-01/`.

별도로 실제 고정 통합 체크포인트와 dihydromyrcenol·linalyl acetate를 연결하고, 명시적인 합성 공정 계수로 향수·로션·바디워시 경로를 실행했다. 세 경로 모두 `research_prediction`, 146축 출력, 질량 보존·비음수·누적 손실 단조성을 확인했다. 바디워시에는 세척 이벤트 1건이 반영됐다. 실제 제품 방출 측정과 비교한 실험은 아니다.

## 9. 소프트웨어 검증과 로컬 상태

중복을 제거한 선택 검사 **211개 중 210개 통과**, 실패 1개. V65 신규 검사 26개는 모두 통과했다. 기존 `test_science_integrity` 1건은 `benchmarks/physsim_r2_transfer_final_strict.json`이 없어 실패했으며, 파일을 만들어 통과시키거나 검사를 약화하지 않았다. 전체 저장소 검사를 모두 통과했다고 주장하지 않는다.

검사 기록:

- `integration-02.xml`: 방출·로션 최적화·참조 평가·캐시·통합 모델·V64 탐색 회귀
- `numerics-accepted.xml`, `physical-final.xml`: 최종 V65 및 다단계 작업량 검사
- `chemistry.xml`: 기존 제형·방출 보조 모델 검사와 누락 파일 실패 기록
- 모두 `.benchmarks/physical_exposure_v65/` 아래에 보존

변경한 파일에 대한 Ruff 검사 통과. 최종 wheel에 포함된 Python 소스 137개가 현재 소스와 일치하고, 금지된 pickle/torch 직렬화 자산이 없는 것을 확인했다.

로컬 활성 카탈로그:

- `dist/physical-exposure-v65/candidate-02/catalog/catalog_manifest.json`
- manifest SHA256: `1101fe5c16d4793bc1cb8f7867a62ab78c1443f1147e5512f844e21bab726a03`
- 전체 원료 필드 digest: `8c962601b6567d56bd1043733b2b2eaccf9deb5d35e4aba70101271fe65bf139` — V64와 동일
- wheel SHA256: `92944542ce03aa62f043f0e73de34ac43925cb09ce4c5b050427c1e1ebd7eb8f`

`perfumery.local.json`을 위 번들에 연결했다. 기존 API 요청 형태는 유지하며 새 노출 적분·계산 버전·작업량 진단 필드는 추가 정보다. 사용자 또는 백엔드가 과거 사다리꼴 가중치로 새 점수를 재계산해서는 안 된다. 향수 전체 생성기의 모든 시간 가중 로직이나 통합 제품 모델의 보조 관능 적분까지 이 방식으로 바꾼 것은 아니다. 이번 해석적 노출 역설계의 직접 적용 대상은 관측 기준 로션 설계 경로다.

운영 배포는 수행하지 않았다. V65 변경만으로 기존 모델을 새로 학습했다거나 모든 향 요청이 95점을 달성했다고 표현하지 않는다.
