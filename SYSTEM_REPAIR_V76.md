# V76 전체 경로 수정 기록

이 기록은 코드 수정, 학습 결과, 전체 요청 평가를 구분한다. 코드 테스트 통과를 향의 실제 유사도로 환산하지 않는다. 배포와 Git push는 수행하지 않았다.

## 적용 범위

일부 대표 향이나 특정 원료 목록만 패치하지 않고, 공통 물성 결합기·원료 표현·학습 자료 생성·신경망·배합 투영·비선형 계산·제조 절차·API 입력 검증을 수정했다. 비교 기준은 기존 고정 요청 400개 × 향수/로션 2종이며, 원래 95점 기준을 유지한다.

전체 원료 등록부와 실제 활성 후보 집합은 다르다. 이번 물성 연결 점검의 분모는 활성 후보 3,830개다. 요청별 금지 원료·가격·제품 조건은 별도로 적용된다.

## 데이터와 향 표현

| 항목 | 수정 전 활성 후보의 관측값 | 수정 후 | 아직 추정 사전분포를 사용하는 수 |
|---|---:|---:|---:|
| 25°C 증기압 | 9 | 282 | 3,548 |
| 끓는점 | 17 | 597 | 3,233 |
| 기상 냄새 역치 | 80 | 92 | 3,738 |

근거: `.benchmarks/v76_system_repair/active-coverage-01/coverage.json`.

- 전체 자료의 증기압 2,554개, 끓는점 5,222개, 역치 364개는 '자료에 있는 분자 수'다. 활성 원료에 연결된 수로 표시하지 않는다.
- 정확한 분자 그래프·입체화학을 포함한 InChIKey·분자량·단위·온도를 대조한다. 기존 관측값을 추정값으로 덮어쓰지 않는다.
- PubChem 공개 냄새 역치 주석 819건을 전수 검토하고 기상 역치로 해석할 수 있는 출처만 사용했다. 자극 역치와 물속 농도를 섞지 않는다.
- 146차원 원료 프로필을 15개 향 축으로 줄이던 보조 경로를 전체 출처 프로필과 연결했다. fresh/aquatic/white_floral/amber가 누락되지 않는다.
- 고정 400개 요청 중 399개는 전체 프로필 경로를 사용한다. 나머지 하나는 첫 향을 '달지 않게'라고만 지정하여 양의 기준 향 프로필이 없는 요청이다. 누락 향 축 때문에 막힌 요청은 아니다.
- 요청 문구를 바꿔도 동일한 배합의 물리·향 예측값은 바뀌지 않게 했다. 목표 향은 수정 방향 계산에만 들어간다.

## 배합과 수치 계산에서 추가로 확인한 문제

1. **선형 배합 학습과 비선형 평가의 불일치:** 학습용 혼합 향과 역방향 배합 손실에 농도·몰분율·증기압·역치·포화·억제·증발을 포함한 공통 계산을 연결했다.
2. **0 투입량의 가짜 신호:** 0을 작은 양수로 바꾸던 계산을 제거했다. 0 투입량의 향 신호는 정확히 0이다.
3. **희석 원료의 용매 누락:** 원료 속 용매도 액상 몰수 분모에 포함한다. 지원하지 않는 용매를 임의의 물성으로 처리하지 않는다.
4. **용매 잔량의 잘못된 단정:** 용매 증발을 계산하지 않은 경우 희석 원료 전체 잔량을 단일 값으로 보고하지 않는다. 계산된 활성 향료 잔량과 알려지지 않은 용매 잔량을 구분한다.
5. **합계 보정 과정의 가짜 원료 투입:** 이미 합계가 1인 10원료 배합을 다시 투영하면서 나머지 수천 원료에 극미량이 생기는 현상을 재현했다. 부동소수점 잔차를 기존 양수 성분의 허용 범위에서만 보정하도록 변경했다. 실제로 입력한 극미량은 삭제하지 않는다.
6. **극미량 학습 기울기 차단:** 허용 범위 내부의 값도 고정 1e-7 여유값 때문에 경계로 취급하던 역전파를 수정했다. 진짜 극미량 성분의 학습 기울기를 보존한다.
7. **배합 합계 제약을 무시한 기울기 정규화:** 질량 보존 접공간에서 기울기를 계산하는 선택 경로를 추가했다. 상수 기울기의 반올림 오차가 정규화 후 큰 입력으로 증폭되지 않게 했다. 채택 여부는 제품별 검증으로 결정한다.
8. **희소 배합에도 전체 제곱 행렬 계산:** 실제 투입량이 0인 성분의 곱셈만 정확히 생략한다. 모든 후보의 진입 기울기는 계산하므로 후보군을 줄이는 근사가 아니다.
9. **수치상 작은 음수 때문에 422:** 로션 '플로럴, 화이트플로럴 향'에서 -1.67e-15가 발생해 계산이 중단되는 문제를 재현했다. 반올림 범위만 보정하고 모든 제약과 점수를 다시 검사한다.
10. **유화 입도 확률 계산:** 불균일 구간에서 확률밀도를 그대로 확률처럼 사용하지 않고 누적분포의 구간 차이로 적분한다.
11. **제품 간 악화를 가리는 모델 선택:** 향수/로션의 평균을 하나로 합쳐 선택하지 않는다. 제품별 오차와 95점 통과 건수를 함께 비교한다.
12. **학습 과정 보존:** 출처 파일, 실험 설정, 학습용 배합, 검증/시험 분리, 체크포인트, 실패 보고서를 별도 보존한다. 후보가 실패하면 승인 플래그를 바꾸지 않는다.

수치 결함 재현 파일: `.benchmarks/v76_system_repair/projection-reproduction-01/report.json`. float32/float64 각각 16개 배합에서 수정 후 가짜 원료 투입은 0건이다. 1e-9 배합 성분의 투영 역전파 값도 0에서 2/3으로 복구됐다.

물리 계산 비교: `.benchmarks/v76_system_repair/sparse-physics-benchmark-01/report.json`. 후보 3,830개·실제 투입 12개·146차원 프로필·16회 물성 시나리오에서 손실값 차이는 0, 예측값 최대 차이는 약 3.5e-18이었다. CPU 중간 계산 시간은 값만 구할 때 0.1942→0.0073초, 기울기까지 구할 때 0.3541→0.0383초였다. 이 결과는 해당 계산 커널의 비교이며 전체 API 응답 시간이나 향 정확도 비교가 아니다.

## 제조 과정과 실제 실험 자료

한 체크포인트의 공통 표현을 사용하며 제품별 계산 조건을 유지한다. 향수와 로션의 방출 계산을 같은 것으로 취급하지 않는다.

- 공개 액상 제형 실험 812건을 확보해 성분 조합별 분리 후 학습했다. 안정성·탁도·전단속도별 점도 출력이 추가되었다. 이 자료는 특정 세정용 수계 제형의 단기 실험이며 임의의 바디로션·피부 안전성 자료가 아니다.
- 공개 향료 쌍 주석 166,814행을 확보했다. 정제·분할 후 실제 학습 자료를 따로 기록했다. 배합비가 없는 전문가 주석을 실측 50:50 혼합 향으로 둔갑시키지 않는다.
- 기존 유화 실험 자료를 사용하는 입도 예측도 같은 체크포인트에 유지했다.
- 제조 절차를 단일 순서에서 선행 조건 그래프로 변경했다. A/B상 준비는 가능한 순서로 병행할 수 있고 향수는 비교·수정 후 새 배합으로 되돌아갈 수 있다.
- 제조 그래프 학습에서는 절차 인코더와 작업/검사 출력만 재학습했다. 향·수송·실험 결과 예측 경로가 바뀌지 않았는지 별도로 확인한다.
- 추가로 조사한 30회 스킨케어 유화 실험 논문은 비상업·변경금지 조건을 포함하므로 일반 서비스용 학습 자료로 승격하지 않았다.

출처 규칙으로 생성한 제조 이력과 실측 제조 결과는 계속 별도 표시한다. 전 세계 제조 방법과 실제 결과를 모두 학습했다는 상태는 아니다.

공개 출처: [812개 액상 제형 자료](https://springernature.figshare.com/articles/software/Liquid_Formulations_Dataset/25451878), [실험 방법 논문](https://www.nature.com/articles/s41597-024-03573-w), [향료 쌍 주석 저장소](https://github.com/odor-pair/odor-pair). 실제 사용한 원본 버전과 해시는 각 자료 획득 매니페스트에 보존했다.

## API와 결합 계약

- 기존 레시피 요청 주소와 인증 방식은 변경하지 않는다.
- `/v1/formulation-workflows/observed-outcomes`: 학습한 정확한 기준 공정과 18개 성분 조건의 실험 결과 예측.
- `/v1/formulations/pair-annotations`: 두 분자의 향 주석 예측. 실제 배합비/향 유사도 보장으로 해석하지 않는다.
- 제조 안내는 가능한 다음 작업 목록과 신경망의 원래 예측을 함께 반환한다. 출처 제약으로 보정한 것을 신경망의 원래 정답으로 보고하지 않는다.
- 잘못된 성분 비율 자료형·불리언·NaN은 입력 검증 오류로 처리한다.
- 물성 자료 해시, 모델, 기준 향, 원료 카탈로그, 코드 패키지 결합을 검사한다.

## 검증 상태

### 추가로 확인된 향수 목표 정의 충돌

현재 향수의 주 채점은 19축 키워드 벡터를 사용한다. 예를 들어 '시트러스 향'은 시트러스 1, 다른 축 0인 목표다. 반면 보조 신경망은 같은 요청에 대해 과일·그린 등의 배경을 포함한 고정 출처 참조 프로필을 사용한다. 출처 참조에 대한 보존 조건 때문에 주 채점에서 개선된 후보가 거부될 수 있다.

전체 생성 중간 진단의 **향수 90건**에서는 주 점수가 더 높은 후보를 보조 가이드가 막은 경우가 69건, 명목 코사인은 95 이상이지만 전체 분포 겹침은 95 미만인 경우가 51건이었다. 이는 중간 진단이며 400건 전체 결과가 아니다. 파일: `.benchmarks/v76_system_repair/partial-conflict-audit-01/report.json`.

이 문제를 통과율 숫자를 올리기 위해 가이드를 끄는 방식으로 처리하지 않았다. 출처 참조를 자연어의 주 목표로 통일하는 것은 계산 기준 변경이므로 사용자의 선택을 요청했다. 기존 기준의 800회 검증은 그대로 유지한다. 새 기준을 채택한다면 같은 고정 참조로 이전·이후 배합을 다시 비교해야 하며, 이전 19축 점수와 동일한 성능 수치로 취급하면 안 된다.

- 최초 전체 회귀: 1,885개 통과, 1개 건너뜀.
- 공통 배합 투영·희소 물리 계산·제조 경로를 수정한 후 전체 회귀: **1,907개 통과, 1개 건너뜀**. 이후 학습용 CPU 투영 연결 26개, 관측 결과 출력 10개 회귀 테스트도 통과했다. 숫자를 중복 합산하지 않는다.
- 기존 버전 전체 800회 생성 결과: 향수 42/400, 로션 247/400.
- 기존 배합만 새로운 공통 계산 기준으로 재평가: 향수 24건, 로션 0건이 95점 이상. 이는 새 배합 생성 결과가 아니며, 물성/표현 변경에 따른 점수 이동을 분리하기 위한 비교 기준이다.
- V76 통합 본체 학습은 관측 제형·쌍 주석·비선형 혼합 학습 검증을 통과했다. 초기 역방향 배합 후보는 제품별 비회귀 기준에 실패하여 승인하지 않았다.
- 이후 학습 중 투영/극미량 역전파 결함이 발견되어 해당 실험을 중단하고, 수정한 전체 배합 자료로 다시 학습한다. 중단 실험을 완료된 성능 결과로 사용하지 않는다.

수정 후 `inverse-training-06`은 768개 학습 배합, 192개 검증 배합, 별도 384개 시험 배합을 사용했다. 두 제품과 후보 수 8/32/128/512/2048/전체를 모두 포함한다. 향수는 공통 비선형 헤드스페이스 계산, 로션은 제형별 수송 계산을 손실에 사용한다.

| 별도 역방향 배합 시험 | 기존 기본 설정(8회) | 새 설정(16회) |
|---|---:|---:|
| 향수 평균 손실 | 0.267760 | 0.259923 |
| 향수 95점 이상 | 5/192 | 6/192 |
| 로션 평균 손실 | 0.317253 | 0.315284 |
| 로션 95점 이상 | 52/192 | 53/192 |

이 비교는 반복 횟수도 증가한 구성 비교다. 기존 가중치도 16회 실행하면 평균 손실은 향수 0.259783, 로션 0.315279였고, 통과 건수는 새 가중치와 같았다. 따라서 **새 가중치 자체의 동일 계산량 우위는 입증되지 않았다.** 시험 점수나 합격 기준을 조정하지 않았다. 이 역방향 배합 시험을 서비스의 고정 400개 자연어 요청 시험과 혼동하지 않는다.

제조 그래프 가중치를 동일 체크포인트에 병합한 뒤 기존 향·수송·유화·관측 결과의 무이력 입력 예측값이 그대로인지 확인했다. 모델/기준 향/물성/카탈로그를 새 소스 패키지에 묶었다.

- 모델: `.benchmarks/v76_system_repair/assembled-01/model.json`
- 모델 SHA256: `158b9f82ff9fb784eeec114ef1f7bbfc4d613badeb98f46e0f60435d5af38d2f`
- 명시적 로컬 프로필: `perfumery.v76-system-01.local.json`
- 패키지 결합 기록: `.benchmarks/v76_system_repair/package-01/preparation.json`
- 실제 로컬 API 연결 결과: `.benchmarks/v76_system_repair/api-verification-02/report.json`
- 전체 400개 × 2종 생성 평가: `.benchmarks/v76_system_repair/full400-after-01/`

로컬 API에서 제조 안내, 관측 제형 예측, 향료 쌍 주석 예측을 확인했다. 자료가 없는 제형은 참조 공정으로 단정하지 않고, 잘못된 비율 자료형은 422로 반환한다. 이전에 계산 오류가 나던 플로럴/화이트플로럴 로션은 HTTP 200, 95.0001점으로 생성됐다. 별도 향수 예시는 HTTP 200이지만 79.6747점으로 미달했다. 한 예시 성공을 전체 달성으로 쓰지 않는다.

현재 이 문서만으로 전체 요청 95점 달성, 실제 후각 90% 또는 상용 배포 완료를 주장할 수 없다. 최종 모델 승인과 전체 생성 결과는 별도의 실험 산출물로 기록한다.
