# V72 전체 요청 개선·모델 비교

2026-09-14. 로컬 작업이며 배포·GitHub push·기본 모델 승격은 하지 않았다.

## 목표와 비교 조건

향수 400건과 바디로션 400건 모두 기존 95점 기준을 통과시키는 것이 목표다. 현재 이 목표를 달성했다는 결과는 없다. 통과 기준을 낮추거나 계산이 실패한 요청을 분모에서 제외하지 않는다.

1. 분리 모델 구성과 V69 통합 모델 구성을 동일한 V71 코드·원료·목표 벡터·물리 계산·95점 기준에서 새로 계산한다. 총 1,600회다.
2. V72 개선 후보는 같은 요청 800건을 별도로 계산한다.
3. 두 실험이 모두 끝나야 제품별 통과·새 통과·기존 통과 손실·점수 변화·오류를 확정한다. 일부 실행 결과로 우승 모델을 선언하지 않는다.

고정 요청에는 한국어·영어 1축·2축 조합과 시간대별 향 전환 사례가 들어 있다. 이것은 가능한 모든 자연어 요청에 대한 증명이나 실제 제품의 인간 후각 유사도 측정이 아니다. 로컬 CPU 병렬 실행이므로 Modal 단독 응답시간 평가로도 쓰지 않는다.

## 구현한 개선

- 향수: 전체 후보 원료에 대한 부분 질량 이동을 추가했다. 새 원료의 허용량이 기존 원료의 투입량보다 작아도, 기존 원료를 전부 바꾸지 않고 일부만 교체할 수 있다. 농도·시간·상호 억제·가격·원료별 상한·필수 원료 최소량을 함께 계산한다. 최종 200회 물리 계산과 기존 통과 조건은 유지한다.
- 로션: 두 종류의 관측 향 프로필, 모든 시간·방출 시나리오, 146개 향 항목을 보존한 직접 원뿔 최적화를 연결했다. 원료 열을 단계적으로 늘리되 빠진 모든 원료의 최적화 기여를 검사한다. 일부 원료 집합의 계산 실패를 전체 불가능으로 바꾸지 않는다.
- 로션: 95점 API 요청에 별도로 붙어 있던 탐색 전용 95.2점 여유를 제거했다. 수치 여유는 0.0001점만 두며 실제 최종 판정 기준은 여전히 95점이다.
- 로션: 비용 최적화가 끝나기 전에 발견한 유효한 배합을 버리지 않는다. 향 구별 조건을 통과하지 못한 고득점 중간값이 모든 조건을 통과한 후보를 밀어내는 오류도 재현해 수정했다.
- 원료 데이터: 동결한 공개 원료 관측값 138개 분자 구조를 읽는 별도 연결을 추가했다. 정확한 구조가 맞을 때만 원본 146차원 값을 사용하고, 예측 캐시에는 관측값을 섞어 저장하지 않는다.
- 실행 효율: 화학 구조에서 계산한 동일한 분자 특성을 재사용한다. 호출마다 반환하는 배열은 독립적이어서 한 요청의 변경이 다른 요청의 입력을 오염시키지 않는다.

이 후보는 V69 신경망 체크포인트를 사용한다. V72라는 이름으로 새 신경망 학습이 완료되었다고 주장하지 않는다.

## 현재 끝난 검증

- 영향 범위 회귀 테스트: **201개 통과, 실패·오류·skip 0건**.
- 원료 프로필 연결 전수 검사: 향수·로션 각각 **3,830개 전부 확인**. 관측 프로필 직접 연결 78개, 모델 예측 3,744개, 구조 연결 불가 8개다. 두 제품이 공유하는 원료 향 표현값은 동일하며 제품별 물리 계산은 별도다.
- 관측값은 원본과 정확히 동일하고, 146개 항목·두 관측 구분이 모두 보존된다.
- 화학 특성 캐시: 활성 원료 3,830개에서 기존 값 또는 기존 오류가 전부 동일했다. 이 검사는 카탈로그에 직접 적힌 구조를 검사하므로, 모델의 별도 구조 연결까지 포함하는 위 검사와 연결 가능 원료 수가 다르다.
- 초기 향수·로션 API 연결 검사는 정상 응답했다. 이 소수 확인을 전체 800건의 성능으로 계산하지 않는다.

근거:

- `.benchmarks/v72_model_compare/final-regression-01.xml`
- `.benchmarks/v72_model_compare/all-component-coverage-02.json`
- `.benchmarks/v72_model_compare/all-descriptors-01.json`
- `.benchmarks/v72_model_compare/cone-retention-before.xml`
- `.benchmarks/v72_model_compare/cone-retention-after.xml`

관측 프로필 출처와 목표 참조 출처가 겹친다. 이것은 알려진 원료의 참조 프로필 연결이며, 새로운 블라인드 성능 검증이 아니다. 미연결 8개 원료에 임의의 분자 구조나 가짜 관능값을 넣지 않았다.

## 고정 후보 및 실행 기록

- 후보 준비 기록: `.benchmarks/v72_model_compare/candidate-02/preparation.json`
- Wheel SHA256: `8a4c9989a19795f5af379897c75eb3438e285c1b8178e4a60daa4bf35932a31a`
- 후보 프로필: `perfumery.v72-candidate-02.local.json`
- 후보 프로필 SHA256: `ce808008a6c20fcf472ba15d027e6e7d72cd286e604a9f79cde9afa4087aac21`
- 신경망 SHA256: `3186e467446abc71f249c052c8248c17c97d0e337976abf3666628af47dd997f`
- 원료 내용 digest: `8c962601b6567d56bd1043733b2b2eaccf9deb5d35e4aba70101271fe65bf139`
- 분리/통합 전체 비교: `.benchmarks/v72_model_compare/full400-01`
- 개선 후보 전체 실행: `.benchmarks/v72_model_compare/improved-full400-02`
- 완료 후 통합 보고서 경로: `.benchmarks/v72_model_compare/comparison-final-02`

두 실행은 작업 중인 소스가 아니라 각각의 고정 wheel을 풀어 사용한다. 실행 중 해당 wheel·프로필·참조·스크립트를 수정하거나 덮어쓰지 않는다. 새 후보 실행은 CPU 스레드를 1로 고정하고 Clarabel 0.11.1, NumPy 2.2.6, SciPy 1.15.2, RDKit 2025.9.4를 사용한다.

첫 번째 개선 후보 실행 `improved-full400-01`은 위 배합 유지 오류를 재현한 뒤 중단했다. 원본 19개 완료 결과와 입력·출력·해시는 그대로 보존했으며, 전체 성능 결과나 최종 통과율로 사용할 수 없다. 거부 사유는 해당 폴더의 `EARLY_REJECTION.json`에 있다. 분리/통합 비교 실행은 중단하지 않았다.

전체 실행의 `summary.json`에 `complete: true`가 쓰이기 전에는 미완료다. 완료 집계는 원본 응답 해시까지 확인하고, 400/400에 도달하지 못하면 그대로 미달로 기록한다. 기본 로컬 프로필·운영 배포·백엔드 인증은 이 실험 결과만으로 자동 변경하지 않는다.

## 중간 결과의 회귀 점검

`.benchmarks/v72_model_compare/paired-progress-01.json`은 701/2,400개 실행 시점의 부분 점검이다. 분모를 줄인 전체 성능 결과가 아니다. 해당 시점에 공통으로 계산된 요청 중 통합 구성 대비 후보의 기존 통과 손실이 향수 1건(`ko-5`), 로션 1건(`en-3`) 발견됐다. 따라서 이 후보를 완전한 상위 버전으로 승격하지 않았다.

관측 프로필 연결은 예측값 자체를 바꾸므로, 예전 탐색 순서를 유지했다고 예전 탐색 결과까지 동일하다고 할 수 없다. 로션에서는 시간 제한 종료도 남아 있다. 전체 비교를 보존해 최종 손실·이득과 실행 미완료를 구분한다. 통과율만 올리기 위해 관측 원본을 버리거나 최종 기준을 낮추지는 않는다.
